import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import test from "node:test";

function readAppShellSource(relativePath: string): string {
  return readFileSync(
    resolve(process.cwd(), "apps", "demo-frontend", "app-shell", "src", ...relativePath.split("/")),
    "utf8",
  );
}

function readRepoSource(relativePath: string): string {
  return readFileSync(resolve(process.cwd(), ...relativePath.split("/")), "utf8");
}

test("app shell wraps routes with the workspace runtime provider", () => {
  const source = readAppShellSource("App.tsx");

  assert.match(source, /import \{ WorkspaceRuntimeProvider \} from "@\/hooks\/useWorkspaceRuntime";/);
  assert.match(source, /<WorkspaceRuntimeProvider>/);
  assert.match(source, /<\/WorkspaceRuntimeProvider>/);
  assert.match(source, /<Route path="\/app\/console\/runtime" element=\{<ConsoleRuntime \/>} \/>/);
});

test("live desk and console surfaces prefer repo-owned runtime data with draft fallback support", () => {
  const liveDesk = readAppShellSource("components/workspace/LiveDesk.tsx");
  const consoleStage = readAppShellSource("components/workspace/ConsoleStage.tsx");
  const consoleRuntime = readAppShellSource("pages/ConsoleRuntime.tsx");
  const caseWikiPanel = readAppShellSource("components/workspace/CaseWikiPanel.tsx");
  const caseVaultPanel = readAppShellSource("components/workspace/CaseVaultPanel.tsx");
  const runtimeDiagnosticsPanels = readAppShellSource("components/workspace/RuntimeDiagnosticsPanels.tsx");
  const sessionBoundaryPanel = readAppShellSource("components/workspace/SessionBoundaryPanel.tsx");
  const sessionOpsPanel = readAppShellSource("components/workspace/SessionOpsPanel.tsx");
  const artifactViewerPanel = readAppShellSource("components/workspace/ArtifactViewerPanel.tsx");
  const artifactViewerLib = readAppShellSource("lib/runtime-artifact-viewer.ts");
  const replayRuntime = readAppShellSource("lib/runtime-session-replay.ts");
  const workspaceRuntime = readAppShellSource("hooks/useWorkspaceRuntime.tsx");
  const consolePage = readAppShellSource("pages/Console.tsx");
  const bundlePage = readAppShellSource("pages/Bundle.tsx");
  const evidenceDetailPage = readAppShellSource("pages/EvidenceDetail.tsx");

  assert.match(liveDesk, /const \{ cases, deviceNodes, addDraftCase, getCaseWikiByRef \} = useWorkspaceRuntime\(\);/);
  assert.match(liveDesk, /existingCases=\{cases\}/);
  assert.match(liveDesk, /addDraftCase\(draft\);/);
  assert.match(liveDesk, /const activePlaybookWiki = useMemo/);
  assert.match(liveDesk, /deviceNodes\.find\(\(n\) => n\.id === nodeFilterId\)/);
  assert.match(liveDesk, /buildCaseVaultPath/);
  assert.match(liveDesk, /Open Case Vault/);
  assert.match(liveDesk, /handleOpenCaseVault/);

  assert.match(consoleStage, /const \{ deviceNodes, getCaseByRef, getCaseWikiByRef \} = useWorkspaceRuntime\(\);/);
  assert.match(consoleStage, /const baseCase = getCaseByRef\(caseRef\);/);
  assert.match(consoleStage, /deviceNodes\.find\(\(n\) => n\.id === c\.sourceNodeId\)/);
  assert.match(consoleStage, /buildCaseVaultPath/);
  assert.match(consoleStage, /buildCaseRuntimeSupportPath/);
  assert.match(consoleStage, /handleOpenCaseVault/);
  assert.match(consoleStage, /label="Open Case Vault"/);
  assert.match(consoleStage, /Runtime support/);
  assert.match(consoleStage, /const showRuntimeSupportStrip = runtimeSupportItems\.length > 0;/);
  assert.match(consoleStage, /if \(exportReady !== true\)/);
  assert.match(consoleStage, /if \(!proofPublished\)/);
  assert.match(consoleStage, /if \(replayNeedsAttention\)/);
  assert.match(consoleStage, /const runtimeSupportCta = exportReady !== true/);
  assert.match(consoleStage, /Inspect compliance blocker/);
  assert.match(consoleStage, /Inspect raw artifact blocker/);
  assert.match(consoleStage, /Inspect signature pending/);
  assert.match(consoleStage, /Inspect unsigned proof/);
  assert.match(consoleStage, /const signatureStatus = wiki\?\.evidenceSignature\?\.status \?\? null;/);
  assert.match(consoleStage, /const compliancePrimaryAction = complianceRemediation\?\.primaryAction \?\? null;/);
  assert.match(consoleStage, /const hasRawArtifactBlocker =/);
  assert.match(consoleStage, /const hasSignatureBlocker =/);
  assert.match(consoleStage, /function formatRuntimeSupportRef/);
  assert.match(consoleStage, /const runtimeSupportHint = exportReady !== true/);
  assert.match(consoleStage, /operatorActionLabel/);
  assert.match(consoleStage, /blockingRef/);
  assert.match(consoleStage, /id="action-queue"/);
  assert.match(consoleStage, /id="live-activity"/);
  assert.doesNotMatch(consoleStage, /SessionBoundaryPanel/);
  assert.doesNotMatch(consoleStage, /CaseWikiPanel/);
  assert.doesNotMatch(consoleStage, /SessionOpsPanel/);
  assert.doesNotMatch(consoleStage, /RuntimeDiagnosticsPanels/);
  assert.match(consoleRuntime, /const \{ defaultConsoleCaseRef, getCaseByRef, getCaseWikiByRef \} = useWorkspaceRuntime\(\);/);
  assert.match(consoleRuntime, /<Topbar section="Operator Runtime" caseRef=\{caseRef\} \/>/);
  assert.match(consoleRuntime, /<SessionBoundaryPanel caseValue=\{runtimeCase\} wiki=\{wiki\} \/>/);
  assert.match(consoleRuntime, /<CaseWikiPanel caseValue=\{runtimeCase\} wiki=\{wiki\} \/>/);
  assert.match(consoleRuntime, /<CaseVaultPanel caseValue=\{runtimeCase\} wiki=\{wiki\} \/>/);
  assert.match(consoleRuntime, /<SessionOpsPanel caseValue=\{runtimeCase\} wiki=\{wiki\} \/>/);
  assert.match(consoleRuntime, /<RuntimeDiagnosticsPanels caseValue=\{runtimeCase\} wiki=\{wiki\} \/>/);
  assert.match(consoleRuntime, /const initialArtifactPath = params\.get\("artifact"\);/);
  assert.match(consoleRuntime, /const initialArtifactIssue = params\.get\("issue"\);/);
  assert.match(consoleRuntime, /const initialArtifactSection = params\.get\("section"\);/);
  assert.match(consoleRuntime, /const initialFocusedOnly = params\.get\("focusedOnly"\) === "1";/);
  assert.match(consoleRuntime, /const handleFocusedOnlyChange = \(enabled: boolean\) => \{/);
  assert.match(consoleRuntime, /const nextParams = new URLSearchParams\(params\);/);
  assert.match(consoleRuntime, /nextParams\.set\("focusedOnly", "1"\);/);
  assert.match(consoleRuntime, /nextParams\.delete\("focusedOnly"\);/);
  assert.match(consoleRuntime, /<ArtifactViewerPanel[\s\S]*initialArtifactPath=\{initialArtifactPath\}[\s\S]*initialArtifactIssue=\{initialArtifactIssue\}[\s\S]*initialArtifactSection=\{initialArtifactSection\}[\s\S]*initialCaseRef=\{runtimeCase\.ref\}[\s\S]*initialFocusedOnly=\{initialFocusedOnly\}[\s\S]*onFocusedOnlyChange=\{handleFocusedOnlyChange\}[\s\S]*\/>/);
  assert.match(consoleRuntime, /navigate\(`\/app\/console\$\{search \? `\?\$\{search\}` : ""\}\$\{hash\}`/);
  assert.match(caseWikiPanel, /Copy handoff/);
  assert.match(caseWikiPanel, /Copy refs/);
  assert.match(caseWikiPanel, /Open bundle/);
  assert.match(caseWikiPanel, /Open evidence/);
  assert.match(caseWikiPanel, /Inspect proof/);
  assert.match(caseWikiPanel, /Open Case Vault/);
  assert.match(caseWikiPanel, /id="case-wiki"/);
  assert.match(caseWikiPanel, /buildCaseVaultPath/);
  assert.match(caseWikiPanel, /buildRuntimeArtifactViewerPath/);
  assert.match(caseWikiPanel, /buildRuntimeArtifactIssueViewerPath/);
  assert.match(caseWikiPanel, /RUNTIME_ARTIFACT_VIEW_PRESETS\.runtimeProof/);
  assert.match(caseWikiPanel, /exportReady === false/);
  assert.match(caseWikiPanel, /operatorPreviewPack\?\.remediation\?\.draft/);
  assert.match(caseWikiPanel, /compliance\?\.enforcement\?\.summary/);
  assert.match(caseWikiPanel, /evidenceSignature\?\.status/);
  assert.match(caseWikiPanel, /const remediationPrimaryAction = complianceEnforcement\?\.remediation\?\.primaryAction \?\? null;/);
  assert.match(caseWikiPanel, /const remediationHint = \[/);
  assert.match(caseWikiPanel, /formatRemediationRef/);
  assert.match(caseWikiPanel, /Raw artifact blocker/);
  assert.match(caseWikiPanel, /Signature pending/);
  assert.match(caseWikiPanel, /Next repo-owned step:/);
  assert.match(caseWikiPanel, /Inspect blocker evidence/);
  assert.match(caseWikiPanel, /Inspect signature evidence/);
  assert.match(caseWikiPanel, /Inspect export evidence/);
  assert.match(caseWikiPanel, /Inspect proof evidence/);
  assert.match(workspaceRuntime, /blockingReasons\?: string\[\] \| null;/);
  assert.match(workspaceRuntime, /primaryAction\?: \{/);
  assert.match(caseVaultPanel, /id="case-vault"/);
  assert.match(caseVaultPanel, /Inspectable memory projection/);
  assert.match(caseVaultPanel, /Rowboat-style/);
  assert.match(caseVaultPanel, /Memory anchors/);
  assert.match(caseVaultPanel, /Open threads/);
  assert.match(caseVaultPanel, /Evidence map/);
  assert.match(caseVaultPanel, /Linked entities/);
  assert.match(caseVaultPanel, /Recent memory trail/);
  assert.match(caseVaultPanel, /Operator handoff/);
  assert.match(caseVaultPanel, /CRM prep/);
  assert.match(caseVaultPanel, /Operator handoff projection/);
  assert.match(caseVaultPanel, /CRM prep projection/);
  assert.match(caseVaultPanel, /Copy memory/);
  assert.match(caseVaultPanel, /Copy handoff/);
  assert.match(caseVaultPanel, /Copy CRM prep/);
  assert.match(caseVaultPanel, /Export Markdown/);
  assert.match(caseVaultPanel, /Export CRM Markdown/);
  assert.match(caseVaultPanel, /Open bundle/);
  assert.match(caseVaultPanel, /Open evidence/);
  assert.match(caseVaultPanel, /Case Vault handoff ready/);
  assert.match(caseVaultPanel, /Case Vault export blocked/);
  assert.match(caseVaultPanel, /Case Vault CRM prep ready/);
  assert.match(caseVaultPanel, /Case Vault CRM prep blocked/);
  assert.match(caseVaultPanel, /Inspect proof/);
  assert.match(caseVaultPanel, /buildRuntimeArtifactIssueViewerPath/);
  assert.match(caseVaultPanel, /const remediationPrimaryAction = complianceEnforcement\?\.remediation\?\.primaryAction \?\? null;/);
  assert.match(caseVaultPanel, /const remediationHint = \[/);
  assert.match(caseVaultPanel, /formatRemediationRef/);
  assert.match(caseVaultPanel, /Raw artifact blocker/);
  assert.match(caseVaultPanel, /Signature pending/);
  assert.match(caseVaultPanel, /Next repo-owned step:/);
  assert.match(caseVaultPanel, /Inspect blocker evidence/);
  assert.match(caseVaultPanel, /Inspect signature evidence/);
  assert.match(caseVaultPanel, /Inspect export evidence/);
  assert.match(caseVaultPanel, /Inspect export posture/);
  assert.match(sessionBoundaryPanel, /Session Boundary/);
  assert.match(sessionBoundaryPanel, /id="connections"/);
  assert.match(sessionBoundaryPanel, /fetchRuntimeSessionReplay/);
  assert.match(sessionBoundaryPanel, /buildRuntimeSessionReplaySummary/);
  assert.match(sessionBoundaryPanel, /buildRuntimeArtifactViewerPath/);
  assert.match(sessionBoundaryPanel, /RUNTIME_ARTIFACT_VIEW_PRESETS\.runtimeProof/);
  assert.match(sessionBoundaryPanel, /Inspect proof/);
  assert.match(sessionBoundaryPanel, /Proof ingress:/);
  assert.match(sessionBoundaryPanel, /Turn ingress:/);
  assert.match(sessionBoundaryPanel, /After refresh/);
  assert.match(sessionOpsPanel, /Operator Session Ops/);
  assert.match(sessionOpsPanel, /id="session-ops"/);
  assert.match(sessionOpsPanel, /Export Markdown/);
  assert.match(sessionOpsPanel, /Export JSON/);
  assert.match(sessionOpsPanel, /Refresh replay/);
  assert.match(sessionOpsPanel, /Refresh Case Wiki/);
  assert.match(sessionOpsPanel, /buildRuntimeArtifactViewerPath/);
  assert.match(sessionOpsPanel, /buildRuntimeArtifactIssueViewerPath/);
  assert.match(sessionOpsPanel, /RUNTIME_ARTIFACT_VIEW_PRESETS\.manifest/);
  assert.match(sessionOpsPanel, /Inspect manifest/);
  assert.match(sessionOpsPanel, /buildSessionExportPayload/);
  assert.match(sessionOpsPanel, /buildSessionExportMarkdown/);
  assert.match(sessionOpsPanel, /case wiki export blocked/);
  assert.match(sessionOpsPanel, /const remediationPrimaryAction = complianceEnforcement\?\.remediation\?\.primaryAction \?\? null;/);
  assert.match(sessionOpsPanel, /const remediationHint = \[/);
  assert.match(sessionOpsPanel, /formatRemediationRef/);
  assert.match(sessionOpsPanel, /Raw artifact blocker/);
  assert.match(sessionOpsPanel, /Signature pending/);
  assert.match(sessionOpsPanel, /Next repo-owned step:/);
  assert.match(sessionOpsPanel, /Inspect blocker evidence/);
  assert.match(sessionOpsPanel, /Inspect signature evidence/);
  assert.match(sessionOpsPanel, /Inspect export evidence/);
  assert.match(sessionOpsPanel, /Inspect export posture/);
  assert.match(replayRuntime, /export async function fetchRuntimeSessionReplay/);
  assert.match(replayRuntime, /export function buildRuntimeSessionReplaySummary/);
  assert.match(replayRuntime, /export function buildSessionExportPayload/);
  assert.match(replayRuntime, /export function buildSessionExportMarkdown/);
  assert.match(replayRuntime, /latestProofIngressSource/);
  assert.match(replayRuntime, /primaryStepRefreshFollowupPath/);
  assert.match(runtimeDiagnosticsPanels, /Workflow Runtime/);
  assert.match(runtimeDiagnosticsPanels, /Runtime Guardrails/);
  assert.match(runtimeDiagnosticsPanels, /Bootstrap Doctor/);
  assert.match(runtimeDiagnosticsPanels, /Browser Workers/);
  assert.match(runtimeDiagnosticsPanels, /id="safety-rules"/);
  assert.match(runtimeDiagnosticsPanels, /id="health-check"/);
  assert.match(runtimeDiagnosticsPanels, /readJsonDataRecord\(\s*"\/v1\/runtime\/workflow-config"/);
  assert.match(runtimeDiagnosticsPanels, /readJsonDataRecord\(\s*"\/v1\/runtime\/workflow-control-plane-override"/);
  assert.match(runtimeDiagnosticsPanels, /readJsonDataRecord\(\s*"\/v1\/runtime\/bootstrap-status"/);
  assert.match(runtimeDiagnosticsPanels, /fetchRuntimeApi\(\s*"\/v1\/runtime\/auth-profiles"/);
  assert.match(runtimeDiagnosticsPanels, /fetchRuntimeApi\(\s*"\/v1\/runtime\/auth-profiles\/rotate"/);
  assert.match(runtimeDiagnosticsPanels, /readJsonDataRecord\(\s*"\/v1\/runtime\/browser-jobs\?limit=6"/);
  assert.match(runtimeDiagnosticsPanels, /buildRuntimeArtifactViewerPath/);
  assert.match(runtimeDiagnosticsPanels, /buildRuntimeArtifactIssueViewerPath/);
  assert.match(runtimeDiagnosticsPanels, /RUNTIME_ARTIFACT_VIEW_PRESETS\.report/);
  assert.match(runtimeDiagnosticsPanels, /RUNTIME_ARTIFACT_VIEW_PRESETS\.badgeDetails/);
  assert.match(runtimeDiagnosticsPanels, /Refresh workflow/);
  assert.match(runtimeDiagnosticsPanels, /Inspect report/);
  assert.match(runtimeDiagnosticsPanels, /Clear override/);
  assert.match(runtimeDiagnosticsPanels, /Refresh guardrails/);
  assert.match(runtimeDiagnosticsPanels, /Inspect badge/);
  assert.match(runtimeDiagnosticsPanels, /type RuntimeDiagnosticsPanelsProps = \{\s*caseValue: WorkspaceCase;\s*wiki: RuntimeCaseWiki \| undefined;/);
  assert.match(runtimeDiagnosticsPanels, /const remediationPrimaryAction = complianceEnforcement\?\.remediation\?\.primaryAction \?\? null;/);
  assert.match(runtimeDiagnosticsPanels, /formatRemediationRef/);
  assert.match(runtimeDiagnosticsPanels, /Raw artifact blocker/);
  assert.match(runtimeDiagnosticsPanels, /Signature pending/);
  assert.match(runtimeDiagnosticsPanels, /Next repo-owned step:/);
  assert.match(runtimeDiagnosticsPanels, /Inspect blocker evidence/);
  assert.match(runtimeDiagnosticsPanels, /Inspect signature evidence/);
  assert.match(runtimeDiagnosticsPanels, /Inspect export evidence/);
  assert.match(runtimeDiagnosticsPanels, /Workflow Runtime/);
  assert.match(runtimeDiagnosticsPanels, /Refresh doctor/);
  assert.match(runtimeDiagnosticsPanels, /Rotate next credential/);
  assert.match(runtimeDiagnosticsPanels, /Refresh workers/);
  assert.match(runtimeDiagnosticsPanels, /Resume job/);
  assert.match(runtimeDiagnosticsPanels, /Cancel job/);
  assert.match(artifactViewerPanel, /id="artifact-viewer"/);
  assert.match(artifactViewerPanel, /Euphony-inspired/);
  assert.match(artifactViewerPanel, /fetchRuntimeArtifactIndex/);
  assert.match(artifactViewerPanel, /fetchRuntimeArtifactDocument/);
  assert.match(artifactViewerPanel, /Open raw/);
  assert.match(artifactViewerPanel, /Copy JSON/);
  assert.match(artifactViewerPanel, /Quick views/);
  assert.match(artifactViewerPanel, /Structured snapshot/);
  assert.match(artifactViewerPanel, /initialArtifactPath\?: string \| null;/);
  assert.match(artifactViewerPanel, /initialArtifactIssue\?: string \| null;/);
  assert.match(artifactViewerPanel, /initialArtifactSection\?: string \| null;/);
  assert.match(artifactViewerPanel, /initialCaseRef\?: string \| null;/);
  assert.match(artifactViewerPanel, /initialFocusedOnly\?: boolean;/);
  assert.match(artifactViewerPanel, /onFocusedOnlyChange\?: \(enabled: boolean\) => void;/);
  assert.match(artifactViewerPanel, /buildRuntimeArtifactSectionAnchorId/);
  assert.match(artifactViewerPanel, /getRuntimeArtifactIssueConfig/);
  assert.match(artifactViewerPanel, /getRuntimeArtifactIssueFocusRowLabels/);
  assert.match(artifactViewerPanel, /getRuntimeArtifactIssueFocusSectionTitle/);
  assert.match(artifactViewerPanel, /buildRuntimeArtifactIssueSummary/);
  assert.match(artifactViewerPanel, /entry\.relativePath === initialArtifactPath/);
  assert.match(artifactViewerPanel, /const focusedSectionTitle = initialArtifactSection\?\.trim\(\) \|\| issueFocusSectionTitle;/);
  assert.match(artifactViewerPanel, /const focusedRowLabels = useMemo/);
  assert.match(artifactViewerPanel, /Focused by/);
  assert.match(artifactViewerPanel, /Focus summary/);
  assert.match(artifactViewerPanel, /Case ref/);
  assert.match(artifactViewerPanel, /Focus section/);
  assert.match(artifactViewerPanel, /Subsection anchor/);
  assert.match(artifactViewerPanel, /issueSummary\.headline/);
  assert.match(artifactViewerPanel, /issueSummary\.rows\.map/);
  assert.match(artifactViewerPanel, /const orderedStructuredSections = useMemo\(\(\) => \{/);
  assert.match(artifactViewerPanel, /const \[showFocusedOnly, setShowFocusedOnly\] = useState\(false\);/);
  assert.match(artifactViewerPanel, /const canShowFocusedOnly =/);
  assert.match(artifactViewerPanel, /const visibleStructuredSections = useMemo\(\(\) => \{/);
  assert.match(artifactViewerPanel, /const focusSection = structuredView\.sections\.find/);
  assert.match(artifactViewerPanel, /document\.getElementById\(sectionAnchorId\)\?\.scrollIntoView/);
  assert.match(artifactViewerPanel, /setShowFocusedOnly\(initialFocusedOnly && canShowFocusedOnly\);/);
  assert.match(artifactViewerPanel, /const toggleFocusedOnly = \(\) => \{/);
  assert.match(artifactViewerPanel, /onFocusedOnlyChange\?\.\(nextValue\);/);
  assert.match(artifactViewerPanel, /Show focused only/);
  assert.match(artifactViewerPanel, /Show all sections/);
  assert.match(artifactViewerPanel, /Showing only the focused structured lane for/);
  assert.match(artifactViewerPanel, /visibleStructuredSections\.map/);
  assert.match(artifactViewerPanel, /section\.title === focusedSectionTitle/);
  assert.match(artifactViewerPanel, /focusedRowLabels\.has\(row\.label\.trim\(\)\.toLowerCase\(\)\)/);
  assert.match(artifactViewerPanel, /focus/);
  assert.match(artifactViewerPanel, /Focus fields/);
  assert.match(artifactViewerPanel, /focus field/);
  assert.match(artifactViewerPanel, /issueSummary\.rows\.slice\(0, 4\)\.map/);
  assert.match(artifactViewerPanel, /focus-field-/);
  assert.match(artifactViewerPanel, /id=\{buildRuntimeArtifactSectionAnchorId\(section\.title\)\}/);
  assert.match(artifactViewerPanel, /Raw JSON focus:/);
  assert.match(artifactViewerPanel, /stays first in the structured snapshot above/);
  assert.match(artifactViewerLib, /export async function fetchRuntimeArtifactIndex/);
  assert.match(artifactViewerLib, /export async function fetchRuntimeArtifactDocument/);
  assert.match(artifactViewerLib, /export function summarizeRuntimeArtifact/);
  assert.match(artifactViewerLib, /export const PINNED_RUNTIME_ARTIFACT_PATHS = \[/);
  assert.match(artifactViewerLib, /export const RUNTIME_ARTIFACT_VIEW_PRESETS = \{/);
  assert.match(artifactViewerLib, /export type RuntimeArtifactIssue =/);
  assert.match(artifactViewerLib, /const RUNTIME_ARTIFACT_ISSUE_CONFIG:/);
  assert.match(artifactViewerLib, /export function isPinnedRuntimeArtifactPath/);
  assert.match(artifactViewerLib, /export function buildRuntimeArtifactViewerPath/);
  assert.match(artifactViewerLib, /section\?: string \| null \| undefined;/);
  assert.match(artifactViewerLib, /focusedOnly\?: boolean \| null \| undefined;/);
  assert.match(artifactViewerLib, /params\.set\("issue", options\.issue\);/);
  assert.match(artifactViewerLib, /params\.set\("section", options\.section\.trim\(\)\);/);
  assert.match(artifactViewerLib, /params\.set\("focusedOnly", "1"\);/);
  assert.match(artifactViewerLib, /export function buildRuntimeArtifactSectionAnchorId/);
  assert.match(artifactViewerLib, /export function getRuntimeArtifactIssueConfig/);
  assert.match(artifactViewerLib, /focusSectionTitle:/);
  assert.match(artifactViewerLib, /focusRowLabels: \[/);
  assert.match(artifactViewerLib, /export function getRuntimeArtifactIssueFocusSectionTitle/);
  assert.match(artifactViewerLib, /export function getRuntimeArtifactIssueFocusRowLabels/);
  assert.match(artifactViewerLib, /export type RuntimeArtifactIssueSummary =/);
  assert.match(artifactViewerLib, /Compliance blocker focus/);
  assert.match(artifactViewerLib, /Export posture focus/);
  assert.match(artifactViewerLib, /Raw artifact blocker focus/);
  assert.match(artifactViewerLib, /Case Wiki proof focus/);
  assert.match(artifactViewerLib, /export function buildRuntimeArtifactIssueSummary/);
  assert.match(artifactViewerLib, /export function buildRuntimeArtifactIssueViewerPath/);
  assert.match(artifactViewerLib, /section: config\?\.focusSectionTitle \?\? null,/);
  assert.match(artifactViewerLib, /export function buildRuntimeArtifactStructuredView/);
  assert.match(artifactViewerLib, /Unified release evidence report/);
  assert.match(artifactViewerLib, /Runtime proof lanes/);
  assert.match(artifactViewerLib, /release-evidence\/action-desk-kpi-report\.json/);
  assert.match(artifactViewerLib, /release-evidence\/consultation-booking-proof\.json/);
  assert.match(artifactViewerLib, /RUNTIME_ARTIFACT_VIEW_PRESETS = \{[\s\S]*actionDeskKpi:/);
  assert.match(artifactViewerLib, /RUNTIME_ARTIFACT_VIEW_PRESETS = \{[\s\S]*consultationBookingProof:/);
  assert.match(artifactViewerLib, /function buildActionDeskKpiStructuredView/);
  assert.match(artifactViewerLib, /Action Desk workflow KPI proof/);
  assert.match(artifactViewerLib, /function buildConsultationBookingProofStructuredView/);
  assert.match(artifactViewerLib, /Consultation booking proof posture/);
  assert.match(artifactViewerLib, /Calendar connector/);
  assert.match(bundlePage, /Inspect report/);
  assert.match(bundlePage, /RUNTIME_ARTIFACT_VIEW_PRESETS\.report/);
  assert.match(bundlePage, /buildRuntimeArtifactViewerPath/);
  assert.match(evidenceDetailPage, /Inspect badge details/);
  assert.match(evidenceDetailPage, /RUNTIME_ARTIFACT_VIEW_PRESETS\.badgeDetails/);
  assert.match(evidenceDetailPage, /buildRuntimeArtifactViewerPath/);

  assert.match(consolePage, /const \{ defaultConsoleCaseRef \} = useWorkspaceRuntime\(\);/);
  assert.match(consolePage, /const \{ hash \} = useLocation\(\);/);
  assert.match(consolePage, /const navigate = useNavigate\(\);/);
  assert.match(consolePage, /navigate\(`\/app\/console\/runtime\$\{search \? `\?\$\{search\}` : ""\}\$\{hash\}`/);
  assert.match(consolePage, /document\s*\.getElementById\(targetId\)\s*\?\.scrollIntoView/);
  assert.match(consolePage, /const caseRef = params\.get\("ref"\) \|\| defaultConsoleCaseRef \|\| "VS-2841";/);
});

test("shared app shell chrome reads runtime-backed counts, nodes, and diagnostics", () => {
  const topbar = readAppShellSource("components/workspace/Topbar.tsx");
  const sidebar = readAppShellSource("components/workspace/AppSidebar.tsx");
  const palette = readAppShellSource("components/workspace/CommandPalette.tsx");
  const rail = readAppShellSource("components/workspace/RuntimeRail.tsx");
  const nodeDetailRail = readAppShellSource("components/nodes/NodeDetailRail.tsx");

  assert.match(topbar, /const \{ runtimeActive, slaBurningCases, degradedInfraCases \} = useWorkspaceRuntime\(\);/);
  assert.match(topbar, /runtime · \{runtimeActive \? "live" : "mock"\}/);

  assert.match(sidebar, /const \{\s+cases,\s+deviceNodes,\s+pendingApprovals,\s+activeCaseCount,\s+pendingApprovalCount,/);
  assert.match(sidebar, /const runtimeSections: Section\[\] = \[/);
  assert.match(sidebar, /const runtimeOperatorSurfaces = operatorSurfaces\.map/);
  assert.match(sidebar, /Live activity", icon: Activity, url: "\/app"/);
  assert.match(sidebar, /Connections", icon: Plug, url: "\/app\/nodes"/);
  assert.match(sidebar, /Action queue"[\s\S]*url: "\/app\/console"/);
  assert.match(sidebar, /Safety rules", icon: ShieldCheck, url: "\/app\/simulation"/);
  assert.match(sidebar, /Health check", icon: HeartPulse, url: "\/app\/nodes"/);
  assert.match(sidebar, /const \{ pathname, hash \} = useLocation\(\);/);
  assert.match(sidebar, /pathname\.startsWith\("\/app\/console"\)/);
  assert.match(sidebar, /\/app\/console\?ref=\$\{encodeURIComponent\(firstPendingRef\)\}/);

  assert.match(palette, /const \{ cases, pendingApprovals \} = useWorkspaceRuntime\(\);/);
  assert.match(palette, /Start 7-minute demo/);
  assert.match(palette, /Local services dispatcher demo/);
  assert.match(palette, /Visa lead qualification/);
  assert.match(palette, /Missing-document follow-up/);
  assert.match(palette, /Consultation booking prep/);
  assert.match(palette, /CRM handoff summary/);
  assert.match(palette, /run\(\(\) => navigate\("\/app\?demo=visa-intake"\)\)/);
  assert.match(palette, /run\(\(\) => navigate\("\/app\?demo=local-services-dispatch&service=ac-repair-dispatch"\)\)/);
  assert.match(palette, /navigate\(`\/app\/console\?ref=\$\{leadQualificationCase\.ref\}`\)/);
  assert.match(palette, /navigate\(`\/app\/console\?ref=\$\{missingDocumentsCase\.ref\}&focus=documents`\)/);
  assert.match(palette, /navigate\(buildCaseBundlePath\(consultationCase\)\)/);
  assert.match(palette, /navigate\(buildCaseVaultPath\(crmHandoffCase\)\)/);
  assert.match(palette, /run\(\(\) => navigate\("\/app\/nodes"\)\)/);
  assert.match(palette, /run\(\(\) => navigate\("\/app\/console\/runtime"\)\)/);
  assert.match(palette, /run\(\(\) => navigate\("\/app\/console\/runtime#artifact-viewer"\)\)/);

  assert.match(rail, /const \{\s+runtimeActive,\s+pendingApprovalCount,\s+runtimeDiagnostics,\s+bootstrapDoctor,\s+browserWorkers,\s+\} = useWorkspaceRuntime\(\);/);
  assert.match(rail, /const items: RailItem\[\] = runtimeActive/);

  assert.match(nodeDetailRail, /const \{ cases \} = useWorkspaceRuntime\(\);/);
  assert.match(nodeDetailRail, /cases\.filter\(\(c\) => c\.sourceNodeId === node\.id\)/);
});

test("runtime app shell resolves backend endpoints through runtime config and shared node helpers", () => {
  const workspaceRuntime = readAppShellSource("hooks/useWorkspaceRuntime.tsx");
  const runtimeNodes = readAppShellSource("lib/runtime-device-nodes.ts");
  const nodesPage = readAppShellSource("pages/Nodes.tsx");

  assert.match(workspaceRuntime, /import \{ fetchRuntimeApi \} from "@\/lib\/runtime-api";/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*"\/v1\/operator\/summary"/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*"\/v1\/governance\/policy"/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*"\/v1\/sessions\?limit=8"/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*`\/v1\/runtime\/case-wiki\?sessionId=\$\{encodeURIComponent\(sessionId\)\}`/);

  assert.match(runtimeNodes, /import \{ fetchRuntimeApi \} from "@\/lib\/runtime-api";/);
  assert.match(runtimeNodes, /fetchRuntimeApi\(\s*"\/v1\/device-nodes\?includeOffline=true&limit=200"/);

  assert.match(nodesPage, /import \{\s*fetchRuntimeDeviceNodes,\s*mapRuntimeDeviceNode,\s*\} from "@\/lib\/runtime-device-nodes";/);
  assert.doesNotMatch(nodesPage, /async function fetchRuntimeDeviceNodes\(\)/);
});

test("live desk, operator console, and simulation drilldowns share case-driven judge artifact links", () => {
  const helper = readAppShellSource("lib/case-artifact-links.ts");
  const liveDesk = readAppShellSource("components/workspace/LiveDesk.tsx");
  const consoleStage = readAppShellSource("components/workspace/ConsoleStage.tsx");
  const runDetailDrawer = readAppShellSource("components/workspace/RunDetailDrawer.tsx");
  const readme = readRepoSource("README.md");
  const operatorGuide = readRepoSource("docs/operator-guide.md");

  assert.match(helper, /export function resolveCaseArtifactRef/);
  assert.match(helper, /return firstArtifactToken\(target\.caseId, target\.sessionId, target\.ref\);/);
  assert.match(helper, /export function buildCaseBundlePath/);
  assert.match(helper, /export function buildCaseEvidencePath/);
  assert.match(helper, /export function buildCaseRuntimeSupportPath/);
  assert.match(helper, /return `\/app\/console\/runtime\$\{search\}\$\{anchor\}`;/);

  assert.match(liveDesk, /import \{\s*buildCaseBundlePath,\s*buildCaseEvidencePath,\s*buildCaseVaultPath,\s*\} from "@\/lib\/case-artifact-links";/);
  assert.match(liveDesk, /navigate\(buildCaseBundlePath\(value\)\);/);
  assert.match(liveDesk, /navigate\(buildCaseEvidencePath\(value\)\);/);
  assert.match(liveDesk, /navigate\(buildCaseVaultPath\(value\)\);/);

  assert.match(consoleStage, /import \{\s*buildCaseBundlePath,\s*buildCaseEvidencePath,\s*buildCaseRuntimeSupportPath,\s*buildCaseVaultPath,\s*\} from "@\/lib\/case-artifact-links";/);
  assert.match(consoleStage, /import \{ useQuery \} from "@tanstack\/react-query";/);
  assert.match(consoleStage, /import \{\s*buildRuntimeSessionReplaySummary,\s*fetchRuntimeSessionReplay,\s*type RuntimeSessionReplaySummary,\s*\} from "@\/lib\/runtime-session-replay";/);
  assert.match(consoleStage, /const \{ deviceNodes, getCaseByRef, getCaseWikiByRef \} = useWorkspaceRuntime\(\);/);
  assert.match(consoleStage, /navigate\(buildCaseBundlePath\(c\)\);/);
  assert.match(consoleStage, /navigate\(buildCaseEvidencePath\(c\)\);/);
  assert.match(consoleStage, /navigate\(buildCaseVaultPath\(c\)\);/);
  assert.match(consoleStage, /const runtimeSupportPath = buildCaseRuntimeSupportPath\(c\);/);
  assert.match(consoleStage, /Runtime support/);
  assert.match(consoleStage, /Export blocked|Export ready|Export waiting/);
  assert.match(consoleStage, /Proof signed|Proof published|Proof pending/);
  assert.match(consoleStage, /Replay loading|Replay waiting/);
  assert.match(consoleStage, /Gate pending/);
  assert.match(consoleStage, /showRuntimeSupportStrip \? \(/);
  assert.match(consoleStage, /Inspect compliance blocker|Inspect export block|Inspect export posture|Inspect unsigned proof|Inspect missing proof|Inspect replay gate|Inspect replay/);

  assert.match(runDetailDrawer, /import \{\s*buildCaseBundlePath,\s*buildCaseEvidencePath,\s*\} from "@\/lib\/case-artifact-links";/);
  assert.match(runDetailDrawer, /navigate\(buildCaseBundlePath\(c \?\? run\.caseRef\)\);/);
  assert.match(runDetailDrawer, /navigate\(buildCaseEvidencePath\(c \?\? run\.caseRef\)\);/);

  assert.match(readme, /Live Desk` row actions\/context menus and `Operator Console` hero quick actions/);
  assert.match(readme, /\/bundle\/:id` and `\/evidence\/:id` prefer runtime `caseId\/sessionId` targets/);
  assert.match(operatorGuide, /Operator proof-link note:/);
  assert.match(operatorGuide, /Presentation Bundle` \/ `Visual Evidence` targets through the same repo-owned `caseId\/sessionId\/ref` resolver/);
});

test("live desk exposes the seven-minute visa intake product path", () => {
  const liveDesk = readAppShellSource("components/workspace/LiveDesk.tsx");
  const readme = readRepoSource("README.md");
  const localDevelopment = readRepoSource("docs/local-development.md");
  const operatorGuide = readRepoSource("docs/operator-guide.md");
  const simplificationPlan = readRepoSource("docs/quality-simplification-plan.md");
  const gettingStarted = readRepoSource("docs/getting-started-7-min.md");
  const evidenceTrust = readRepoSource("docs/evidence-and-trust.md");
  const localServicesSpec = readRepoSource("docs/local-services-action-desk-spec.md");
  const pilotOffer = readRepoSource("docs/local-services-pilot-offer.md");
  const demoScript = readRepoSource("docs/local-services-demo-script.md");
  const demoRecordingChecklist = readRepoSource("docs/local-services-demo-recording-checklist.md");
  const outreachList = readRepoSource("docs/local-services-outreach-list.md");
  const pilotScorecard = readRepoSource("docs/local-services-pilot-scorecard.md");
  const pilotRunbook = readRepoSource("docs/local-services-pilot-runbook.md");
  const outreachExecutionPack = readRepoSource("docs/local-services-outreach-execution-pack.md");
  const founderExecutionLog = readRepoSource("docs/local-services-founder-execution-log.md");
  const demoFrontendServer = readRepoSource("apps/demo-frontend/src/server.ts");

  assert.match(liveDesk, /const visaIntakeDemo = searchParams\.get\("demo"\) === "visa-intake";/);
  assert.match(liveDesk, /const localServicesDispatchDemo = searchParams\.get\("demo"\) === "local-services-dispatch";/);
  assert.match(liveDesk, /const localServicesRecordingMode = localServicesDispatchDemo && searchParams\.get\("recording"\) === "90s";/);
  assert.match(liveDesk, /const localServicesSetupWizardMode = localServicesDispatchDemo && searchParams\.get\("setup"\) === "7min";/);
  assert.match(liveDesk, /const activeLocalServiceId = searchParams\.get\("service"\);/);
  assert.match(liveDesk, /const openVisaIntakeDemo = \(\) => \{/);
  assert.match(liveDesk, /const openLocalServicesDispatchDemo = \(\) => \{/);
  assert.match(liveDesk, /const openLocalServicesRecordingMode = \(\) => \{/);
  assert.match(liveDesk, /const closeLocalServicesRecordingMode = \(\) => \{/);
  assert.match(liveDesk, /const openLocalServicesSetupWizard = \(serviceId\?: string\) => \{/);
  assert.match(liveDesk, /const closeLocalServicesSetupWizard = \(\) => \{/);
  assert.match(liveDesk, /next\.set\("demo", "local-services-dispatch"\);/);
  assert.match(liveDesk, /next\.set\("service", "ac-repair-dispatch"\);/);
  assert.match(liveDesk, /next\.set\("recording", "90s"\);/);
  assert.match(liveDesk, /next\.set\("setup", "7min"\);/);
  assert.match(liveDesk, /const SevenMinuteVisaIntakePanel = \(\{/);
  assert.match(liveDesk, /const LocalServicesDispatchDemoPanel = \(\{/);
  assert.match(liveDesk, /recordingMode: boolean;/);
  assert.match(liveDesk, /recordingMode=\{localServicesRecordingMode\}/);
  assert.match(liveDesk, /90s recording/);
  assert.match(liveDesk, /Exit recording/);
  assert.match(liveDesk, /90-second recording mode/);
  assert.match(liveDesk, /Recording path/);
  assert.match(liveDesk, /Outreach tables and scorecard controls are hidden during recording/);
  assert.match(liveDesk, /aria-hidden=\{hidePilotPlanning\}/);
  assert.match(liveDesk, /No autonomous send/);
  assert.match(liveDesk, /Case Outcome Summary/);
  assert.match(liveDesk, /AI Dispatcher for Local Services/);
  assert.match(liveDesk, /Phone AI intake/);
  assert.match(liveDesk, /Operator-approved booking/);
  assert.match(liveDesk, /AC repair dispatch/);
  assert.match(liveDesk, /Plumbing emergency/);
  assert.match(liveDesk, /Cleaning quote and booking/);
  assert.match(liveDesk, /Measurement visit booking/);
  assert.match(liveDesk, /construction-adjacent P0/i);
  assert.match(liveDesk, /No material availability, installation date, or final price is promised/);
  assert.match(liveDesk, /Pricing and slot inputs/);
  assert.match(liveDesk, /Customer confirmation draft/);
  assert.match(liveDesk, /Master\/operator handoff/);
  assert.match(liveDesk, /Telegram intake prototype/);
  assert.match(liveDesk, /Normalized fields/);
  assert.match(liveDesk, /Reply draft/);
  assert.match(liveDesk, /Open intake evidence/);
  assert.match(liveDesk, /Transcript \+ evidence/);
  assert.match(liveDesk, /Saved intake evidence/);
  assert.match(liveDesk, /Intake transcript \+ evidence link/);
  assert.match(liveDesk, /Transcript preview/);
  assert.match(liveDesk, /Evidence export mode/);
  assert.match(liveDesk, /Copy intake evidence/);
  assert.match(liveDesk, /local_services_intake_evidence/);
  assert.match(liveDesk, /transcript_evidence_link/);
  assert.match(liveDesk, /type LocalServiceIntakeEvidence = \{/);
  assert.match(liveDesk, /function buildLocalServiceIntakeEvidence\(/);
  assert.match(liveDesk, /LocalServiceIntakeEvidenceSheet/);
  assert.match(liveDesk, /no_live_channel_storage/);
  assert.match(liveDesk, /Pilot readiness/);
  assert.match(liveDesk, /One-page offer/);
  assert.match(liveDesk, /90-second demo script/);
  assert.match(liveDesk, /Outreach focus/);
  assert.match(liveDesk, /Launch checklist/);
  assert.match(liveDesk, /Agent setup \/ training state/);
  assert.match(liveDesk, /localServicesSetupWizardMode/);
  assert.match(liveDesk, /openLocalServicesSetupWizard/);
  assert.match(liveDesk, /closeLocalServicesSetupWizard/);
  assert.match(liveDesk, /next\.set\("setup", "7min"\)/);
  assert.match(liveDesk, /setupWizardMode: boolean/);
  assert.match(liveDesk, /setupWizardMode=\{localServicesSetupWizardMode\}/);
  assert.match(liveDesk, /type LocalServiceSetupStepId =/);
  assert.match(liveDesk, /type LocalServiceTestCallCheckId =/);
  assert.match(liveDesk, /setupStepCompletionByService/);
  assert.match(liveDesk, /setupReadyByService/);
  assert.match(liveDesk, /testCallChecklistByService/);
  assert.match(liveDesk, /testCallPassedByService/);
  assert.match(liveDesk, /readSetupStepCompletionByService/);
  assert.match(liveDesk, /readTestCallChecklistByService/);
  assert.match(liveDesk, /readBooleanRecord/);
  assert.match(liveDesk, /updateSetupStepCompletion/);
  assert.match(liveDesk, /markReadyForPilotTest/);
  assert.match(liveDesk, /resetSetupProgress/);
  assert.match(liveDesk, /updateTestCallCheck/);
  assert.match(liveDesk, /recordTestCallPassed/);
  assert.match(liveDesk, /resetTestCall/);
  assert.match(liveDesk, /Open setup checklist/);
  assert.match(liveDesk, /7-minute setup wizard/);
  assert.match(liveDesk, /Setup path/);
  assert.match(liveDesk, /Setup progress/);
  assert.match(liveDesk, /Saved setup state/);
  assert.match(liveDesk, /Mark complete/);
  assert.match(liveDesk, /Mark ready for pilot test/);
  assert.match(liveDesk, /Ready for pilot test/);
  assert.match(liveDesk, /Reset setup progress/);
  assert.match(liveDesk, /Test call\/message panel/);
  assert.match(liveDesk, /Sample inbound/);
  assert.match(liveDesk, /Expected extracted fields/);
  assert.match(liveDesk, /Pass\/fail checklist/);
  assert.match(liveDesk, /Mark check passed/);
  assert.match(liveDesk, /Record test passed/);
  assert.match(liveDesk, /Test call passed/);
  assert.match(liveDesk, /Reset test call/);
  assert.match(liveDesk, /Copy test call brief/);
  assert.match(liveDesk, /7-min setup/);
  assert.match(liveDesk, /Exit setup/);
  assert.match(liveDesk, /Open day-one setup/);
  assert.match(liveDesk, /Business profile/);
  assert.match(liveDesk, /Knowledge sources/);
  assert.match(liveDesk, /Agent behavior/);
  assert.match(liveDesk, /Test call\/message/);
  assert.match(liveDesk, /Ready for test call\/message/);
  assert.match(liveDesk, /Training cards/);
  assert.match(liveDesk, /Copy setup brief/);
  assert.match(liveDesk, /local_services_agent_setup_training/);
  assert.match(liveDesk, /deterministic_setup_checklist/);
  assert.match(liveDesk, /No channel activation/);
  assert.match(liveDesk, /hidePilotPlanning/);
  assert.match(liveDesk, /aria-hidden=\{hidePilotPlanning\}/);
  assert.match(liveDesk, /Outreach list filters/);
  assert.match(liveDesk, /Column settings/);
  assert.match(liveDesk, /Service filter/);
  assert.match(liveDesk, /Status filter/);
  assert.match(liveDesk, /Filtered candidates/);
  assert.match(liveDesk, /Filtered outreach list/);
  assert.match(liveDesk, /All services/);
  assert.match(liveDesk, /All statuses/);
  assert.match(liveDesk, /Clear filters/);
  assert.match(liveDesk, /View only, no send/);
  assert.match(liveDesk, /type LocalServicePilotExecutionStep = \{/);
  assert.match(liveDesk, /LOCAL_SERVICES_PILOT_RUNBOOK_PATH = "\/workspace-docs\/local-services-pilot-runbook\.md"/);
  assert.match(liveDesk, /LOCAL_SERVICES_FOUNDER_EXECUTION_LOG_PATH = "\/workspace-docs\/local-services-founder-execution-log\.md"/);
  assert.match(liveDesk, /Pilot execution checklist/);
  assert.match(liveDesk, /14-day pilot operating loop/);
  assert.match(liveDesk, /Pass test call\/message/);
  assert.match(liveDesk, /Needs test call passed/);
  assert.match(liveDesk, /Prepare first manual batch/);
  assert.match(liveDesk, /Ready for first manual batch/);
  assert.match(liveDesk, /Record ready drafts/);
  assert.match(liveDesk, /Log manual contact/);
  assert.match(liveDesk, /Book discovery call/);
  assert.match(liveDesk, /Start metric capture/);
  assert.match(liveDesk, /completedPilotExecutionStepCount/);
  assert.match(liveDesk, /Pilot checklist progress/);
  assert.match(liveDesk, /Dry run required/);
  assert.match(liveDesk, /Dry run passed/);
  assert.match(liveDesk, /selectedDraftReady/);
  assert.match(liveDesk, /Manual launch blocked/);
  assert.match(liveDesk, /Manual launch ready/);
  assert.match(liveDesk, /type LocalServicePilotActivityEvent = \{/);
  assert.match(liveDesk, /function appendLocalServicePilotActivity\(/);
  assert.match(liveDesk, /activityLog: LocalServicePilotActivityEvent\[\]/);
  assert.match(liveDesk, /Manual activity log/);
  assert.match(liveDesk, /Last manual action/);
  assert.match(liveDesk, /Copy activity log/);
  assert.match(liveDesk, /local_services_manual_activity_log/);
  assert.match(liveDesk, /No external side\s+effects/);
  assert.match(liveDesk, /Pilot status recorded/);
  assert.match(liveDesk, /Metric capture recorded/);
  assert.match(liveDesk, /function buildLocalServicePilotLaunchPacket\(/);
  assert.match(liveDesk, /Open launch packet/);
  assert.match(liveDesk, /Pilot launch packet/);
  assert.match(liveDesk, /Launch packet preview/);
  assert.match(liveDesk, /First manual contact checklist/);
  assert.match(liveDesk, /Copy launch packet/);
  assert.match(liveDesk, /local_services_pilot_launch_packet/);
  assert.match(liveDesk, /dry_run_required_before_first_contact/);
  assert.match(liveDesk, /Founder\/operator validation/);
  assert.match(liveDesk, /No autonomous send/);
  assert.match(liveDesk, /type LocalServiceDiscoveryCallPrep = \{/);
  assert.match(liveDesk, /function buildLocalServiceDiscoveryCallPrep\(/);
  assert.match(liveDesk, /LocalServiceDiscoveryCallPrepSheet/);
  assert.match(liveDesk, /Open discovery prep/);
  assert.match(liveDesk, /Discovery call prep/);
  assert.match(liveDesk, /Questions to ask/);
  assert.match(liveDesk, /Pilot success criteria/);
  assert.match(liveDesk, /Copy discovery call prep/);
  assert.match(liveDesk, /local_services_discovery_call_prep/);
  assert.match(liveDesk, /no_calendar_booking_created/);
  assert.match(liveDesk, /type LocalServiceDayOneSetupBrief = \{/);
  assert.match(liveDesk, /function buildLocalServiceDayOneSetupBrief\(/);
  assert.match(liveDesk, /LocalServiceDayOneSetupSheet/);
  assert.match(liveDesk, /Open day-one setup/);
  assert.match(liveDesk, /Day-one setup brief/);
  assert.match(liveDesk, /Business profile lock/);
  assert.match(liveDesk, /Setup tasks/);
  assert.match(liveDesk, /Test call plan/);
  assert.match(liveDesk, /Copy day-one setup brief/);
  assert.match(liveDesk, /local_services_day_one_setup_brief/);
  assert.match(liveDesk, /no_phone_channel_activation/);
  assert.match(liveDesk, /Open pilot runbook/);
  assert.match(liveDesk, /Open offer doc/);
  assert.match(liveDesk, /Open demo script/);
  assert.match(liveDesk, /Open recording checklist/);
  assert.match(liveDesk, /Open outreach list/);
  assert.match(liveDesk, /Open outreach execution pack/);
  assert.match(liveDesk, /Open pilot scorecard/);
  assert.match(liveDesk, /4-step outreach wizard/);
  assert.match(liveDesk, /Pilot outreach wizard/);
  assert.match(liveDesk, /Offer preview/);
  assert.match(liveDesk, /Audience from outreach list/);
  assert.match(liveDesk, /Message\/test preview/);
  assert.match(liveDesk, /Test message preview/);
  assert.match(liveDesk, /Open preview modal/);
  assert.match(liveDesk, /Preview \/ Test message modal/);
  assert.match(liveDesk, /Copy test message/);
  assert.match(liveDesk, /Copy test message preview/);
  assert.match(liveDesk, /local_services_test_message_preview/);
  assert.match(liveDesk, /manual_confirmation_required_before_outreach/);
  assert.match(liveDesk, /Operator confirmation/);
  assert.match(liveDesk, /Operator confirmation summary/);
  assert.match(liveDesk, /Open confirmation summary/);
  assert.match(liveDesk, /Ready for manual outreach/);
  assert.match(liveDesk, /Copy confirmation summary/);
  assert.match(liveDesk, /local_services_operator_confirmation/);
  assert.match(liveDesk, /ready_for_manual_outreach/);
  assert.match(liveDesk, /Wizard progress/);
  assert.match(liveDesk, /Record ready for manual outreach/);
  assert.match(liveDesk, /Ready for manual outreach recorded/);
  assert.match(liveDesk, /Awaiting operator confirmation/);
  assert.match(liveDesk, /Local status only, no send/);
  assert.match(liveDesk, /Preview complete/);
  assert.match(liveDesk, /AI analyst/);
  assert.match(liveDesk, /Ask AI about pilot/);
  assert.match(liveDesk, /Suggested questions/);
  assert.match(liveDesk, /Best candidate/);
  assert.match(liveDesk, /Bottleneck/);
  assert.match(liveDesk, /Next message/);
  assert.match(liveDesk, /Copy analyst brief/);
  assert.match(liveDesk, /local_services_pilot_ai_analyst/);
  assert.match(liveDesk, /deterministic_operator_assist/);
  assert.match(liveDesk, /No external LLM call/);
  assert.match(liveDesk, /no outbound send/);
  assert.match(liveDesk, /operator-approved/);
  assert.match(liveDesk, /readyRecorded=\{currentPilotStatus === "draft_ready"\}/);
  assert.match(liveDesk, /onRecordReady=\{recordReadyForManualOutreach\}/);
  assert.match(liveDesk, /type LocalServiceOutreachProspect = \{/);
  assert.match(liveDesk, /type LocalServicePilotWorkspaceState = \{/);
  assert.match(liveDesk, /type LocalServicePilotMetricStatus =/);
  assert.match(liveDesk, /metricStatusByService/);
  assert.match(liveDesk, /LOCAL_SERVICE_PILOT_WORKSPACE_STORAGE_KEY = "liveDesk:localServicesPilotWorkspace:v1"/);
  assert.match(liveDesk, /LOCAL_SERVICE_PILOT_STATUS_ORDER/);
  assert.match(liveDesk, /LOCAL_SERVICE_PILOT_METRIC_STATUS_ACTIONS/);
  assert.match(liveDesk, /readLocalServicePilotWorkspaceState/);
  assert.match(liveDesk, /window\.localStorage\.setItem\(LOCAL_SERVICE_PILOT_WORKSPACE_STORAGE_KEY/);
  assert.match(liveDesk, /allPilotProspects/);
  assert.match(liveDesk, /pilotFunnelCounts/);
  assert.match(liveDesk, /nextManualBatch/);
  assert.match(liveDesk, /Pilot funnel summary/);
  assert.match(liveDesk, /All candidates/);
  assert.match(liveDesk, /Next manual\s+batch/);
  assert.match(liveDesk, /Manual execution rule/);
  assert.match(liveDesk, /Open pilot export/);
  assert.match(liveDesk, /Pilot workspace export drawer/);
  assert.match(liveDesk, /Copy pilot workspace export/);
  assert.match(liveDesk, /buildLocalServicePilotWorkspaceExport\(pilotFunnelRows, pilotFunnelCounts, pilotWorkspaceState\.activityLog\)/);
  assert.match(liveDesk, /activity_log/);
  assert.match(liveDesk, /Review the manual activity log before copying the export/);
  assert.match(liveDesk, /no_crm_write/);
  assert.match(liveDesk, /Pilot metrics tracker/);
  assert.match(liveDesk, /Open metrics tracker/);
  assert.match(liveDesk, /Copy pilot metrics tracker/);
  assert.match(liveDesk, /local_services_pilot_metrics_tracker/);
  assert.match(liveDesk, /Pilot daily log/);
  assert.match(liveDesk, /Open daily log/);
  assert.match(liveDesk, /Daily capture fields/);
  assert.match(liveDesk, /Daily operating loop/);
  assert.match(liveDesk, /Copy pilot daily log/);
  assert.match(liveDesk, /local_services_pilot_daily_log/);
  assert.match(liveDesk, /Outcome state key/);
  assert.match(liveDesk, /first_request_outcome/);
  assert.match(liveDesk, /manual_first_request_outcome_note/);
  assert.match(liveDesk, /Record the first request outcome before weekly scorecard sync/);
  assert.match(liveDesk, /Weekly scorecard sync gate/);
  assert.match(liveDesk, /manual_weekly_scorecard_sync_gate/);
  assert.match(liveDesk, /Open weekly sync checklist/);
  assert.match(liveDesk, /Weekly scorecard sync checklist/);
  assert.match(liveDesk, /Copy weekly sync checklist/);
  assert.match(liveDesk, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(liveDesk, /manual_weekly_scorecard_sync_checklist/);
  assert.match(liveDesk, /Record weekly sync reviewed/);
  assert.match(liveDesk, /Reset weekly sync review/);
  assert.match(liveDesk, /weeklyScorecardSyncReviewedByService/);
  assert.match(liveDesk, /Weekly sync review state key/);
  assert.match(liveDesk, /Blocked until weekly scorecard sync is reviewed/);
  assert.match(liveDesk, /Evidence readiness/);
  assert.match(liveDesk, /manual_daily_operating_loop/);
  assert.match(liveDesk, /no_calendar_booking_created/);
  assert.match(liveDesk, /no_customer_message_sent/);
  assert.match(liveDesk, /Pilot week-one review/);
  assert.match(liveDesk, /Open week-one review/);
  assert.match(liveDesk, /Continue \/ stop decision/);
  assert.match(liveDesk, /Copy week-one review/);
  assert.match(liveDesk, /local_services_pilot_week_one_review/);
  assert.match(liveDesk, /manual_first_request_outcome_review/);
  assert.match(liveDesk, /Owner-ready summary/);
  assert.match(liveDesk, /Decision readiness/);
  assert.match(liveDesk, /Latest manual signal/);
  assert.match(liveDesk, /day_one_recap_to_week_one_review/);
  assert.match(liveDesk, /Week-one owner\s+decision state/);
  assert.match(liveDesk, /Record continue/);
  assert.match(liveDesk, /Record pause/);
  assert.match(liveDesk, /Record stop/);
  assert.match(liveDesk, /Reset owner decision/);
  assert.match(liveDesk, /weekOneOwnerDecisionByProspectKey/);
  assert.match(liveDesk, /week_one_owner_decision_to_evidence_pack/);
  assert.match(liveDesk, /owner_decision_manual_only/);
  assert.match(liveDesk, /Week-one owner decision recorded/);
  assert.match(liveDesk, /owner_review_required/);
  assert.match(liveDesk, /Confirm the first request outcome is recorded before choosing continue, pause, or stop/);
  assert.match(liveDesk, /manual_week_one_decision_pack/);
  assert.match(liveDesk, /no_autonomous_pilot_decision/);
  assert.match(liveDesk, /no_billing_change/);
  assert.match(liveDesk, /Pilot evidence pack/);
  assert.match(liveDesk, /Open evidence pack/);
  assert.match(liveDesk, /Week-two evidence pack/);
  assert.match(liveDesk, /Copy evidence pack/);
  assert.match(liveDesk, /local_services_pilot_evidence_pack/);
  assert.match(liveDesk, /manual_first_request_outcome_evidence/);
  assert.match(liveDesk, /Week-one owner decision/);
  assert.match(liveDesk, /Include the first request outcome before paid-pilot readiness is reviewed/);
  assert.match(liveDesk, /Include the week-one owner decision before paid-pilot readiness is reviewed/);
  assert.match(liveDesk, /manual_week_two_evidence_pack/);
  assert.match(liveDesk, /no_private_customer_data_in_public_docs/);
  assert.match(liveDesk, /redact_names_phone_addresses_media/);
  assert.match(liveDesk, /Metric capture state/);
  assert.match(liveDesk, /Mark baseline captured/);
  assert.match(liveDesk, /Mark tracking live/);
  assert.match(liveDesk, /Mark review ready/);
  assert.match(liveDesk, /Reset metrics/);
  assert.match(liveDesk, /browser-local/);
  assert.match(liveDesk, /Pilot scorecard action/);
  assert.match(liveDesk, /Selected company/);
  assert.match(liveDesk, /AC MASTER/);
  assert.match(liveDesk, /Santexniki\.uz/);
  assert.match(liveDesk, /Service-Pro/);
  assert.match(liveDesk, /Pilot workspace state/);
  assert.match(liveDesk, /Saved in this browser/);
  assert.match(liveDesk, /Draft ready/);
  assert.match(liveDesk, /Mark manually contacted/);
  assert.match(liveDesk, /Mark reply received/);
  assert.match(liveDesk, /Reject for now/);
  assert.match(liveDesk, /Reset to not contacted/);
  assert.match(liveDesk, /Record scorecard draft/);
  assert.match(liveDesk, /Pilot workspace state persisted locally/);
  assert.match(liveDesk, /No outbound message sent/);
  assert.match(liveDesk, /First request outcome/);
  assert.match(liveDesk, /Manual outcome state/);
  assert.match(liveDesk, /Qualified/);
  assert.match(liveDesk, /Needs follow-up/);
  assert.match(liveDesk, /Booked manually/);
  assert.match(liveDesk, /firstRequestOutcomeByProspectKey/);
  assert.match(liveDesk, /First request outcome recorded/);
  assert.match(liveDesk, /outcome_change/);
  assert.match(liveDesk, /Reset outcome/);
  assert.match(liveDesk, /Outcome chain summary/);
  assert.match(liveDesk, /Scorecard draft/);
  assert.match(liveDesk, /Daily log/);
  assert.match(liveDesk, /Week-one review/);
  assert.match(liveDesk, /Paid-pilot readiness/);
  assert.match(liveDesk, /Confirm manually/);
  assert.match(liveDesk, /Log outcome in scorecard/);
  assert.match(liveDesk, /path\.startsWith\("\/workspace-docs\/"\)/);
  assert.match(liveDesk, /window\.location\.assign\(path\)/);
  assert.match(liveDesk, /LOCAL_SERVICES_PILOT_OFFER_PATH = "\/workspace-docs\/local-services-pilot-offer\.md"/);
  assert.match(liveDesk, /LOCAL_SERVICES_DEMO_SCRIPT_PATH = "\/workspace-docs\/local-services-demo-script\.md"/);
  assert.match(liveDesk, /LOCAL_SERVICES_DEMO_RECORDING_CHECKLIST_PATH =\s*"\/workspace-docs\/local-services-demo-recording-checklist\.md"/);
  assert.match(liveDesk, /LOCAL_SERVICES_OUTREACH_LIST_PATH = "\/workspace-docs\/local-services-outreach-list\.md"/);
  assert.match(liveDesk, /LOCAL_SERVICES_PILOT_SCORECARD_PATH = "\/workspace-docs\/local-services-pilot-scorecard\.md"/);
  assert.match(liveDesk, /LOCAL_SERVICES_OUTREACH_EXECUTION_PACK_PATH = "\/workspace-docs\/local-services-outreach-execution-pack\.md"/);
  assert.match(liveDesk, /Open founder execution log/);
  assert.match(liveDesk, /type LocalServiceFounderContactProof/);
  assert.match(liveDesk, /contactProofByProspectKey/);
  assert.match(liveDesk, /First 10 contacts workspace/);
  assert.match(liveDesk, /Pilot proof checklist/);
  assert.match(liveDesk, /Pilot ops today/);
  assert.match(liveDesk, /Copy pilot ops handoff/);
  assert.match(liveDesk, /local_services_pilot_ops_today/);
  assert.match(liveDesk, /Open ops confirmation/);
  assert.match(liveDesk, /Pilot ops confirmation drawer/);
  assert.match(liveDesk, /Copy ops confirmation/);
  assert.match(liveDesk, /local_services_pilot_ops_confirmation/);
  assert.match(liveDesk, /Pilot proof update rail/);
  assert.match(liveDesk, /local_services_pilot_proof_update_rail/);
  assert.match(liveDesk, /Recommended: /);
  assert.match(liveDesk, /Open batch review/);
  assert.match(liveDesk, /First contact batch review drawer/);
  assert.match(liveDesk, /Copy batch review/);
  assert.match(liveDesk, /Stop \/ Continue decision gate/);
  assert.match(liveDesk, /Keep collecting proof/);
  assert.match(liveDesk, /Continue to pilot setup/);
  assert.match(liveDesk, /Stop expansion for now/);
  assert.match(liveDesk, /NEWO-style AI employee platform/);
  assert.match(liveDesk, /founder\/operator validation/);
  assert.match(liveDesk, /type LocalServiceCategoryPilotScore/);
  assert.match(liveDesk, /type LocalServiceLeadingCategoryActionLayer/);
  assert.match(liveDesk, /type LocalServiceLeadingCategoryPilotReadiness/);
  assert.match(liveDesk, /type LocalServicePilotReadinessActionPlan/);
  assert.match(liveDesk, /buildLocalServiceCategoryPilotScores/);
  assert.match(liveDesk, /buildLocalServiceLeadingCategoryActionLayer/);
  assert.match(liveDesk, /buildLocalServiceLeadingCategoryPilotReadiness/);
  assert.match(liveDesk, /buildLocalServicePilotReadinessActionPlan/);
  assert.match(liveDesk, /formatLocalServiceReadinessActionPlanText/);
  assert.match(liveDesk, /buildLocalServiceReadinessProofDrawer/);
  assert.match(liveDesk, /Category pilot score/);
  assert.match(liveDesk, /Leading category/);
  assert.match(liveDesk, /No category expansion without proof/);
  assert.match(liveDesk, /Leading category\s+action layer/);
  assert.match(liveDesk, /Next manual\s+batch/);
  assert.match(liveDesk, /Discovery\s+questions/);
  assert.match(liveDesk, /Pilot setup\s+checklist/);
  assert.match(liveDesk, /Integration\s+hold/);
  assert.match(liveDesk, /Focus leading\s+category/);
  assert.match(liveDesk, /local_services_category_pilot_score/);
  assert.match(liveDesk, /category_pilot_score/);
  assert.match(liveDesk, /local_services_leading_category_action_layer/);
  assert.match(liveDesk, /leading_category_action_layer/);
  assert.match(liveDesk, /Pilot setup readiness/);
  assert.match(liveDesk, /Paid pilot gate/);
  assert.match(liveDesk, /Ready for first paid pilot/);
  assert.match(liveDesk, /Not ready for paid pilot/);
  assert.match(liveDesk, /Week-one owner decision/);
  assert.match(liveDesk, /selectedLeadingCategoryWeekOneOwnerDecision/);
  assert.match(liveDesk, /recordedLeadingCategoryWeekOneOwnerDecision/);
  assert.match(liveDesk, /Continue recorded; paid-pilot proposal path can open if all other gates pass/);
  assert.match(liveDesk, /No paid pilot proposal unless Week-one owner decision is Continue/);
  assert.match(liveDesk, /Week-one review/);
  assert.match(liveDesk, /local_services_pilot_setup_readiness/);
  assert.match(liveDesk, /pilot_setup_readiness/);
  assert.match(liveDesk, /Readiness action plan/);
  assert.match(liveDesk, /Continue\s+setup\/test path/);
  assert.match(liveDesk, /Copy readiness action plan/);
  assert.match(liveDesk, /local_services_readiness_action_plan/);
  assert.match(liveDesk, /readiness_action_plan/);
  assert.match(liveDesk, /Open proof drawer/);
  assert.match(liveDesk, /Readiness proof drawer/);
  assert.match(liveDesk, /Copy readiness proof/);
  assert.match(liveDesk, /local_services_readiness_proof_drawer/);
  assert.match(liveDesk, /readiness_evidence_view/);
  assert.match(liveDesk, /Open proposal preview/);
  assert.match(liveDesk, /Paid pilot proposal preview/);
  assert.match(liveDesk, /Copy proposal preview/);
  assert.match(liveDesk, /local_services_paid_pilot_proposal_preview/);
  assert.match(liveDesk, /operator_approved_proposal_preview/);
  assert.match(liveDesk, /Open approval handoff/);
  assert.match(liveDesk, /Proposal approval handoff/);
  assert.match(liveDesk, /Copy approval handoff/);
  assert.match(liveDesk, /local_services_proposal_approval_handoff/);
  assert.match(liveDesk, /manual_paid_pilot_approval_handoff/);
  assert.match(liveDesk, /Proposal approval state/);
  assert.match(liveDesk, /Approve proposal handoff/);
  assert.match(liveDesk, /Needs changes/);
  assert.match(liveDesk, /Block proposal/);
  assert.match(liveDesk, /proposalApprovalByService/);
  assert.match(liveDesk, /Proposal approval decision/);
  assert.match(liveDesk, /Open kickoff gate/);
  assert.match(liveDesk, /Pilot kickoff gate/);
  assert.match(liveDesk, /Copy kickoff gate/);
  assert.match(liveDesk, /local_services_pilot_kickoff_gate/);
  assert.match(liveDesk, /manual_day_one_kickoff_gate/);
  assert.match(liveDesk, /Kickoff decision state/);
  assert.match(liveDesk, /Mark kickoff ready/);
  assert.match(liveDesk, /Needs more prep/);
  assert.match(liveDesk, /Block kickoff/);
  assert.match(liveDesk, /kickoffDecisionByService/);
  assert.match(liveDesk, /Kickoff operator decision/);
  assert.match(liveDesk, /Day-one outcome capture gate/);
  assert.match(liveDesk, /Reset day-one outcome/);
  assert.match(liveDesk, /Ready for manual weekly scorecard sync/);
  assert.match(liveDesk, /Open run sheet/);
  assert.match(liveDesk, /Day-one operator run sheet/);
  assert.match(liveDesk, /Copy run sheet/);
  assert.match(liveDesk, /local_services_day_one_operator_run_sheet/);
  assert.match(liveDesk, /manual_day_one_operator_run_sheet/);
  assert.match(liveDesk, /day_one_run_sheet_outcome_capture/);
  assert.match(liveDesk, /Open day-one recap/);
  assert.match(liveDesk, /Day-one recap/);
  assert.match(liveDesk, /Copy day-one recap/);
  assert.match(liveDesk, /local_services_day_one_recap/);
  assert.match(liveDesk, /manual_day_one_recap/);
  assert.match(liveDesk, /day_one_recap_to_week_one_review/);
  assert.match(liveDesk, /local_services_first_contact_batch_review/);
  assert.match(liveDesk, /founder_manual_validation_review/);
  assert.match(liveDesk, /no_booking_created/);
  assert.match(liveDesk, /Copy founder workspace/);
  assert.match(liveDesk, /Founder proof recorded/);
  assert.match(liveDesk, /local_services_founder_contact_workspace/);
  assert.match(demoFrontendServer, /local-services-founder-execution-log\.md/);
  assert.match(liveDesk, /Pilot metrics/);
  assert.match(liveDesk, /Missed-call recovery/);
  assert.match(liveDesk, /Manual operator edits/);
  assert.match(liveDesk, /No-show \/ cancellation/);
  assert.match(liveDesk, /pilotKit/);
  assert.match(liveDesk, /launchChecklist/);
  assert.match(liveDesk, /telegramIntake/);
  assert.match(liveDesk, /telegram_intake: template\.detail\.telegramIntake/);
  assert.match(liveDesk, /Dispatch payload preview/);
  assert.match(liveDesk, /Copy dispatch payload/);
  assert.match(liveDesk, /Open dispatch drawer/);
  assert.match(liveDesk, /Open customer drawer/);
  assert.match(liveDesk, /Open handoff drawer/);
  assert.match(liveDesk, /Dispatch payload drawer/);
  assert.match(liveDesk, /Customer confirmation drawer/);
  assert.match(liveDesk, /Master\/operator handoff drawer/);
  assert.match(liveDesk, /LocalServiceDispatchDrawer/);
  assert.match(liveDesk, /type LocalServiceExportKind = "dispatch" \| "customer" \| "handoff";/);
  assert.match(liveDesk, /type LocalServicePayloadPreview = \{/);
  assert.match(liveDesk, /type LocalServiceDispatchExport = \{/);
  assert.match(liveDesk, /function buildLocalServicePayloadPreview\(/);
  assert.match(liveDesk, /function buildLocalServiceDispatchExport\(/);
  assert.match(liveDesk, /Local services dispatch export/);
  assert.match(liveDesk, /Dispatch export mode/);
  assert.match(liveDesk, /Operator approval checklist/);
  assert.match(liveDesk, /Copy dispatch export/);
  assert.match(liveDesk, /Copy customer confirmation/);
  assert.match(liveDesk, /Copy master handoff/);
  assert.match(liveDesk, /export_kind: kind/);
  assert.match(liveDesk, /operator_required_before_customer_or_master_send/);
  assert.match(liveDesk, /Dispatch payload copied/);
  assert.match(liveDesk, /title: `\$\{label\} copied`/);
  assert.match(liveDesk, /handoff_status: "approval_required"/);
  assert.match(liveDesk, /approval_policy/);
  assert.match(liveDesk, /evidence_link/);
  assert.match(liveDesk, /bundle_path/);
  assert.match(liveDesk, /buildCaseEvidencePath\("LS-7101"\)/);
  assert.match(liveDesk, /buildCaseBundlePath\("LS-7302"\)/);
  assert.match(liveDesk, /Playbook templates/);
  assert.match(liveDesk, /const activePlaybookId = searchParams\.get\("playbook"\);/);
  assert.match(liveDesk, /Visa lead qualification/);
  assert.match(liveDesk, /Missing-document follow-up/);
  assert.match(liveDesk, /Consultation booking prep/);
  assert.match(liveDesk, /CRM handoff summary/);
  assert.match(liveDesk, /highlights: \[/);
  assert.match(liveDesk, /label: "Outcome"/);
  assert.match(liveDesk, /label: "Approval"/);
  assert.match(liveDesk, /label: "Evidence"/);
  assert.match(liveDesk, /label: "Deliverable"/);
  assert.match(liveDesk, /Console review only/);
  assert.match(liveDesk, /Reminder queued/);
  assert.match(liveDesk, /Protected follow-up/);
  assert.match(liveDesk, /Presentation bundle/);
  assert.match(liveDesk, /CRM-ready handoff/);
  assert.match(liveDesk, /Resolved case proof/);
  assert.match(liveDesk, /Sample input/);
  assert.match(liveDesk, /Approval policy/);
  assert.match(liveDesk, /Evidence output/);
  assert.match(liveDesk, /CRM fields/);
  assert.match(liveDesk, /Payload preview/);
  assert.match(liveDesk, /Surface path/);
  assert.match(liveDesk, /Copy payload/);
  assert.match(liveDesk, /Open export drawer/);
  assert.match(liveDesk, /CRM payload drawer/);
  assert.match(liveDesk, /Consultation handoff drawer/);
  assert.match(liveDesk, /Human-readable/);
  assert.match(liveDesk, /JSON/);
  assert.match(liveDesk, /Integration-ready export/);
  assert.match(liveDesk, /Operator review checklist/);
  assert.match(liveDesk, /Selected template/);
  assert.match(liveDesk, /Inspect template/);
  assert.match(liveDesk, /type PlaybookPayloadPreview = \{/);
  assert.match(liveDesk, /type PlaybookExportMode = "human" \| "json";/);
  assert.match(liveDesk, /type PlaybookOperatorExport = \{/);
  assert.match(liveDesk, /function buildPlaybookPayloadPreview\(/);
  assert.match(liveDesk, /function buildPlaybookOperatorExport\(/);
  assert.match(liveDesk, /const PlaybookTemplateDetailPanel = \(\{/);
  assert.match(liveDesk, /const PlaybookExportDrawer = \(\{/);
  assert.match(liveDesk, /payloadPreview: PlaybookPayloadPreview;/);
  assert.match(liveDesk, /onCopyPayload: \(\) => void;/);
  assert.match(liveDesk, /onOpenExportDrawer: \(\) => void;/);
  assert.match(liveDesk, /next\.set\("playbook", "missing-documents"\);/);
  assert.match(liveDesk, /next\.set\("playbook", template\.id\);/);
  assert.match(liveDesk, /lead_status/);
  assert.match(liveDesk, /follow_up_status/);
  assert.match(liveDesk, /consultation_at/);
  assert.match(liveDesk, /case_status/);
  assert.match(liveDesk, /delivery_channel/);
  assert.match(liveDesk, /surface_path/);
  assert.match(liveDesk, /approval_owner/);
  assert.match(liveDesk, /bundle_path/);
  assert.match(liveDesk, /navigator\.clipboard\.writeText/);
  assert.match(liveDesk, /title: "Payload copied"/);
  assert.match(liveDesk, /title: `\$\{label\} copied`/);
  assert.match(liveDesk, /setPlaybookExportMode\(activePlaybookTemplate\?\.id === "crm-handoff" \? "json" : "human"\);/);
  assert.match(liveDesk, /navigate\(activePlaybookPayloadPreview\.surfacePath\);/);
  assert.match(liveDesk, /Review approval/);
  assert.match(liveDesk, /Start 7-minute demo/);
  assert.match(liveDesk, /Exit demo/);
  assert.match(liveDesk, /Presentation bundle/);
  assert.match(liveDesk, /Evidence bundle/);
  assert.match(liveDesk, /cases\.find\(\(c\) => c\.ref === "VS-2841"\)/);
  assert.match(liveDesk, /cases\.find\(\(c\) => c\.ref === "VS-2838"\)/);
  assert.match(liveDesk, /cases\.find\(\(c\) => c\.ref === "VS-2840"\)/);
  assert.match(liveDesk, /cases\.find\(\(c\) => c\.ref === "VS-2837"\)/);
  assert.match(liveDesk, /next\.set\("demo", "visa-intake"\);/);
  assert.match(liveDesk, /navigate\(`\/app\/console\?ref=\$\{encodeURIComponent\(visaIntakeDemoCase\.ref\)\}&focus=approval`\)/);
  assert.match(liveDesk, /navigate\(`\/app\/console\?ref=\$\{encodeURIComponent\(leadQualificationCase\.ref\)\}`\)/);
  assert.match(liveDesk, /navigate\(`\/app\/console\?ref=\$\{encodeURIComponent\(missingDocumentsCase\.ref\)\}&focus=documents`\)/);
  assert.match(liveDesk, /navigate\(buildCaseBundlePath\(consultationCase\)\)/);
  assert.match(liveDesk, /navigate\(buildCaseVaultPath\(crmHandoffCase\)\)/);
  assert.match(liveDesk, /navigate\(buildCaseBundlePath\(visaIntakeDemoCase\)\)/);
  assert.match(liveDesk, /navigate\(buildCaseEvidencePath\(visaIntakeDemoCase\)\)/);
  assert.match(liveDesk, /navigate\(buildCaseVaultPath\(visaIntakeDemoCase\)\)/);

  assert.match(readme, /Seven-minute product path:/);
  assert.match(readme, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch/);
  assert.match(readme, /AI Dispatcher for local service businesses/);
  assert.match(readme, /AC repair dispatch/);
  assert.match(readme, /Plumbing emergency/);
  assert.match(readme, /Cleaning quote and booking/);
  assert.match(readme, /Measurement visit booking/);
  assert.match(readme, /construction-material quote and delivery stays out of P0/);
  assert.match(readme, /operator-approved/);
  assert.match(readme, /Open dispatch drawer/);
  assert.match(readme, /Open customer\s+drawer/);
  assert.match(readme, /Open handoff drawer/);
  assert.match(readme, /Telegram intake prototype/);
  assert.match(readme, /Open intake evidence/);
  assert.match(readme, /Transcript \+ evidence/);
  assert.match(readme, /Saved intake evidence/);
  assert.match(readme, /Intake transcript \+ evidence link/);
  assert.match(readme, /Copy intake evidence/);
  assert.match(readme, /local_services_intake_evidence/);
  assert.match(readme, /transcript_evidence_link/);
  assert.match(readme, /Pilot readiness/);
  assert.match(readme, /One-page offer/);
  assert.match(readme, /90-second demo script/);
  assert.match(readme, /Launch checklist/);
  assert.match(readme, /Agent setup \/ training state/);
  assert.match(readme, /Open setup checklist/);
  assert.match(readme, /Business profile/);
  assert.match(readme, /Knowledge sources/);
  assert.match(readme, /Agent behavior/);
  assert.match(readme, /Test call\/message/);
  assert.match(readme, /Ready for test call\/message/);
  assert.match(readme, /Training cards/);
  assert.match(readme, /Copy setup brief/);
  assert.match(readme, /local_services_agent_setup_training/);
  assert.match(readme, /\?setup=7min/);
  assert.match(readme, /7-minute setup wizard/);
  assert.match(readme, /Setup path/);
  assert.match(readme, /Open day-one setup/);
  assert.match(readme, /setupStepCompletionByService/);
  assert.match(readme, /setupReadyByService/);
  assert.match(readme, /Setup progress/);
  assert.match(readme, /Saved setup state/);
  assert.match(readme, /Mark complete/);
  assert.match(readme, /Mark ready for pilot test/);
  assert.match(readme, /Ready for pilot test/);
  assert.match(readme, /Test call\/message panel/);
  assert.match(readme, /Sample inbound/);
  assert.match(readme, /Expected extracted fields/);
  assert.match(readme, /Pass\/fail checklist/);
  assert.match(readme, /Mark check passed/);
  assert.match(readme, /Record test passed/);
  assert.match(readme, /Test call passed/);
  assert.match(readme, /Reset test call/);
  assert.match(readme, /testCallChecklistByService/);
  assert.match(readme, /testCallPassedByService/);
  assert.match(readme, /Outreach list filters/);
  assert.match(readme, /Column settings/);
  assert.match(readme, /Service filter/);
  assert.match(readme, /Status filter/);
  assert.match(readme, /Filtered candidates/);
  assert.match(readme, /Filtered outreach list/);
  assert.match(readme, /All services/);
  assert.match(readme, /All statuses/);
  assert.match(readme, /Clear filters/);
  assert.match(readme, /View only, no send/);
  assert.match(readme, /Pilot execution checklist/);
  assert.match(readme, /Pass test call\/message/);
  assert.match(readme, /Needs test call passed/);
  assert.match(readme, /Prepare first manual batch/);
  assert.match(readme, /Ready for first manual\s+batch/);
  assert.match(readme, /Record ready drafts/);
  assert.match(readme, /Log manual contact/);
  assert.match(readme, /Book discovery call/);
  assert.match(readme, /Start metric capture/);
  assert.match(readme, /Pilot checklist progress/);
  assert.match(readme, /Dry run required/);
  assert.match(readme, /Dry run passed/);
  assert.match(readme, /Manual launch blocked/);
  assert.match(readme, /Manual launch ready/);
  assert.match(readme, /Open launch packet/);
  assert.match(readme, /Pilot launch packet/);
  assert.match(readme, /Launch packet preview/);
  assert.match(readme, /First manual contact checklist/);
  assert.match(readme, /Copy launch packet/);
  assert.match(readme, /local_services_pilot_launch_packet/);
  assert.match(readme, /Manual activity log/);
  assert.match(readme, /Last manual action/);
  assert.match(readme, /Copy activity log/);
  assert.match(readme, /local_services_manual_activity_log/);
  assert.match(readme, /No external side\s+effects/);
  assert.match(readme, /Founder\/operator validation/);
  assert.match(readme, /No autonomous send/);
  assert.match(readme, /Open discovery prep/);
  assert.match(readme, /Discovery call\s+prep/);
  assert.match(readme, /Questions to ask/);
  assert.match(readme, /Pilot success criteria/);
  assert.match(readme, /Copy discovery call prep/);
  assert.match(readme, /local_services_discovery_call_prep/);
  assert.match(readme, /Open day-one setup/);
  assert.match(readme, /Day-one setup brief/);
  assert.match(readme, /Business profile lock/);
  assert.match(readme, /Setup tasks/);
  assert.match(readme, /Test call plan/);
  assert.match(readme, /Copy day-one setup brief/);
  assert.match(readme, /local_services_day_one_setup_brief/);
  assert.match(readme, /Open pilot runbook/);
  assert.match(readme, /Pilot metrics/);
  assert.match(readme, /Pilot outreach wizard/);
  assert.match(readme, /Audience from outreach list/);
  assert.match(readme, /Message\/test preview/);
  assert.match(readme, /Preview \/ Test message modal/);
  assert.match(readme, /Copy test message/);
  assert.match(readme, /Copy test message preview/);
  assert.match(readme, /Operator confirmation/);
  assert.match(readme, /Operator confirmation summary/);
  assert.match(readme, /Ready for manual outreach/);
  assert.match(readme, /Copy confirmation summary/);
  assert.match(readme, /Wizard progress/);
  assert.match(readme, /Record ready for manual outreach/);
  assert.match(readme, /Ready for manual outreach recorded/);
  assert.match(readme, /Ask AI about pilot/);
  assert.match(readme, /Suggested questions/);
  assert.match(readme, /Copy analyst brief/);
  assert.match(readme, /local_services_pilot_ai_analyst/);
  assert.match(readme, /no\s+external LLM call/i);
  assert.match(readme, /Pilot scorecard action/);
  assert.match(readme, /Record scorecard draft/);
  assert.match(readme, /Not contacted/);
  assert.match(readme, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(readme, /Draft ready/);
  assert.match(readme, /Contacted manually/);
  assert.match(readme, /Reply received/);
  assert.match(readme, /Rejected for now/);
  assert.match(readme, /First request outcome/);
  assert.match(readme, /Manual outcome state/);
  assert.match(readme, /firstRequestOutcomeByProspectKey/);
  assert.match(readme, /Needs follow-up/);
  assert.match(readme, /Booked manually/);
  assert.match(readme, /Outcome chain summary/);
  assert.match(readme, /Scorecard draft/);
  assert.match(readme, /Week-one review/);
  assert.match(readme, /Pilot funnel summary/);
  assert.match(readme, /All candidates/);
  assert.match(readme, /Next manual\s+batch/);
  assert.match(readme, /Pilot workspace export drawer/);
  assert.match(readme, /Copy pilot workspace export/);
  assert.match(readme, /Manual activity log/);
  assert.match(readme, /Last manual action/);
  assert.match(readme, /Pilot metrics tracker/);
  assert.match(readme, /Copy pilot metrics tracker/);
  assert.match(readme, /Pilot daily log/);
  assert.match(readme, /Open daily log/);
  assert.match(readme, /Daily capture fields/);
  assert.match(readme, /Daily operating loop/);
  assert.match(readme, /Copy pilot daily log/);
  assert.match(readme, /local_services_pilot_daily_log/);
  assert.match(readme, /weekly scorecard sync/);
  assert.match(readme, /Day-one outcome capture gate/);
  assert.match(readme, /Weekly scorecard sync gate/);
  assert.match(readme, /manual_weekly_scorecard_sync_gate/);
  assert.match(readme, /Open weekly sync checklist/);
  assert.match(readme, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(readme, /weeklyScorecardSyncReviewedByService/);
  assert.match(readme, /Evidence readiness/);
  assert.match(readme, /Pilot week-one review/);
  assert.match(readme, /Open week-one review/);
  assert.match(readme, /Continue \/ stop decision/);
  assert.match(readme, /Copy week-one review/);
  assert.match(readme, /local_services_pilot_week_one_review/);
  assert.match(readme, /continue, pause, or stop decision/);
  assert.match(readme, /Owner-ready summary/);
  assert.match(readme, /Decision readiness/);
  assert.match(readme, /Latest manual signal/);
  assert.match(readme, /day_one_recap_to_week_one_review/);
  assert.match(readme, /Week-one owner\s+decision state/);
  assert.match(readme, /Record continue/);
  assert.match(readme, /Record pause/);
  assert.match(readme, /Record stop/);
  assert.match(readme, /weekOneOwnerDecisionByProspectKey/);
  assert.match(readme, /week_one_owner_decision_to_evidence_pack/);
  assert.match(readme, /Pilot evidence\s+pack/);
  assert.match(readme, /Open evidence pack/);
  assert.match(readme, /Week-two evidence pack/);
  assert.match(readme, /Copy evidence pack/);
  assert.match(readme, /local_services_pilot_evidence_pack/);
  assert.match(readme, /Week-one owner decision/);
  assert.match(readme, /paid-pilot readiness proof pack/);
  assert.match(readme, /\/workspace-docs\/local-services-pilot-offer\.md/);
  assert.match(readme, /\/workspace-docs\/local-services-demo-script\.md/);
  assert.match(readme, /Open recording checklist/);
  assert.match(readme, /\/workspace-docs\/local-services-demo-recording-checklist\.md/);
  assert.match(readme, /\?recording=90s/);
  assert.match(readme, /90-second recording mode/);
  assert.match(readme, /Recording path/);
  assert.match(readme, /\/workspace-docs\/local-services-outreach-list\.md/);
  assert.match(readme, /\/workspace-docs\/local-services-pilot-scorecard\.md/);
  assert.match(readme, /Open founder execution log/);
  assert.match(readme, /\/workspace-docs\/local-services-founder-execution-log\.md/);
  assert.match(readme, /first-10-contact worksheet/);
  assert.match(readme, /First 10 contacts workspace/);
  assert.match(readme, /Pilot proof checklist/);
  assert.match(readme, /Pilot ops today/);
  assert.match(readme, /local_services_pilot_ops_today/);
  assert.match(readme, /Open ops confirmation/);
  assert.match(readme, /local_services_pilot_ops_confirmation/);
  assert.match(readme, /Pilot proof update rail/);
  assert.match(readme, /local_services_pilot_proof_update_rail/);
  assert.match(readme, /Stop \/ Continue decision gate/);
  assert.match(readme, /Open batch review/);
  assert.match(readme, /Copy batch review/);
  assert.match(readme, /Copy founder workspace/);
  assert.match(readme, /NEWO-style AI\s+employee\s+platform/);
  assert.match(readme, /Category pilot score/);
  assert.match(readme, /Leading category/);
  assert.match(readme, /No category expansion without proof/);
  assert.match(readme, /Leading category\s+action layer/);
  assert.match(readme, /Next manual\s+batch/);
  assert.match(readme, /Discovery\s+questions/);
  assert.match(readme, /Pilot setup\s+checklist/);
  assert.match(readme, /Integration\s+hold/);
  assert.match(readme, /Focus leading\s+category/);
  assert.match(readme, /Pilot setup readiness/);
  assert.match(readme, /Paid pilot gate/);
  assert.match(readme, /Ready for first paid pilot/);
  assert.match(readme, /Not ready for paid pilot/);
  assert.match(readme, /Week-one owner decision/);
  assert.match(readme, /only `Continue` can move the lane toward `Paid pilot proposal`/);
  assert.match(readme, /Readiness action\s+plan/);
  assert.match(readme, /Continue\s+setup\/test path/);
  assert.match(readme, /Copy readiness action plan/);
  assert.match(readme, /local_services_readiness_action_plan/);
  assert.match(readme, /Open proof drawer/);
  assert.match(readme, /Readiness proof drawer/);
  assert.match(readme, /Copy readiness proof/);
  assert.match(readme, /local_services_readiness_proof_drawer/);
  assert.match(readme, /Open proposal preview/);
  assert.match(readme, /Paid pilot proposal preview/);
  assert.match(readme, /Copy proposal preview/);
  assert.match(readme, /local_services_paid_pilot_proposal_preview/);
  assert.match(readme, /Open approval handoff/);
  assert.match(readme, /Proposal approval handoff/);
  assert.match(readme, /Copy approval handoff/);
  assert.match(readme, /local_services_proposal_approval_handoff/);
  assert.match(readme, /Proposal\s+approval state/);
  assert.match(readme, /Approve proposal handoff/);
  assert.match(readme, /proposalApprovalByService/);
  assert.match(readme, /Open kickoff gate/);
  assert.match(readme, /Pilot kickoff gate/);
  assert.match(readme, /Copy kickoff gate/);
  assert.match(readme, /local_services_pilot_kickoff_gate/);
  assert.match(readme, /Kickoff\s+decision state/);
  assert.match(readme, /Mark kickoff ready/);
  assert.match(readme, /kickoffDecisionByService/);
  assert.match(readme, /Open run sheet/);
  assert.match(readme, /Day-one operator run sheet/);
  assert.match(readme, /Copy run sheet/);
  assert.match(readme, /local_services_day_one_operator_run_sheet/);
  assert.match(readme, /manual_day_one_operator_run_sheet/);
  assert.match(readme, /day_one_run_sheet_outcome_capture/);
  assert.match(readme, /Open day-one recap/);
  assert.match(readme, /Day-one recap/);
  assert.match(readme, /Copy day-one recap/);
  assert.match(readme, /local_services_day_one_recap/);
  assert.match(readme, /day_one_recap_to_week_one_review/);
  assert.match(readme, /docs\/local-services-pilot-runbook\.md/);
  assert.match(readme, /docs\/local-services-outreach-execution-pack\.md/);
  assert.match(readme, /Open outreach execution pack/);
  assert.match(readme, /Start 7-minute demo/);
  assert.match(readme, /docs\/evidence-and-trust\.md/);
  assert.match(readme, /Live Desk also exposes four productized playbook templates/);
  assert.match(readme, /Outcome`, `Approval`, `Evidence`, and `Deliverable/);
  assert.match(readme, /Selecting a template opens an inline detail panel/);
  assert.match(readme, /\/app\?playbook=<lane-id>/);
  assert.match(readme, /Payload preview`, `Surface path`, and/);
  assert.match(readme, /Copy payload/);
  assert.match(readme, /Open export drawer/);
  assert.match(readme, /CRM payload drawer/);
  assert.match(readme, /Consultation handoff drawer/);
  assert.match(readme, /Human-readable` and `JSON`/);
  assert.match(localDevelopment, /\/app\?demo=visa-intake/);
  assert.match(localDevelopment, /docs\/evidence-and-trust\.md/);
  assert.match(localDevelopment, /replay, signing, release evidence/);
  assert.match(localDevelopment, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch/);
  assert.match(localDevelopment, /AI Dispatcher for Local Services/);
  assert.match(localDevelopment, /Measurement visit booking/);
  assert.match(localDevelopment, /measurement lane is construction-adjacent P0/);
  assert.match(localDevelopment, /Dispatch payload preview/);
  assert.match(localDevelopment, /Open dispatch drawer/);
  assert.match(localDevelopment, /Open customer drawer/);
  assert.match(localDevelopment, /Open handoff drawer/);
  assert.match(localDevelopment, /Telegram intake prototype/);
  assert.match(localDevelopment, /Open intake evidence/);
  assert.match(localDevelopment, /Transcript \+ evidence/);
  assert.match(localDevelopment, /Saved intake evidence/);
  assert.match(localDevelopment, /Intake transcript \+ evidence link/);
  assert.match(localDevelopment, /Copy intake evidence/);
  assert.match(localDevelopment, /local_services_intake_evidence/);
  assert.match(localDevelopment, /transcript_evidence_link/);
  assert.match(localDevelopment, /Pilot readiness/);
  assert.match(localDevelopment, /One-page offer/);
  assert.match(localDevelopment, /90-second demo script/);
  assert.match(localDevelopment, /Launch checklist/);
  assert.match(localDevelopment, /Agent setup \/ training state/);
  assert.match(localDevelopment, /Open setup checklist/);
  assert.match(localDevelopment, /Business profile/);
  assert.match(localDevelopment, /Knowledge sources/);
  assert.match(localDevelopment, /Agent behavior/);
  assert.match(localDevelopment, /Test call\/message/);
  assert.match(localDevelopment, /Ready for test call\/message/);
  assert.match(localDevelopment, /Training cards/);
  assert.match(localDevelopment, /Copy setup brief/);
  assert.match(localDevelopment, /local_services_agent_setup_training/);
  assert.match(localDevelopment, /No channel activation/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&setup=7min/);
  assert.match(localDevelopment, /7-minute setup wizard/);
  assert.match(localDevelopment, /Setup path/);
  assert.match(localDevelopment, /Open day-one setup/);
  assert.match(localDevelopment, /setupStepCompletionByService/);
  assert.match(localDevelopment, /setupReadyByService/);
  assert.match(localDevelopment, /Setup progress/);
  assert.match(localDevelopment, /Saved setup state/);
  assert.match(localDevelopment, /Mark complete/);
  assert.match(localDevelopment, /Mark ready for pilot test/);
  assert.match(localDevelopment, /Ready for pilot test/);
  assert.match(localDevelopment, /Test call\/message panel/);
  assert.match(localDevelopment, /Sample inbound/);
  assert.match(localDevelopment, /Expected extracted fields/);
  assert.match(localDevelopment, /Pass\/fail checklist/);
  assert.match(localDevelopment, /Mark check passed/);
  assert.match(localDevelopment, /Record test passed/);
  assert.match(localDevelopment, /Test call passed/);
  assert.match(localDevelopment, /Reset test call/);
  assert.match(localDevelopment, /testCallChecklistByService/);
  assert.match(localDevelopment, /testCallPassedByService/);
  assert.match(localDevelopment, /Outreach list filters/);
  assert.match(localDevelopment, /Column settings/);
  assert.match(localDevelopment, /Service filter/);
  assert.match(localDevelopment, /Status filter/);
  assert.match(localDevelopment, /Filtered candidates/);
  assert.match(localDevelopment, /Filtered outreach list/);
  assert.match(localDevelopment, /All services/);
  assert.match(localDevelopment, /All statuses/);
  assert.match(localDevelopment, /Clear filters/);
  assert.match(localDevelopment, /View only, no send/);
  assert.match(localDevelopment, /Pilot execution checklist/);
  assert.match(localDevelopment, /Pass test call\/message/);
  assert.match(localDevelopment, /Needs test call passed/);
  assert.match(localDevelopment, /Prepare first manual batch/);
  assert.match(localDevelopment, /Ready for first manual batch/);
  assert.match(localDevelopment, /Record ready drafts/);
  assert.match(localDevelopment, /Log manual contact/);
  assert.match(localDevelopment, /Book discovery call/);
  assert.match(localDevelopment, /Start metric capture/);
  assert.match(localDevelopment, /Pilot checklist progress/);
  assert.match(localDevelopment, /Dry run required/);
  assert.match(localDevelopment, /Dry run passed/);
  assert.match(localDevelopment, /Manual launch blocked/);
  assert.match(localDevelopment, /Manual launch ready/);
  assert.match(localDevelopment, /Open launch packet/);
  assert.match(localDevelopment, /Pilot launch packet/);
  assert.match(localDevelopment, /Launch packet\s+preview/);
  assert.match(localDevelopment, /First manual contact checklist/);
  assert.match(localDevelopment, /Copy launch packet/);
  assert.match(localDevelopment, /local_services_pilot_launch_packet/);
  assert.match(localDevelopment, /Manual activity log/);
  assert.match(localDevelopment, /Last manual action/);
  assert.match(localDevelopment, /Copy activity log/);
  assert.match(localDevelopment, /local_services_manual_activity_log/);
  assert.match(localDevelopment, /No external side\s+effects/);
  assert.match(localDevelopment, /Founder\/operator validation/);
  assert.match(localDevelopment, /No autonomous send/);
  assert.match(localDevelopment, /Open discovery prep/);
  assert.match(localDevelopment, /Discovery call\s+prep/);
  assert.match(localDevelopment, /Questions to ask/);
  assert.match(localDevelopment, /Pilot success criteria/);
  assert.match(localDevelopment, /Copy discovery call prep/);
  assert.match(localDevelopment, /local_services_discovery_call_prep/);
  assert.match(localDevelopment, /Open day-one setup/);
  assert.match(localDevelopment, /Day-one setup brief/);
  assert.match(localDevelopment, /Business profile lock/);
  assert.match(localDevelopment, /Setup tasks/);
  assert.match(localDevelopment, /Test call plan/);
  assert.match(localDevelopment, /Copy day-one setup brief/);
  assert.match(localDevelopment, /local_services_day_one_setup_brief/);
  assert.match(localDevelopment, /Open pilot runbook/);
  assert.match(localDevelopment, /Pilot metrics/);
  assert.match(localDevelopment, /Pilot outreach wizard/);
  assert.match(localDevelopment, /4-step/);
  assert.match(localDevelopment, /Audience from outreach list/);
  assert.match(localDevelopment, /Message\/test preview/);
  assert.match(localDevelopment, /Preview \/ Test message modal/);
  assert.match(localDevelopment, /Copy test message/);
  assert.match(localDevelopment, /Copy test message preview/);
  assert.match(localDevelopment, /Operator confirmation/);
  assert.match(localDevelopment, /Operator confirmation summary/);
  assert.match(localDevelopment, /Ready for manual outreach/);
  assert.match(localDevelopment, /Copy confirmation summary/);
  assert.match(localDevelopment, /Wizard progress/);
  assert.match(localDevelopment, /Record ready for manual outreach/);
  assert.match(localDevelopment, /Ready for manual outreach recorded/);
  assert.match(localDevelopment, /Ask AI about pilot/);
  assert.match(localDevelopment, /Suggested questions/);
  assert.match(localDevelopment, /Copy analyst brief/);
  assert.match(localDevelopment, /local_services_pilot_ai_analyst/);
  assert.match(localDevelopment, /No external LLM call/);
  assert.match(localDevelopment, /Pilot scorecard action/);
  assert.match(localDevelopment, /Selected company/);
  assert.match(localDevelopment, /Record scorecard draft/);
  assert.match(localDevelopment, /Pilot workspace state/);
  assert.match(localDevelopment, /Saved in this browser/);
  assert.match(localDevelopment, /Contacted manually/);
  assert.match(localDevelopment, /Reply received/);
  assert.match(localDevelopment, /Rejected for now/);
  assert.match(localDevelopment, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(localDevelopment, /First request outcome/);
  assert.match(localDevelopment, /Manual outcome state/);
  assert.match(localDevelopment, /firstRequestOutcomeByProspectKey/);
  assert.match(localDevelopment, /Needs follow-up/);
  assert.match(localDevelopment, /Booked manually/);
  assert.match(localDevelopment, /Outcome chain summary/);
  assert.match(localDevelopment, /Scorecard draft/);
  assert.match(localDevelopment, /Week-one review/);
  assert.match(localDevelopment, /Pilot funnel summary/);
  assert.match(localDevelopment, /All candidates/);
  assert.match(localDevelopment, /Next manual\s+batch/);
  assert.match(localDevelopment, /Manual execution rule/);
  assert.match(localDevelopment, /Open pilot export/);
  assert.match(localDevelopment, /Open metrics tracker/);
  assert.match(localDevelopment, /Pilot daily\s+log/);
  assert.match(localDevelopment, /Open daily log/);
  assert.match(localDevelopment, /Daily capture fields/);
  assert.match(localDevelopment, /Daily operating loop/);
  assert.match(localDevelopment, /Copy pilot daily log/);
  assert.match(localDevelopment, /local_services_pilot_daily_log/);
  assert.match(localDevelopment, /weekly scorecard sync/);
  assert.match(localDevelopment, /Day-one outcome capture gate/);
  assert.match(localDevelopment, /Weekly scorecard sync gate/);
  assert.match(localDevelopment, /manual_weekly_scorecard_sync_gate/);
  assert.match(localDevelopment, /Open weekly sync checklist/);
  assert.match(localDevelopment, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(localDevelopment, /weeklyScorecardSyncReviewedByService/);
  assert.match(localDevelopment, /Pilot\s+week-one review/);
  assert.match(localDevelopment, /Open week-one review/);
  assert.match(localDevelopment, /Continue \/ stop decision/);
  assert.match(localDevelopment, /Copy week-one review/);
  assert.match(localDevelopment, /local_services_pilot_week_one_review/);
  assert.match(localDevelopment, /reviewed week-one decision packet/);
  assert.match(localDevelopment, /Owner-ready summary/);
  assert.match(localDevelopment, /Decision readiness/);
  assert.match(localDevelopment, /Latest manual signal/);
  assert.match(localDevelopment, /day_one_recap_to_week_one_review/);
  assert.match(localDevelopment, /Week-one owner\s+decision state/);
  assert.match(localDevelopment, /Record continue/);
  assert.match(localDevelopment, /Record pause/);
  assert.match(localDevelopment, /Record stop/);
  assert.match(localDevelopment, /weekOneOwnerDecisionByProspectKey/);
  assert.match(localDevelopment, /week_one_owner_decision_to_evidence_pack/);
  assert.match(localDevelopment, /Pilot\s+evidence pack/);
  assert.match(localDevelopment, /Open evidence pack/);
  assert.match(localDevelopment, /Week-two evidence pack/);
  assert.match(localDevelopment, /Copy evidence pack/);
  assert.match(localDevelopment, /local_services_pilot_evidence_pack/);
  assert.match(localDevelopment, /Week-one owner decision/);
  assert.match(localDevelopment, /paid-pilot readiness proof pack/);
  assert.match(localDevelopment, /No outbound message sent/);
  assert.match(localDevelopment, /Open offer doc/);
  assert.match(localDevelopment, /Open demo script/);
  assert.match(localDevelopment, /Open outreach list/);
  assert.match(localDevelopment, /Open outreach execution pack/);
  assert.match(localDevelopment, /Open pilot scorecard/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/workspace-docs\/local-services-pilot-offer\.md/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/workspace-docs\/local-services-demo-script\.md/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/workspace-docs\/local-services-demo-recording-checklist\.md/);
  assert.match(localDevelopment, /Open founder execution log/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/workspace-docs\/local-services-founder-execution-log\.md/);
  assert.match(localDevelopment, /first-10-contact worksheet/);
  assert.match(localDevelopment, /First 10 contacts workspace/);
  assert.match(localDevelopment, /Pilot proof\s+checklist/);
  assert.match(localDevelopment, /Pilot ops today/);
  assert.match(localDevelopment, /local_services_pilot_ops_today/);
  assert.match(localDevelopment, /Open ops confirmation/);
  assert.match(localDevelopment, /local_services_pilot_ops_confirmation/);
  assert.match(localDevelopment, /Pilot proof update rail/);
  assert.match(localDevelopment, /local_services_pilot_proof_update_rail/);
  assert.match(localDevelopment, /Stop \/ Continue decision gate/);
  assert.match(localDevelopment, /Open batch review/);
  assert.match(localDevelopment, /First\s+contact batch\s+review drawer/);
  assert.match(localDevelopment, /Copy batch review/);
  assert.match(localDevelopment, /Copy founder workspace/);
  assert.match(localDevelopment, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(localDevelopment, /NEWO-style AI employee/);
  assert.match(localDevelopment, /Category pilot score/);
  assert.match(localDevelopment, /Leading category/);
  assert.match(localDevelopment, /No category expansion without proof/);
  assert.match(localDevelopment, /Leading category\s+action layer/);
  assert.match(localDevelopment, /Next manual\s+batch/);
  assert.match(localDevelopment, /Discovery\s+questions/);
  assert.match(localDevelopment, /Pilot setup\s+checklist/);
  assert.match(localDevelopment, /Integration\s+hold/);
  assert.match(localDevelopment, /Focus leading\s+category/);
  assert.match(localDevelopment, /Pilot setup readiness/);
  assert.match(localDevelopment, /Paid pilot gate/);
  assert.match(localDevelopment, /Ready for first paid pilot/);
  assert.match(localDevelopment, /Not ready for paid pilot/);
  assert.match(localDevelopment, /Week-one owner decision/);
  assert.match(localDevelopment, /only\s+`Continue` can move the lane toward `Paid pilot proposal`/);
  assert.match(localDevelopment, /Readiness action\s+plan/);
  assert.match(localDevelopment, /Continue\s+setup\/test path/);
  assert.match(localDevelopment, /Copy readiness action plan/);
  assert.match(localDevelopment, /local_services_readiness_action_plan/);
  assert.match(localDevelopment, /Open proof drawer/);
  assert.match(localDevelopment, /Readiness proof drawer/);
  assert.match(localDevelopment, /Copy readiness proof/);
  assert.match(localDevelopment, /local_services_readiness_proof_drawer/);
  assert.match(localDevelopment, /Open proposal preview/);
  assert.match(localDevelopment, /Paid pilot proposal preview/);
  assert.match(localDevelopment, /Copy proposal preview/);
  assert.match(localDevelopment, /local_services_paid_pilot_proposal_preview/);
  assert.match(localDevelopment, /Open approval handoff/);
  assert.match(localDevelopment, /Proposal approval handoff/);
  assert.match(localDevelopment, /Copy approval handoff/);
  assert.match(localDevelopment, /local_services_proposal_approval_handoff/);
  assert.match(localDevelopment, /Proposal\s+approval state/);
  assert.match(localDevelopment, /Approve proposal handoff/);
  assert.match(localDevelopment, /proposalApprovalByService/);
  assert.match(localDevelopment, /Open kickoff gate/);
  assert.match(localDevelopment, /Pilot kickoff gate/);
  assert.match(localDevelopment, /Copy kickoff gate/);
  assert.match(localDevelopment, /local_services_pilot_kickoff_gate/);
  assert.match(localDevelopment, /Kickoff\s+decision state/);
  assert.match(localDevelopment, /Mark kickoff ready/);
  assert.match(localDevelopment, /kickoffDecisionByService/);
  assert.match(localDevelopment, /Open run sheet/);
  assert.match(localDevelopment, /Day-one operator run sheet/);
  assert.match(localDevelopment, /Copy run sheet/);
  assert.match(localDevelopment, /local_services_day_one_operator_run_sheet/);
  assert.match(localDevelopment, /manual_day_one_operator_run_sheet/);
  assert.match(localDevelopment, /day_one_run_sheet_outcome_capture/);
  assert.match(localDevelopment, /Open day-one recap/);
  assert.match(localDevelopment, /Day-one recap/);
  assert.match(localDevelopment, /Copy day-one recap/);
  assert.match(localDevelopment, /local_services_day_one_recap/);
  assert.match(localDevelopment, /day_one_recap_to_week_one_review/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&recording=90s/);
  assert.match(localDevelopment, /90-second recording mode/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/workspace-docs\/local-services-pilot-runbook\.md/);
  assert.match(localDevelopment, /http:\/\/localhost:3000\/workspace-docs\/local-services-outreach-execution-pack\.md/);
  assert.match(localDevelopment, /Start 7-minute demo/);
  assert.match(localDevelopment, /Visa lead qualification/);
  assert.match(localDevelopment, /Each template card previews `Outcome`, `Approval`, `Evidence`, and `Deliverable`/);
  assert.match(localDevelopment, /inline detail panel with `Sample input`, `Approval policy`, `Evidence output`, and `CRM fields`/);
  assert.match(localDevelopment, /real `Payload preview`, `Surface path`, and `Copy payload` action/);
  assert.match(localDevelopment, /Open export drawer/);
  assert.match(localDevelopment, /`CRM payload drawer` and `Consultation handoff drawer`/);
  assert.match(localDevelopment, /\?playbook=<lane-id>/);
  assert.match(operatorGuide, /Seven-minute demo note:/);
  assert.match(operatorGuide, /docs\/evidence-and-trust\.md/);
  assert.match(operatorGuide, /replay, signing,\s+release evidence/);
  assert.match(operatorGuide, /Local-services demo note:/);
  assert.match(operatorGuide, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch/);
  assert.match(operatorGuide, /AI Dispatcher\s+for Local Services/);
  assert.match(operatorGuide, /Start 7-minute\s+demo/);
  assert.match(operatorGuide, /Open dispatch drawer/);
  assert.match(operatorGuide, /Open customer drawer/);
  assert.match(operatorGuide, /Open handoff drawer/);
  assert.match(operatorGuide, /Telegram intake prototype/);
  assert.match(operatorGuide, /Open intake evidence/);
  assert.match(operatorGuide, /Transcript \+ evidence/);
  assert.match(operatorGuide, /Saved intake evidence/);
  assert.match(operatorGuide, /Intake transcript \+ evidence link/);
  assert.match(operatorGuide, /Copy intake evidence/);
  assert.match(operatorGuide, /local_services_intake_evidence/);
  assert.match(operatorGuide, /transcript_evidence_link/);
  assert.match(operatorGuide, /Pilot readiness/);
  assert.match(operatorGuide, /One-page offer/);
  assert.match(operatorGuide, /90-second demo script/);
  assert.match(operatorGuide, /Launch checklist/);
  assert.match(operatorGuide, /Agent setup \/ training state/);
  assert.match(operatorGuide, /Open setup checklist/);
  assert.match(operatorGuide, /Business profile/);
  assert.match(operatorGuide, /Knowledge sources/);
  assert.match(operatorGuide, /Agent behavior/);
  assert.match(operatorGuide, /Test call\/message/);
  assert.match(operatorGuide, /Ready for test call\/message/);
  assert.match(operatorGuide, /Training cards/);
  assert.match(operatorGuide, /Copy setup brief/);
  assert.match(operatorGuide, /local_services_agent_setup_training/);
  assert.match(operatorGuide, /\?setup=7min/);
  assert.match(operatorGuide, /7-minute setup wizard/);
  assert.match(operatorGuide, /Setup path/);
  assert.match(operatorGuide, /Open day-one setup/);
  assert.match(operatorGuide, /setupStepCompletionByService/);
  assert.match(operatorGuide, /setupReadyByService/);
  assert.match(operatorGuide, /Setup progress/);
  assert.match(operatorGuide, /Saved setup state/);
  assert.match(operatorGuide, /Mark complete/);
  assert.match(operatorGuide, /Mark ready for pilot test/);
  assert.match(operatorGuide, /Reset setup progress/);
  assert.match(operatorGuide, /Test call\/message panel/);
  assert.match(operatorGuide, /Sample inbound/);
  assert.match(operatorGuide, /Expected extracted fields/);
  assert.match(operatorGuide, /Pass\/fail checklist/);
  assert.match(operatorGuide, /Mark check passed/);
  assert.match(operatorGuide, /Record test passed/);
  assert.match(operatorGuide, /Test call passed/);
  assert.match(operatorGuide, /Reset test call/);
  assert.match(operatorGuide, /testCallChecklistByService/);
  assert.match(operatorGuide, /testCallPassedByService/);
  assert.match(operatorGuide, /Outreach list filters/);
  assert.match(operatorGuide, /Column settings/);
  assert.match(operatorGuide, /Service filter/);
  assert.match(operatorGuide, /Status filter/);
  assert.match(operatorGuide, /Filtered candidates/);
  assert.match(operatorGuide, /Filtered outreach list/);
  assert.match(operatorGuide, /All services/);
  assert.match(operatorGuide, /All statuses/);
  assert.match(operatorGuide, /Clear filters/);
  assert.match(operatorGuide, /View only, no send/);
  assert.match(operatorGuide, /Pilot execution checklist/);
  assert.match(operatorGuide, /Pass test call\/message/);
  assert.match(operatorGuide, /Needs test call passed/);
  assert.match(operatorGuide, /Prepare first manual batch/);
  assert.match(operatorGuide, /Ready for first manual batch/);
  assert.match(operatorGuide, /Record ready drafts/);
  assert.match(operatorGuide, /Log manual contact/);
  assert.match(operatorGuide, /Book discovery call/);
  assert.match(operatorGuide, /Start metric capture/);
  assert.match(operatorGuide, /Founder\/operator validation/);
  assert.match(operatorGuide, /No autonomous send/);
  assert.match(operatorGuide, /Manual activity log/);
  assert.match(operatorGuide, /Last manual action/);
  assert.match(operatorGuide, /Copy activity log/);
  assert.match(operatorGuide, /local_services_manual_activity_log/);
  assert.match(operatorGuide, /No external side\s+effects/);
  assert.match(operatorGuide, /Open discovery prep/);
  assert.match(operatorGuide, /Discovery call\s+prep/);
  assert.match(operatorGuide, /Questions to ask/);
  assert.match(operatorGuide, /Pilot success criteria/);
  assert.match(operatorGuide, /Copy discovery call prep/);
  assert.match(operatorGuide, /local_services_discovery_call_prep/);
  assert.match(operatorGuide, /Open day-one setup/);
  assert.match(operatorGuide, /Day-one setup brief/);
  assert.match(operatorGuide, /Business profile lock/);
  assert.match(operatorGuide, /Setup tasks/);
  assert.match(operatorGuide, /Test call plan/);
  assert.match(operatorGuide, /Copy day-one setup brief/);
  assert.match(operatorGuide, /local_services_day_one_setup_brief/);
  assert.match(operatorGuide, /Open pilot runbook/);
  assert.match(operatorGuide, /Pilot metrics/);
  assert.match(operatorGuide, /Pilot outreach wizard/);
  assert.match(operatorGuide, /4-step/);
  assert.match(operatorGuide, /Audience from outreach list/);
  assert.match(operatorGuide, /Message\/test preview/);
  assert.match(operatorGuide, /Preview \/ Test message modal/);
  assert.match(operatorGuide, /Copy test message/);
  assert.match(operatorGuide, /Copy test message preview/);
  assert.match(operatorGuide, /Operator confirmation/);
  assert.match(operatorGuide, /Operator confirmation summary/);
  assert.match(operatorGuide, /Ready for manual outreach/);
  assert.match(operatorGuide, /Copy confirmation summary/);
  assert.match(operatorGuide, /Wizard progress/);
  assert.match(operatorGuide, /Record ready for manual outreach/);
  assert.match(operatorGuide, /Ready for manual outreach recorded/);
  assert.match(operatorGuide, /Ask AI about pilot/);
  assert.match(operatorGuide, /Suggested questions/);
  assert.match(operatorGuide, /Copy analyst brief/);
  assert.match(operatorGuide, /local_services_pilot_ai_analyst/);
  assert.match(operatorGuide, /no\s+external LLM call/i);
  assert.match(operatorGuide, /Pilot scorecard action/);
  assert.match(operatorGuide, /Record scorecard draft/);
  assert.match(operatorGuide, /Not contacted/);
  assert.match(operatorGuide, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(operatorGuide, /Draft ready/);
  assert.match(operatorGuide, /Contacted manually/);
  assert.match(operatorGuide, /Reply received/);
  assert.match(operatorGuide, /Rejected for now/);
  assert.match(operatorGuide, /First request outcome/);
  assert.match(operatorGuide, /Manual outcome state/);
  assert.match(operatorGuide, /firstRequestOutcomeByProspectKey/);
  assert.match(operatorGuide, /Needs follow-up/);
  assert.match(operatorGuide, /Booked manually/);
  assert.match(operatorGuide, /Outcome chain summary/);
  assert.match(operatorGuide, /Scorecard draft/);
  assert.match(operatorGuide, /Week-one review/);
  assert.match(operatorGuide, /Pilot funnel summary/);
  assert.match(operatorGuide, /All candidates/);
  assert.match(operatorGuide, /Next manual\s+batch/);
  assert.match(operatorGuide, /Pilot workspace export drawer/);
  assert.match(operatorGuide, /Copy pilot workspace export/);
  assert.match(operatorGuide, /Pilot metrics tracker/);
  assert.match(operatorGuide, /Copy pilot metrics tracker/);
  assert.match(operatorGuide, /Pilot daily log/);
  assert.match(operatorGuide, /Open daily log/);
  assert.match(operatorGuide, /Daily capture fields/);
  assert.match(operatorGuide, /Daily operating loop/);
  assert.match(operatorGuide, /Copy pilot daily log/);
  assert.match(operatorGuide, /local_services_pilot_daily_log/);
  assert.match(operatorGuide, /weekly scorecard sync/);
  assert.match(operatorGuide, /Day-one outcome capture\s+gate/);
  assert.match(operatorGuide, /Weekly scorecard sync gate/);
  assert.match(operatorGuide, /manual_weekly_scorecard_sync_gate/);
  assert.match(operatorGuide, /Open weekly sync checklist/);
  assert.match(operatorGuide, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(operatorGuide, /weeklyScorecardSyncReviewedByService/);
  assert.match(operatorGuide, /Evidence readiness/);
  assert.match(operatorGuide, /Pilot week-one\s+review/);
  assert.match(operatorGuide, /Open week-one review/);
  assert.match(operatorGuide, /Continue \/ stop decision/);
  assert.match(operatorGuide, /Copy week-one review/);
  assert.match(operatorGuide, /local_services_pilot_week_one_review/);
  assert.match(operatorGuide, /first\s+observed request result/);
  assert.match(operatorGuide, /Owner-ready summary/);
  assert.match(operatorGuide, /Decision readiness/);
  assert.match(operatorGuide, /Latest manual signal/);
  assert.match(operatorGuide, /day_one_recap_to_week_one_review/);
  assert.match(operatorGuide, /Week-one owner\s+decision state/);
  assert.match(operatorGuide, /Record continue/);
  assert.match(operatorGuide, /Record pause/);
  assert.match(operatorGuide, /Record stop/);
  assert.match(operatorGuide, /weekOneOwnerDecisionByProspectKey/);
  assert.match(operatorGuide, /week_one_owner_decision_to_evidence_pack/);
  assert.match(operatorGuide, /Pilot evidence pack/);
  assert.match(operatorGuide, /Open evidence pack/);
  assert.match(operatorGuide, /Week-two evidence pack/);
  assert.match(operatorGuide, /Copy evidence pack/);
  assert.match(operatorGuide, /local_services_pilot_evidence_pack/);
  assert.match(operatorGuide, /Week-one owner decision/);
  assert.match(operatorGuide, /paid-pilot readiness proof pack/);
  assert.match(operatorGuide, /Open offer doc/);
  assert.match(operatorGuide, /Open demo script/);
  assert.match(operatorGuide, /Open outreach list/);
  assert.match(operatorGuide, /Open outreach execution pack/);
  assert.match(operatorGuide, /Open pilot scorecard/);
  assert.match(operatorGuide, /Open founder execution log/);
  assert.match(operatorGuide, /\/workspace-docs\/local-services-founder-execution-log\.md/);
  assert.match(operatorGuide, /first-10-contact worksheet/);
  assert.match(operatorGuide, /First 10 contacts workspace/);
  assert.match(operatorGuide, /Pilot proof checklist/);
  assert.match(operatorGuide, /Pilot ops today/);
  assert.match(operatorGuide, /local_services_pilot_ops_today/);
  assert.match(operatorGuide, /Open ops confirmation/);
  assert.match(operatorGuide, /local_services_pilot_ops_confirmation/);
  assert.match(operatorGuide, /Pilot proof update rail/);
  assert.match(operatorGuide, /local_services_pilot_proof_update_rail/);
  assert.match(operatorGuide, /Stop \/ Continue decision gate/);
  assert.match(operatorGuide, /Open batch\s+review/);
  assert.match(operatorGuide, /First\s+contact batch\s+review drawer/);
  assert.match(operatorGuide, /Copy batch review/);
  assert.match(operatorGuide, /Copy\s+founder workspace/);
  assert.match(operatorGuide, /NEWO-style AI\s+employee\s+platform/);
  assert.match(operatorGuide, /Category pilot score/);
  assert.match(operatorGuide, /Leading category/);
  assert.match(operatorGuide, /No category expansion without proof/);
  assert.match(operatorGuide, /Leading category\s+action layer/);
  assert.match(operatorGuide, /Next manual\s+batch/);
  assert.match(operatorGuide, /Discovery\s+questions/);
  assert.match(operatorGuide, /Pilot setup\s+checklist/);
  assert.match(operatorGuide, /Integration\s+hold/);
  assert.match(operatorGuide, /Focus leading\s+category/);
  assert.match(operatorGuide, /Pilot setup\s+readiness/);
  assert.match(operatorGuide, /Paid pilot gate/);
  assert.match(operatorGuide, /Ready for first paid pilot/);
  assert.match(operatorGuide, /Not ready for paid pilot/);
  assert.match(operatorGuide, /Week-one owner decision/);
  assert.match(operatorGuide, /only `Continue` lets the\s+operator prepare a `Paid pilot proposal`/);
  assert.match(operatorGuide, /Readiness action\s+plan/);
  assert.match(operatorGuide, /Continue\s+setup\/test path/);
  assert.match(operatorGuide, /Copy readiness action plan/);
  assert.match(operatorGuide, /Open proof drawer/);
  assert.match(operatorGuide, /Readiness proof drawer/);
  assert.match(operatorGuide, /Copy readiness proof/);
  assert.match(operatorGuide, /Open proposal preview/);
  assert.match(operatorGuide, /Paid pilot\s+proposal preview/);
  assert.match(operatorGuide, /Copy proposal preview/);
  assert.match(operatorGuide, /Open approval handoff/);
  assert.match(operatorGuide, /Proposal\s+approval handoff/);
  assert.match(operatorGuide, /Copy approval handoff/);
  assert.match(operatorGuide, /Proposal\s+approval state/);
  assert.match(operatorGuide, /Approve proposal handoff/);
  assert.match(operatorGuide, /proposalApprovalByService/);
  assert.match(operatorGuide, /Open kickoff gate/);
  assert.match(operatorGuide, /Pilot kickoff gate/);
  assert.match(operatorGuide, /Copy kickoff gate/);
  assert.match(operatorGuide, /Kickoff\s+decision state/);
  assert.match(operatorGuide, /Mark kickoff ready/);
  assert.match(operatorGuide, /kickoffDecisionByService/);
  assert.match(operatorGuide, /Open run sheet/);
  assert.match(operatorGuide, /Day-one operator run sheet/);
  assert.match(operatorGuide, /Copy run sheet/);
  assert.match(operatorGuide, /local_services_day_one_operator_run_sheet/);
  assert.match(operatorGuide, /manual_day_one_operator_run_sheet/);
  assert.match(operatorGuide, /day_one_run_sheet_outcome_capture/);
  assert.match(operatorGuide, /Open day-one recap/);
  assert.match(operatorGuide, /Day-one recap/);
  assert.match(operatorGuide, /Copy day-one recap/);
  assert.match(operatorGuide, /local_services_day_one_recap/);
  assert.match(operatorGuide, /day_one_recap_to_week_one_review/);
  assert.match(operatorGuide, /docs\/local-services-pilot-runbook\.md/);
  assert.match(operatorGuide, /docs\/local-services-outreach-execution-pack\.md/);
  assert.match(operatorGuide, /Open recording checklist/);
  assert.match(operatorGuide, /90-second walkthrough/);
  assert.match(operatorGuide, /\?recording=90s/);
  assert.match(operatorGuide, /Recording path/);
  assert.match(operatorGuide, /14-day operating\s+sequence/);
  assert.match(operatorGuide, /Each card previews `Outcome`, `Approval`, `Evidence`,/);
  assert.match(operatorGuide, /inline detail panel with `Sample input`, `Approval\s+policy`, `Evidence output`, and `CRM fields`/);
  assert.match(operatorGuide, /`Payload preview`, `Surface path`, and `Copy payload` action/);
  assert.match(operatorGuide, /Open export drawer/);
  assert.match(operatorGuide, /CRM payload\s+drawer/);
  assert.match(operatorGuide, /Consultation handoff drawer/);
  assert.match(operatorGuide, /\/app\?playbook=<lane-id>/);
  assert.match(simplificationPlan, /\/app\?demo=visa-intake/);
  assert.match(simplificationPlan, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch/);
  assert.match(simplificationPlan, /phone-first and operator-approved/);
  assert.match(simplificationPlan, /Open dispatch drawer/);
  assert.match(simplificationPlan, /Open customer drawer/);
  assert.match(simplificationPlan, /Open handoff drawer/);
  assert.match(simplificationPlan, /Telegram intake prototype/);
  assert.match(simplificationPlan, /Open intake evidence/);
  assert.match(simplificationPlan, /Transcript \+ evidence/);
  assert.match(simplificationPlan, /Saved intake evidence/);
  assert.match(simplificationPlan, /Intake transcript \+ evidence link/);
  assert.match(simplificationPlan, /Copy intake evidence/);
  assert.match(simplificationPlan, /local_services_intake_evidence/);
  assert.match(simplificationPlan, /transcript_evidence_link/);
  assert.match(simplificationPlan, /Pilot readiness/);
  assert.match(simplificationPlan, /One-page offer/);
  assert.match(simplificationPlan, /90-second demo script/);
  assert.match(simplificationPlan, /Launch checklist/);
  assert.match(simplificationPlan, /Agent setup \/ training state/);
  assert.match(simplificationPlan, /Open setup checklist/);
  assert.match(simplificationPlan, /Business profile/);
  assert.match(simplificationPlan, /Knowledge sources/);
  assert.match(simplificationPlan, /Agent behavior/);
  assert.match(simplificationPlan, /Test call\/message/);
  assert.match(simplificationPlan, /Ready for test call\/message/);
  assert.match(simplificationPlan, /Training cards/);
  assert.match(simplificationPlan, /Copy setup brief/);
  assert.match(simplificationPlan, /local_services_agent_setup_training/);
  assert.match(simplificationPlan, /\?setup=7min/);
  assert.match(simplificationPlan, /7-minute setup wizard/);
  assert.match(simplificationPlan, /Setup path/);
  assert.match(simplificationPlan, /Open day-one setup/);
  assert.match(simplificationPlan, /setupStepCompletionByService/);
  assert.match(simplificationPlan, /setupReadyByService/);
  assert.match(simplificationPlan, /Setup progress/);
  assert.match(simplificationPlan, /Saved setup state/);
  assert.match(simplificationPlan, /Mark complete/);
  assert.match(simplificationPlan, /Mark ready for pilot test/);
  assert.match(simplificationPlan, /Ready for pilot test/);
  assert.match(simplificationPlan, /Test call\/message panel/);
  assert.match(simplificationPlan, /Sample inbound/);
  assert.match(simplificationPlan, /Expected extracted fields/);
  assert.match(simplificationPlan, /Pass\/fail checklist/);
  assert.match(simplificationPlan, /Mark check passed/);
  assert.match(simplificationPlan, /Record test passed/);
  assert.match(simplificationPlan, /Test call passed/);
  assert.match(simplificationPlan, /Reset test call/);
  assert.match(simplificationPlan, /testCallChecklistByService/);
  assert.match(simplificationPlan, /testCallPassedByService/);
  assert.match(simplificationPlan, /Outreach list filters/);
  assert.match(simplificationPlan, /Column settings/);
  assert.match(simplificationPlan, /Service filter/);
  assert.match(simplificationPlan, /Status filter/);
  assert.match(simplificationPlan, /Filtered candidates/);
  assert.match(simplificationPlan, /Filtered outreach list/);
  assert.match(simplificationPlan, /All services/);
  assert.match(simplificationPlan, /All statuses/);
  assert.match(simplificationPlan, /Clear filters/);
  assert.match(simplificationPlan, /View only, no send/);
  assert.match(simplificationPlan, /Pilot execution checklist/);
  assert.match(simplificationPlan, /Pass test call\/message/);
  assert.match(simplificationPlan, /Needs test call passed/);
  assert.match(simplificationPlan, /Prepare first manual\s+batch/);
  assert.match(simplificationPlan, /Ready for first manual batch/);
  assert.match(simplificationPlan, /Record ready drafts/);
  assert.match(simplificationPlan, /Log manual contact/);
  assert.match(simplificationPlan, /Book discovery call/);
  assert.match(simplificationPlan, /Start metric capture/);
  assert.match(simplificationPlan, /Founder\/operator validation/);
  assert.match(simplificationPlan, /No autonomous send/);
  assert.match(simplificationPlan, /Manual activity log/);
  assert.match(simplificationPlan, /Last manual action/);
  assert.match(simplificationPlan, /Copy activity log/);
  assert.match(simplificationPlan, /local_services_manual_activity_log/);
  assert.match(simplificationPlan, /No external side\s+effects/);
  assert.match(simplificationPlan, /Open discovery prep/);
  assert.match(simplificationPlan, /Discovery call\s+prep/);
  assert.match(simplificationPlan, /Questions to ask/);
  assert.match(simplificationPlan, /Pilot success criteria/);
  assert.match(simplificationPlan, /Copy discovery call prep/);
  assert.match(simplificationPlan, /local_services_discovery_call_prep/);
  assert.match(simplificationPlan, /Open day-one setup/);
  assert.match(simplificationPlan, /Day-one setup brief/);
  assert.match(simplificationPlan, /Business profile lock/);
  assert.match(simplificationPlan, /Setup tasks/);
  assert.match(simplificationPlan, /Test call plan/);
  assert.match(simplificationPlan, /Copy day-one setup brief/);
  assert.match(simplificationPlan, /local_services_day_one_setup_brief/);
  assert.match(simplificationPlan, /Open pilot runbook/);
  assert.match(simplificationPlan, /Pilot metrics/);
  assert.match(simplificationPlan, /Pilot outreach wizard/);
  assert.match(simplificationPlan, /4-step/);
  assert.match(simplificationPlan, /Audience from outreach list/);
  assert.match(simplificationPlan, /Message\/test preview/);
  assert.match(simplificationPlan, /Preview \/ Test message modal/);
  assert.match(simplificationPlan, /Copy test message/);
  assert.match(simplificationPlan, /Copy test message preview/);
  assert.match(simplificationPlan, /Operator confirmation/);
  assert.match(simplificationPlan, /Operator confirmation summary/);
  assert.match(simplificationPlan, /Ready for manual outreach/);
  assert.match(simplificationPlan, /Copy confirmation summary/);
  assert.match(simplificationPlan, /Wizard progress/);
  assert.match(simplificationPlan, /Record ready for manual outreach/);
  assert.match(simplificationPlan, /Ready for manual outreach recorded/);
  assert.match(simplificationPlan, /Ask AI about pilot/);
  assert.match(simplificationPlan, /Suggested questions/);
  assert.match(simplificationPlan, /Copy analyst brief/);
  assert.match(simplificationPlan, /local_services_pilot_ai_analyst/);
  assert.match(simplificationPlan, /external LLM dependency/);
  assert.match(simplificationPlan, /Pilot scorecard action/);
  assert.match(simplificationPlan, /Record scorecard draft/);
  assert.match(simplificationPlan, /Not contacted/);
  assert.match(simplificationPlan, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(simplificationPlan, /Draft ready/);
  assert.match(simplificationPlan, /Contacted manually/);
  assert.match(simplificationPlan, /Reply received/);
  assert.match(simplificationPlan, /Rejected for now/);
  assert.match(simplificationPlan, /First request outcome/);
  assert.match(simplificationPlan, /Manual outcome state/);
  assert.match(simplificationPlan, /firstRequestOutcomeByProspectKey/);
  assert.match(simplificationPlan, /Needs follow-up/);
  assert.match(simplificationPlan, /Booked manually/);
  assert.match(simplificationPlan, /Outcome chain summary/);
  assert.match(simplificationPlan, /Scorecard draft/);
  assert.match(simplificationPlan, /Week-one review/);
  assert.match(simplificationPlan, /Pilot funnel summary/);
  assert.match(simplificationPlan, /All candidates/);
  assert.match(simplificationPlan, /Next manual\s+batch/);
  assert.match(simplificationPlan, /Open pilot export/);
  assert.match(simplificationPlan, /Pilot workspace export\s+drawer/);
  assert.match(simplificationPlan, /Pilot metrics tracker/);
  assert.match(simplificationPlan, /Pilot daily\s+log/);
  assert.match(simplificationPlan, /Open daily log/);
  assert.match(simplificationPlan, /Daily capture fields/);
  assert.match(simplificationPlan, /Daily operating loop/);
  assert.match(simplificationPlan, /Copy pilot daily log/);
  assert.match(simplificationPlan, /local_services_pilot_daily_log/);
  assert.match(simplificationPlan, /weekly scorecard sync/);
  assert.match(simplificationPlan, /Day-one outcome capture gate/);
  assert.match(simplificationPlan, /Weekly scorecard sync gate/);
  assert.match(simplificationPlan, /manual_weekly_scorecard_sync_gate/);
  assert.match(simplificationPlan, /Open weekly sync checklist/);
  assert.match(simplificationPlan, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(simplificationPlan, /weeklyScorecardSyncReviewedByService/);
  assert.match(simplificationPlan, /Pilot week-one\s+review/);
  assert.match(simplificationPlan, /Open week-one review/);
  assert.match(simplificationPlan, /Continue \/ stop decision/);
  assert.match(simplificationPlan, /Copy week-one review/);
  assert.match(simplificationPlan, /local_services_pilot_week_one_review/);
  assert.match(simplificationPlan, /reviewed week-one decision/);
  assert.match(simplificationPlan, /Owner-ready summary/);
  assert.match(simplificationPlan, /Decision readiness/);
  assert.match(simplificationPlan, /Latest manual signal/);
  assert.match(simplificationPlan, /day_one_recap_to_week_one_review/);
  assert.match(simplificationPlan, /Week-one owner\s+decision state/);
  assert.match(simplificationPlan, /Record continue/);
  assert.match(simplificationPlan, /Record pause/);
  assert.match(simplificationPlan, /Record stop/);
  assert.match(simplificationPlan, /weekOneOwnerDecisionByProspectKey/);
  assert.match(simplificationPlan, /week_one_owner_decision_to_evidence_pack/);
  assert.match(simplificationPlan, /Pilot evidence pack/);
  assert.match(simplificationPlan, /Open evidence pack/);
  assert.match(simplificationPlan, /Week-two evidence pack/);
  assert.match(simplificationPlan, /Copy evidence pack/);
  assert.match(simplificationPlan, /local_services_pilot_evidence_pack/);
  assert.match(simplificationPlan, /Week-one owner decision/);
  assert.match(simplificationPlan, /paid-pilot\s+readiness includes the first\s+observed request result/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-pilot-offer\.md/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-demo-script\.md/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-demo-recording-checklist\.md/);
  assert.match(simplificationPlan, /\?recording=90s/);
  assert.match(simplificationPlan, /90-second recording mode/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-outreach-list\.md/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-pilot-scorecard\.md/);
  assert.match(simplificationPlan, /Open founder execution log/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-founder-execution-log\.md/);
  assert.match(simplificationPlan, /first-10-contact worksheet/);
  assert.match(simplificationPlan, /First 10 contacts\s+workspace/);
  assert.match(simplificationPlan, /Pilot proof checklist/);
  assert.match(simplificationPlan, /Pilot ops today/);
  assert.match(simplificationPlan, /local_services_pilot_ops_today/);
  assert.match(simplificationPlan, /Open ops confirmation/);
  assert.match(simplificationPlan, /local_services_pilot_ops_confirmation/);
  assert.match(simplificationPlan, /Pilot proof update rail/);
  assert.match(simplificationPlan, /local_services_pilot_proof_update_rail/);
  assert.match(simplificationPlan, /Stop \/ Continue decision gate/);
  assert.match(simplificationPlan, /Open batch review/);
  assert.match(simplificationPlan, /First contact batch\s+review drawer/);
  assert.match(simplificationPlan, /Copy batch review/);
  assert.match(simplificationPlan, /Copy founder workspace/);
  assert.match(simplificationPlan, /NEWO-style AI\s+employee\s+platform/);
  assert.match(simplificationPlan, /Category pilot score/);
  assert.match(simplificationPlan, /Leading category/);
  assert.match(simplificationPlan, /No category expansion without proof/);
  assert.match(simplificationPlan, /Leading category\s+action layer/);
  assert.match(simplificationPlan, /Next manual\s+batch/);
  assert.match(simplificationPlan, /Discovery\s+questions/);
  assert.match(simplificationPlan, /Pilot setup\s+checklist/);
  assert.match(simplificationPlan, /Integration\s+hold/);
  assert.match(simplificationPlan, /Focus leading\s+category/);
  assert.match(simplificationPlan, /Pilot setup readiness/);
  assert.match(simplificationPlan, /Paid pilot gate/);
  assert.match(simplificationPlan, /Ready for first paid pilot/);
  assert.match(simplificationPlan, /Not ready for paid pilot/);
  assert.match(simplificationPlan, /Week-one owner decision/);
  assert.match(simplificationPlan, /Only\s+`Continue` can move the lane toward `Paid pilot proposal`/);
  assert.match(simplificationPlan, /Readiness action\s+plan/);
  assert.match(simplificationPlan, /Continue\s+setup\/test path/);
  assert.match(simplificationPlan, /Copy readiness action plan/);
  assert.match(simplificationPlan, /Open proof drawer/);
  assert.match(simplificationPlan, /Readiness proof\s+drawer/);
  assert.match(simplificationPlan, /Copy readiness proof/);
  assert.match(simplificationPlan, /local_services_readiness_proof_drawer/);
  assert.match(simplificationPlan, /Open proposal\s+preview/);
  assert.match(simplificationPlan, /Paid pilot proposal preview/);
  assert.match(simplificationPlan, /Copy proposal preview/);
  assert.match(simplificationPlan, /local_services_paid_pilot_proposal_preview/);
  assert.match(simplificationPlan, /Open approval handoff/);
  assert.match(simplificationPlan, /Proposal approval handoff/);
  assert.match(simplificationPlan, /Copy approval handoff/);
  assert.match(simplificationPlan, /local_services_proposal_approval_handoff/);
  assert.match(simplificationPlan, /Proposal\s+approval state/);
  assert.match(simplificationPlan, /Approve proposal handoff/);
  assert.match(simplificationPlan, /proposalApprovalByService/);
  assert.match(simplificationPlan, /Open kickoff gate/);
  assert.match(simplificationPlan, /Pilot kickoff gate/);
  assert.match(simplificationPlan, /Copy kickoff gate/);
  assert.match(simplificationPlan, /local_services_pilot_kickoff_gate/);
  assert.match(simplificationPlan, /Kickoff\s+decision state/);
  assert.match(simplificationPlan, /Mark kickoff ready/);
  assert.match(simplificationPlan, /kickoffDecisionByService/);
  assert.match(simplificationPlan, /Open run sheet/);
  assert.match(simplificationPlan, /Day-one operator run sheet/);
  assert.match(simplificationPlan, /Copy run sheet/);
  assert.match(simplificationPlan, /local_services_day_one_operator_run_sheet/);
  assert.match(simplificationPlan, /manual_day_one_operator_run_sheet/);
  assert.match(simplificationPlan, /day_one_run_sheet_outcome_capture/);
  assert.match(simplificationPlan, /Open day-one recap/);
  assert.match(simplificationPlan, /Day-one recap/);
  assert.match(simplificationPlan, /Copy day-one recap/);
  assert.match(simplificationPlan, /local_services_day_one_recap/);
  assert.match(simplificationPlan, /day_one_recap_to_week_one_review/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-pilot-runbook\.md/);
  assert.match(simplificationPlan, /\/workspace-docs\/local-services-outreach-execution-pack\.md/);
  assert.match(simplificationPlan, /Open outreach execution pack/);
  assert.match(simplificationPlan, /productized launch cards/);
  assert.match(simplificationPlan, /Each card should preview `Outcome`, `Approval`, `Evidence`,/);
  assert.match(simplificationPlan, /The selected lane should\s+also expose `Sample input`, `Approval policy`, `Evidence output`, and `CRM\s+fields` inline/);
  assert.match(simplificationPlan, /repo-owned `Payload preview`, the canonical `Surface path`, and a\s+`Copy payload` action/);
  assert.match(simplificationPlan, /Open export drawer/);
  assert.match(simplificationPlan, /CRM payload drawer/);
  assert.match(simplificationPlan, /Consultation handoff drawer/);
  assert.match(simplificationPlan, /\?playbook=/);
  assert.match(simplificationPlan, /docs\/evidence-and-trust\.md/);
  assert.match(simplificationPlan, /support document for proof depth/);
  assert.match(gettingStarted, /Start 7-minute demo/);
  assert.match(gettingStarted, /Local Services Expansion Path/);
  assert.match(gettingStarted, /Local services demo/);
  assert.match(gettingStarted, /AC repair dispatch/);
  assert.match(gettingStarted, /Plumbing emergency/);
  assert.match(gettingStarted, /Cleaning quote and booking/);
  assert.match(gettingStarted, /Measurement visit booking/);
  assert.match(gettingStarted, /Construction-material commerce is intentionally not in this first path/);
  assert.match(gettingStarted, /Dispatch payload preview/);
  assert.match(gettingStarted, /Open dispatch drawer/);
  assert.match(gettingStarted, /Open customer drawer/);
  assert.match(gettingStarted, /Open handoff drawer/);
  assert.match(gettingStarted, /Telegram intake prototype/);
  assert.match(gettingStarted, /Open intake evidence/);
  assert.match(gettingStarted, /Transcript \+ evidence/);
  assert.match(gettingStarted, /Saved intake evidence/);
  assert.match(gettingStarted, /Intake transcript \+ evidence link/);
  assert.match(gettingStarted, /Copy intake evidence/);
  assert.match(gettingStarted, /local_services_intake_evidence/);
  assert.match(gettingStarted, /transcript_evidence_link/);
  assert.match(gettingStarted, /Pilot readiness/);
  assert.match(gettingStarted, /One-page offer/);
  assert.match(gettingStarted, /90-second demo script/);
  assert.match(gettingStarted, /Launch checklist/);
  assert.match(gettingStarted, /Agent setup \/ training state/);
  assert.match(gettingStarted, /Open setup checklist/);
  assert.match(gettingStarted, /Business profile/);
  assert.match(gettingStarted, /Knowledge sources/);
  assert.match(gettingStarted, /Agent behavior/);
  assert.match(gettingStarted, /Test call\/message/);
  assert.match(gettingStarted, /Ready for test call\/message/);
  assert.match(gettingStarted, /Training cards/);
  assert.match(gettingStarted, /Copy setup brief/);
  assert.match(gettingStarted, /local_services_agent_setup_training/);
  assert.match(gettingStarted, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&setup=7min/);
  assert.match(gettingStarted, /7-minute setup wizard/);
  assert.match(gettingStarted, /Setup path/);
  assert.match(gettingStarted, /Open day-one setup/);
  assert.match(gettingStarted, /setupStepCompletionByService/);
  assert.match(gettingStarted, /setupReadyByService/);
  assert.match(gettingStarted, /Mark ready for pilot test/);
  assert.match(gettingStarted, /Ready for pilot test/);
  assert.match(gettingStarted, /Test call\/message panel/);
  assert.match(gettingStarted, /Sample inbound/);
  assert.match(gettingStarted, /Expected extracted fields/);
  assert.match(gettingStarted, /Pass\/fail checklist/);
  assert.match(gettingStarted, /Mark check passed/);
  assert.match(gettingStarted, /Record test passed/);
  assert.match(gettingStarted, /Test call passed/);
  assert.match(gettingStarted, /Reset test call/);
  assert.match(gettingStarted, /testCallChecklistByService/);
  assert.match(gettingStarted, /testCallPassedByService/);
  assert.match(gettingStarted, /Outreach list filters/);
  assert.match(gettingStarted, /Column settings/);
  assert.match(gettingStarted, /Service filter/);
  assert.match(gettingStarted, /Status filter/);
  assert.match(gettingStarted, /Filtered candidates/);
  assert.match(gettingStarted, /Filtered outreach list/);
  assert.match(gettingStarted, /All services/);
  assert.match(gettingStarted, /All statuses/);
  assert.match(gettingStarted, /Clear filters/);
  assert.match(gettingStarted, /View only, no send/);
  assert.match(gettingStarted, /Pilot execution checklist/);
  assert.match(gettingStarted, /Pass test call\/message/);
  assert.match(gettingStarted, /Needs test call passed/);
  assert.match(gettingStarted, /Prepare first manual\s+batch/);
  assert.match(gettingStarted, /Ready for first manual batch/);
  assert.match(gettingStarted, /Record ready drafts/);
  assert.match(gettingStarted, /Log manual contact/);
  assert.match(gettingStarted, /Book discovery call/);
  assert.match(gettingStarted, /Start metric capture/);
  assert.match(gettingStarted, /Founder\/operator validation/);
  assert.match(gettingStarted, /No autonomous send/);
  assert.match(gettingStarted, /Manual activity log/);
  assert.match(gettingStarted, /Last manual action/);
  assert.match(gettingStarted, /Copy activity log/);
  assert.match(gettingStarted, /local_services_manual_activity_log/);
  assert.match(gettingStarted, /No external side\s+effects/);
  assert.match(gettingStarted, /Open discovery prep/);
  assert.match(gettingStarted, /Discovery call\s+prep/);
  assert.match(gettingStarted, /Questions to ask/);
  assert.match(gettingStarted, /Pilot success criteria/);
  assert.match(gettingStarted, /Copy discovery call prep/);
  assert.match(gettingStarted, /local_services_discovery_call_prep/);
  assert.match(gettingStarted, /Open day-one setup/);
  assert.match(gettingStarted, /Day-one setup brief/);
  assert.match(gettingStarted, /Business profile lock/);
  assert.match(gettingStarted, /Setup tasks/);
  assert.match(gettingStarted, /Test call plan/);
  assert.match(gettingStarted, /Copy day-one setup brief/);
  assert.match(gettingStarted, /local_services_day_one_setup_brief/);
  assert.match(gettingStarted, /Open pilot runbook/);
  assert.match(gettingStarted, /Pilot metrics/);
  assert.match(gettingStarted, /Pilot outreach wizard/);
  assert.match(gettingStarted, /4-step/);
  assert.match(gettingStarted, /Audience from outreach list/);
  assert.match(gettingStarted, /Message\/test preview/);
  assert.match(gettingStarted, /Preview \/ Test message modal/);
  assert.match(gettingStarted, /Copy test message/);
  assert.match(gettingStarted, /Copy test message preview/);
  assert.match(gettingStarted, /Operator confirmation/);
  assert.match(gettingStarted, /Operator confirmation summary/);
  assert.match(gettingStarted, /Ready for manual outreach/);
  assert.match(gettingStarted, /Copy confirmation summary/);
  assert.match(gettingStarted, /Wizard progress/);
  assert.match(gettingStarted, /Record ready for manual outreach/);
  assert.match(gettingStarted, /Ready for manual outreach recorded/);
  assert.match(gettingStarted, /Ask AI about pilot/);
  assert.match(gettingStarted, /Suggested questions/);
  assert.match(gettingStarted, /Copy analyst brief/);
  assert.match(gettingStarted, /local_services_pilot_ai_analyst/);
  assert.match(gettingStarted, /no\s+external LLM call/i);
  assert.match(gettingStarted, /Pilot scorecard action/);
  assert.match(gettingStarted, /AC MASTER/);
  assert.match(gettingStarted, /Santexniki\.uz/);
  assert.match(gettingStarted, /Service-Pro/);
  assert.match(gettingStarted, /Record scorecard draft/);
  assert.match(gettingStarted, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(gettingStarted, /Draft ready/);
  assert.match(gettingStarted, /Contacted manually/);
  assert.match(gettingStarted, /Reply received/);
  assert.match(gettingStarted, /Rejected for now/);
  assert.match(gettingStarted, /First request outcome/);
  assert.match(gettingStarted, /firstRequestOutcomeByProspectKey/);
  assert.match(gettingStarted, /Needs follow-up/);
  assert.match(gettingStarted, /Booked manually/);
  assert.match(gettingStarted, /Outcome chain summary/);
  assert.match(gettingStarted, /Scorecard draft/);
  assert.match(gettingStarted, /Week-one review/);
  assert.match(gettingStarted, /Pilot funnel summary/);
  assert.match(gettingStarted, /All candidates/);
  assert.match(gettingStarted, /Next manual\s+batch/);
  assert.match(gettingStarted, /Open pilot export/);
  assert.match(gettingStarted, /Copy pilot workspace export/);
  assert.match(gettingStarted, /Open metrics tracker/);
  assert.match(gettingStarted, /Copy pilot metrics tracker/);
  assert.match(gettingStarted, /Pilot daily log/);
  assert.match(gettingStarted, /Open daily log/);
  assert.match(gettingStarted, /Daily capture fields/);
  assert.match(gettingStarted, /Daily operating loop/);
  assert.match(gettingStarted, /Copy pilot daily log/);
  assert.match(gettingStarted, /local_services_pilot_daily_log/);
  assert.match(gettingStarted, /weekly scorecard sync/);
  assert.match(gettingStarted, /Day-one outcome capture gate/);
  assert.match(gettingStarted, /Weekly scorecard sync gate/);
  assert.match(gettingStarted, /manual_weekly_scorecard_sync_gate/);
  assert.match(gettingStarted, /Open weekly sync checklist/);
  assert.match(gettingStarted, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(gettingStarted, /weeklyScorecardSyncReviewedByService/);
  assert.match(gettingStarted, /Pilot week-one review/);
  assert.match(gettingStarted, /Open week-one review/);
  assert.match(gettingStarted, /Continue \/ stop decision/);
  assert.match(gettingStarted, /Copy week-one review/);
  assert.match(gettingStarted, /local_services_pilot_week_one_review/);
  assert.match(gettingStarted, /continue, pause, or stop/);
  assert.match(gettingStarted, /Owner-ready summary/);
  assert.match(gettingStarted, /Decision readiness/);
  assert.match(gettingStarted, /Latest manual signal/);
  assert.match(gettingStarted, /day_one_recap_to_week_one_review/);
  assert.match(gettingStarted, /Week-one owner\s+decision state/);
  assert.match(gettingStarted, /Record continue/);
  assert.match(gettingStarted, /Record pause/);
  assert.match(gettingStarted, /Record stop/);
  assert.match(gettingStarted, /weekOneOwnerDecisionByProspectKey/);
  assert.match(gettingStarted, /week_one_owner_decision_to_evidence_pack/);
  assert.match(gettingStarted, /Pilot evidence pack/);
  assert.match(gettingStarted, /Open evidence pack/);
  assert.match(gettingStarted, /Week-two evidence pack/);
  assert.match(gettingStarted, /Copy evidence pack/);
  assert.match(gettingStarted, /local_services_pilot_evidence_pack/);
  assert.match(gettingStarted, /Week-one owner decision/);
  assert.match(gettingStarted, /paid-pilot readiness proof pack/);
  assert.match(gettingStarted, /Open founder execution log/);
  assert.match(gettingStarted, /\/workspace-docs\/local-services-founder-execution-log\.md/);
  assert.match(gettingStarted, /replies, demo bookings, discovery-call pain/);
  assert.match(gettingStarted, /First 10 contacts workspace/);
  assert.match(gettingStarted, /Pilot proof checklist/);
  assert.match(gettingStarted, /Pilot ops today/);
  assert.match(gettingStarted, /local_services_pilot_ops_today/);
  assert.match(gettingStarted, /Open ops confirmation/);
  assert.match(gettingStarted, /local_services_pilot_ops_confirmation/);
  assert.match(gettingStarted, /Pilot proof update rail/);
  assert.match(gettingStarted, /local_services_pilot_proof_update_rail/);
  assert.match(gettingStarted, /Stop \/ Continue decision gate/);
  assert.match(gettingStarted, /Open batch review/);
  assert.match(gettingStarted, /First\s+contact batch\s+review drawer/);
  assert.match(gettingStarted, /Copy batch review/);
  assert.match(gettingStarted, /Copy founder workspace/);
  assert.match(gettingStarted, /NEWO-style AI\s+employee\s+platform/);
  assert.match(gettingStarted, /Category pilot score/);
  assert.match(gettingStarted, /Leading category/);
  assert.match(gettingStarted, /No category expansion without proof/);
  assert.match(gettingStarted, /Leading category\s+action layer/);
  assert.match(gettingStarted, /Next manual\s+batch/);
  assert.match(gettingStarted, /Discovery\s+questions/);
  assert.match(gettingStarted, /Pilot setup\s+checklist/);
  assert.match(gettingStarted, /Integration\s+hold/);
  assert.match(gettingStarted, /Focus leading\s+category/);
  assert.match(gettingStarted, /Pilot setup readiness/);
  assert.match(gettingStarted, /Paid pilot gate/);
  assert.match(gettingStarted, /Ready for first paid pilot/);
  assert.match(gettingStarted, /Not ready for paid pilot/);
  assert.match(gettingStarted, /Week-one owner decision/);
  assert.match(gettingStarted, /`Continue` is required before `Paid pilot proposal`/);
  assert.match(gettingStarted, /Readiness action\s+plan/);
  assert.match(gettingStarted, /Continue\s+setup\/test path/);
  assert.match(gettingStarted, /Copy readiness action plan/);
  assert.match(gettingStarted, /Open proof drawer/);
  assert.match(gettingStarted, /Readiness proof\s+drawer/);
  assert.match(gettingStarted, /Copy readiness proof/);
  assert.match(gettingStarted, /local_services_readiness_proof_drawer/);
  assert.match(gettingStarted, /Open proposal preview/);
  assert.match(gettingStarted, /Paid pilot\s+proposal preview/);
  assert.match(gettingStarted, /Copy proposal preview/);
  assert.match(gettingStarted, /local_services_paid_pilot_proposal_preview/);
  assert.match(gettingStarted, /Open approval handoff/);
  assert.match(gettingStarted, /Proposal approval handoff/);
  assert.match(gettingStarted, /Copy approval handoff/);
  assert.match(gettingStarted, /local_services_proposal_approval_handoff/);
  assert.match(gettingStarted, /Proposal\s+approval state/);
  assert.match(gettingStarted, /Approve proposal handoff/);
  assert.match(gettingStarted, /proposalApprovalByService/);
  assert.match(gettingStarted, /Open kickoff gate/);
  assert.match(gettingStarted, /Pilot kickoff gate/);
  assert.match(gettingStarted, /Copy kickoff gate/);
  assert.match(gettingStarted, /local_services_pilot_kickoff_gate/);
  assert.match(gettingStarted, /Kickoff\s+decision state/);
  assert.match(gettingStarted, /Mark kickoff ready/);
  assert.match(gettingStarted, /kickoffDecisionByService/);
  assert.match(gettingStarted, /Open run sheet/);
  assert.match(gettingStarted, /Day-one operator run sheet/);
  assert.match(gettingStarted, /Copy run sheet/);
  assert.match(gettingStarted, /local_services_day_one_operator_run_sheet/);
  assert.match(gettingStarted, /manual_day_one_operator_run_sheet/);
  assert.match(gettingStarted, /day_one_run_sheet_outcome_capture/);
  assert.match(gettingStarted, /Open day-one recap/);
  assert.match(gettingStarted, /Day-one recap/);
  assert.match(gettingStarted, /Copy day-one recap/);
  assert.match(gettingStarted, /local_services_day_one_recap/);
  assert.match(gettingStarted, /day_one_recap_to_week_one_review/);
  assert.match(gettingStarted, /Open offer doc/);
  assert.match(gettingStarted, /Open demo script/);
  assert.match(gettingStarted, /Open outreach list/);
  assert.match(gettingStarted, /Open outreach execution pack/);
  assert.match(gettingStarted, /Open pilot scorecard/);
  assert.match(gettingStarted, /docs\/local-services-pilot-runbook\.md/);
  assert.match(gettingStarted, /docs\/local-services-outreach-execution-pack\.md/);
  assert.match(gettingStarted, /Open recording checklist/);
  assert.match(gettingStarted, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&recording=90s/);
  assert.match(gettingStarted, /Recording path/);
  assert.match(gettingStarted, /Playbook templates/);
  assert.match(gettingStarted, /Each card previews `Outcome`, `Approval`, `Evidence`, and `Deliverable`/);
  assert.match(gettingStarted, /Selecting a card opens the inline detail panel with `Sample input`, `Approval policy`,\s+`Evidence output`, and `CRM fields`/);
  assert.match(gettingStarted, /`Payload preview`, `Surface path`, and `Copy payload`/);
  assert.match(gettingStarted, /Open export drawer/);
  assert.match(gettingStarted, /Human-readable` and `JSON`/);
  assert.match(gettingStarted, /Case Outcome Summary/);
  assert.match(evidenceTrust, /# Evidence and Trust/);
  assert.match(evidenceTrust, /support surface for replay, signing, release evidence/);
  assert.match(evidenceTrust, /Case Outcome Summary/);
  assert.match(evidenceTrust, /Signature pending/);
  assert.match(evidenceTrust, /npm run verify:release/);
  assert.match(evidenceTrust, /public\/demo-e2e\/badge\.json/);
  assert.match(evidenceTrust, /artifacts\/release-evidence\/runtime-proof-report\.json/);
  assert.match(evidenceTrust, /No external side effects/);
  assert.match(evidenceTrust, /Open founder execution log/);
  assert.match(evidenceTrust, /first-10-contact worksheet/);
  assert.match(localServicesSpec, /AI Dispatcher for local service businesses/);
  assert.match(localServicesSpec, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch/);
  assert.match(localServicesSpec, /Measurement visit booking/);
  assert.match(localServicesSpec, /measurement visits for windows, doors, ceilings, blinds, and fit-out work/);
  assert.match(localServicesSpec, /Dispatch payload drawer/);
  assert.match(localServicesSpec, /Customer confirmation drawer/);
  assert.match(localServicesSpec, /Master\/operator handoff drawer/);
  assert.match(localServicesSpec, /Telegram intake prototype/);
  assert.match(localServicesSpec, /Open intake evidence/);
  assert.match(localServicesSpec, /Transcript \+ evidence/);
  assert.match(localServicesSpec, /Saved intake evidence/);
  assert.match(localServicesSpec, /Intake transcript \+ evidence link/);
  assert.match(localServicesSpec, /Copy intake evidence/);
  assert.match(localServicesSpec, /local_services_intake_evidence/);
  assert.match(localServicesSpec, /transcript_evidence_link/);
  assert.match(localServicesSpec, /Pilot readiness/);
  assert.match(localServicesSpec, /One-page offer/);
  assert.match(localServicesSpec, /90-second demo script/);
  assert.match(localServicesSpec, /Launch checklist/);
  assert.match(localServicesSpec, /Agent setup \/ training state/);
  assert.match(localServicesSpec, /Open setup checklist/);
  assert.match(localServicesSpec, /Business profile/);
  assert.match(localServicesSpec, /Knowledge sources/);
  assert.match(localServicesSpec, /Agent behavior/);
  assert.match(localServicesSpec, /Test call\/message/);
  assert.match(localServicesSpec, /Ready for test call\/message/);
  assert.match(localServicesSpec, /Training cards/);
  assert.match(localServicesSpec, /Copy setup brief/);
  assert.match(localServicesSpec, /local_services_agent_setup_training/);
  assert.match(localServicesSpec, /No channel activation/);
  assert.match(localServicesSpec, /\?setup=7min/);
  assert.match(localServicesSpec, /7-minute setup wizard/);
  assert.match(localServicesSpec, /Setup path/);
  assert.match(localServicesSpec, /Open day-one setup/);
  assert.match(localServicesSpec, /setupStepCompletionByService/);
  assert.match(localServicesSpec, /setupReadyByService/);
  assert.match(localServicesSpec, /Setup progress/);
  assert.match(localServicesSpec, /Saved setup state/);
  assert.match(localServicesSpec, /Mark complete/);
  assert.match(localServicesSpec, /Mark ready for pilot test/);
  assert.match(localServicesSpec, /Ready for pilot test/);
  assert.match(localServicesSpec, /Reset setup progress/);
  assert.match(localServicesSpec, /Test call\/message panel/);
  assert.match(localServicesSpec, /Sample inbound/);
  assert.match(localServicesSpec, /Expected extracted fields/);
  assert.match(localServicesSpec, /Pass\/fail checklist/);
  assert.match(localServicesSpec, /Mark check passed/);
  assert.match(localServicesSpec, /Record test passed/);
  assert.match(localServicesSpec, /Test call passed/);
  assert.match(localServicesSpec, /Reset test call/);
  assert.match(localServicesSpec, /testCallChecklistByService/);
  assert.match(localServicesSpec, /testCallPassedByService/);
  assert.match(localServicesSpec, /Outreach list filters/);
  assert.match(localServicesSpec, /Column settings/);
  assert.match(localServicesSpec, /Service filter/);
  assert.match(localServicesSpec, /Status filter/);
  assert.match(localServicesSpec, /Filtered candidates/);
  assert.match(localServicesSpec, /Filtered outreach list/);
  assert.match(localServicesSpec, /All services/);
  assert.match(localServicesSpec, /All statuses/);
  assert.match(localServicesSpec, /Clear filters/);
  assert.match(localServicesSpec, /View only, no send/);
  assert.match(localServicesSpec, /Pilot execution checklist/);
  assert.match(localServicesSpec, /Pass test call\/message/);
  assert.match(localServicesSpec, /Needs test call passed/);
  assert.match(localServicesSpec, /Prepare first manual batch/);
  assert.match(localServicesSpec, /Ready for first manual batch/);
  assert.match(localServicesSpec, /Record ready drafts/);
  assert.match(localServicesSpec, /Log manual contact/);
  assert.match(localServicesSpec, /Book discovery call/);
  assert.match(localServicesSpec, /Start metric capture/);
  assert.match(localServicesSpec, /Pilot checklist progress/);
  assert.match(localServicesSpec, /Dry run required/);
  assert.match(localServicesSpec, /Dry run passed/);
  assert.match(localServicesSpec, /Manual launch blocked/);
  assert.match(localServicesSpec, /Manual launch ready/);
  assert.match(localServicesSpec, /Open launch packet/);
  assert.match(localServicesSpec, /Pilot launch packet/);
  assert.match(localServicesSpec, /Launch packet\s+preview/);
  assert.match(localServicesSpec, /First manual contact checklist/);
  assert.match(localServicesSpec, /Copy launch packet/);
  assert.match(localServicesSpec, /local_services_pilot_launch_packet/);
  assert.match(localServicesSpec, /Manual activity log/);
  assert.match(localServicesSpec, /Last manual action/);
  assert.match(localServicesSpec, /Copy activity log/);
  assert.match(localServicesSpec, /local_services_manual_activity_log/);
  assert.match(localServicesSpec, /No external side\s+effects/);
  assert.match(localServicesSpec, /Founder\/operator validation/);
  assert.match(localServicesSpec, /No autonomous send/);
  assert.match(localServicesSpec, /Open discovery prep/);
  assert.match(localServicesSpec, /Discovery call\s+prep/);
  assert.match(localServicesSpec, /Questions to ask/);
  assert.match(localServicesSpec, /Pilot success criteria/);
  assert.match(localServicesSpec, /Copy discovery call prep/);
  assert.match(localServicesSpec, /local_services_discovery_call_prep/);
  assert.match(localServicesSpec, /Open day-one setup/);
  assert.match(localServicesSpec, /Day-one setup brief/);
  assert.match(localServicesSpec, /Business profile lock/);
  assert.match(localServicesSpec, /Setup tasks/);
  assert.match(localServicesSpec, /Test call plan/);
  assert.match(localServicesSpec, /Copy day-one setup brief/);
  assert.match(localServicesSpec, /local_services_day_one_setup_brief/);
  assert.match(localServicesSpec, /Open pilot runbook/);
  assert.match(localServicesSpec, /Pilot metrics/);
  assert.match(localServicesSpec, /Pilot outreach wizard/);
  assert.match(localServicesSpec, /4-step/);
  assert.match(localServicesSpec, /Audience from outreach list/);
  assert.match(localServicesSpec, /Message\/test preview/);
  assert.match(localServicesSpec, /Preview \/ Test message modal/);
  assert.match(localServicesSpec, /Copy test message/);
  assert.match(localServicesSpec, /Copy test message preview/);
  assert.match(localServicesSpec, /local_services_test_message_preview/);
  assert.match(localServicesSpec, /manual_confirmation_required_before_outreach/);
  assert.match(localServicesSpec, /Operator confirmation/);
  assert.match(localServicesSpec, /Operator confirmation summary/);
  assert.match(localServicesSpec, /Ready for manual outreach/);
  assert.match(localServicesSpec, /Copy confirmation summary/);
  assert.match(localServicesSpec, /local_services_operator_confirmation/);
  assert.match(localServicesSpec, /ready_for_manual_outreach/);
  assert.match(localServicesSpec, /Wizard progress/);
  assert.match(localServicesSpec, /Record ready for manual outreach/);
  assert.match(localServicesSpec, /Ready for manual outreach recorded/);
  assert.match(localServicesSpec, /Ask AI about pilot/);
  assert.match(localServicesSpec, /Suggested questions/);
  assert.match(localServicesSpec, /Best candidate/);
  assert.match(localServicesSpec, /Bottleneck/);
  assert.match(localServicesSpec, /Next message/);
  assert.match(localServicesSpec, /Copy analyst brief/);
  assert.match(localServicesSpec, /local_services_pilot_ai_analyst/);
  assert.match(localServicesSpec, /No external LLM call/);
  assert.match(localServicesSpec, /Pilot scorecard action/);
  assert.match(localServicesSpec, /Record scorecard draft/);
  assert.match(localServicesSpec, /Not contacted/);
  assert.match(localServicesSpec, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(localServicesSpec, /Draft ready/);
  assert.match(localServicesSpec, /Contacted manually/);
  assert.match(localServicesSpec, /Reply received/);
  assert.match(localServicesSpec, /Rejected for now/);
  assert.match(localServicesSpec, /Saved in this browser/);
  assert.match(localServicesSpec, /First request outcome/);
  assert.match(localServicesSpec, /Manual outcome state/);
  assert.match(localServicesSpec, /firstRequestOutcomeByProspectKey/);
  assert.match(localServicesSpec, /Needs follow-up/);
  assert.match(localServicesSpec, /Booked manually/);
  assert.match(localServicesSpec, /Outcome chain summary/);
  assert.match(localServicesSpec, /Scorecard draft/);
  assert.match(localServicesSpec, /Week-one review/);
  assert.match(localServicesSpec, /Pilot funnel summary/);
  assert.match(localServicesSpec, /All candidates/);
  assert.match(localServicesSpec, /Next manual\s+batch/);
  assert.match(localServicesSpec, /Manual execution rule/);
  assert.match(localServicesSpec, /Open pilot export/);
  assert.match(localServicesSpec, /Pilot workspace export drawer/);
  assert.match(localServicesSpec, /Copy pilot workspace export/);
  assert.match(localServicesSpec, /Pilot metrics tracker/);
  assert.match(localServicesSpec, /Copy pilot metrics tracker/);
  assert.match(localServicesSpec, /Pilot daily log/);
  assert.match(localServicesSpec, /Open daily log/);
  assert.match(localServicesSpec, /Daily capture fields/);
  assert.match(localServicesSpec, /Daily operating loop/);
  assert.match(localServicesSpec, /Copy pilot daily log/);
  assert.match(localServicesSpec, /local_services_pilot_daily_log/);
  assert.match(localServicesSpec, /weekly scorecard sync/);
  assert.match(localServicesSpec, /Open weekly sync checklist/);
  assert.match(localServicesSpec, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(localServicesSpec, /weeklyScorecardSyncReviewedByService/);
  assert.match(localServicesSpec, /Evidence readiness/);
  assert.match(localServicesSpec, /Pilot week-one review/);
  assert.match(localServicesSpec, /Open week-one review/);
  assert.match(localServicesSpec, /Continue \/ stop decision/);
  assert.match(localServicesSpec, /Copy week-one review/);
  assert.match(localServicesSpec, /local_services_pilot_week_one_review/);
  assert.match(localServicesSpec, /first\s+observed request result/);
  assert.match(localServicesSpec, /Owner-ready summary/);
  assert.match(localServicesSpec, /Decision readiness/);
  assert.match(localServicesSpec, /Latest manual signal/);
  assert.match(localServicesSpec, /day_one_recap_to_week_one_review/);
  assert.match(localServicesSpec, /Week-one owner\s+decision state/);
  assert.match(localServicesSpec, /Record continue/);
  assert.match(localServicesSpec, /Record pause/);
  assert.match(localServicesSpec, /Record stop/);
  assert.match(localServicesSpec, /weekOneOwnerDecisionByProspectKey/);
  assert.match(localServicesSpec, /week_one_owner_decision_to_evidence_pack/);
  assert.match(localServicesSpec, /Pilot evidence pack/);
  assert.match(localServicesSpec, /Open evidence pack/);
  assert.match(localServicesSpec, /Week-two evidence pack/);
  assert.match(localServicesSpec, /Copy evidence pack/);
  assert.match(localServicesSpec, /local_services_pilot_evidence_pack/);
  assert.match(localServicesSpec, /Week-one owner decision/);
  assert.match(localServicesSpec, /paid-pilot\s+readiness includes the first\s+observed request result/);
  assert.match(localServicesSpec, /No outbound message sent/);
  assert.match(localServicesSpec, /no real\s+outreach is sent autonomously/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-pilot-offer\.md/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-demo-script\.md/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-demo-recording-checklist\.md/);
  assert.match(localServicesSpec, /\?recording=90s/);
  assert.match(localServicesSpec, /90-second recording mode/);
  assert.match(localServicesSpec, /outreach tables and scorecard\s+controls are hidden during recording/i);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-outreach-list\.md/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-pilot-scorecard\.md/);
  assert.match(localServicesSpec, /Open founder execution log/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-founder-execution-log\.md/);
  assert.match(localServicesSpec, /first-10-contact worksheet/);
  assert.match(localServicesSpec, /First 10 contacts workspace/);
  assert.match(localServicesSpec, /Pilot proof checklist/);
  assert.match(localServicesSpec, /Pilot ops today/);
  assert.match(localServicesSpec, /Copy pilot ops handoff/);
  assert.match(localServicesSpec, /local_services_pilot_ops_today/);
  assert.match(localServicesSpec, /Open ops confirmation/);
  assert.match(localServicesSpec, /Pilot ops confirmation drawer/);
  assert.match(localServicesSpec, /Copy ops confirmation/);
  assert.match(localServicesSpec, /local_services_pilot_ops_confirmation/);
  assert.match(localServicesSpec, /Pilot proof update rail/);
  assert.match(localServicesSpec, /local_services_pilot_proof_update_rail/);
  assert.match(localServicesSpec, /Stop \/ Continue decision gate/);
  assert.match(localServicesSpec, /Keep collecting proof/);
  assert.match(localServicesSpec, /Continue to pilot setup/);
  assert.match(localServicesSpec, /Stop expansion for now/);
  assert.match(localServicesSpec, /Open batch review/);
  assert.match(localServicesSpec, /First\s+contact batch\s+review drawer/);
  assert.match(localServicesSpec, /Copy batch review/);
  assert.match(localServicesSpec, /Copy founder\s+workspace/);
  assert.match(localServicesSpec, /contactProofByProspectKey/);
  assert.match(localServicesSpec, /Founder proof recorded/);
  assert.match(localServicesSpec, /local_services_first_contact_batch_review/);
  assert.match(localServicesSpec, /founder_manual_validation_review/);
  assert.match(localServicesSpec, /no_booking_created/);
  assert.match(localServicesSpec, /Category pilot score/);
  assert.match(localServicesSpec, /Leading category/);
  assert.match(localServicesSpec, /No category expansion without proof/);
  assert.match(localServicesSpec, /local_services_category_pilot_score/);
  assert.match(localServicesSpec, /category_pilot_score/);
  assert.match(localServicesSpec, /Leading category\s+action layer/);
  assert.match(localServicesSpec, /Next manual\s+batch/);
  assert.match(localServicesSpec, /Discovery\s+questions/);
  assert.match(localServicesSpec, /Pilot setup\s+checklist/);
  assert.match(localServicesSpec, /Integration\s+hold/);
  assert.match(localServicesSpec, /Focus leading\s+category/);
  assert.match(localServicesSpec, /local_services_leading_category_action_layer/);
  assert.match(localServicesSpec, /leading_category_action_layer/);
  assert.match(localServicesSpec, /Pilot setup readiness/);
  assert.match(localServicesSpec, /Paid pilot gate/);
  assert.match(localServicesSpec, /Ready for first paid pilot/);
  assert.match(localServicesSpec, /Not ready for paid pilot/);
  assert.match(localServicesSpec, /Week-one owner decision/);
  assert.match(localServicesSpec, /Only\s+`Continue` can move the lane toward `Paid pilot proposal`/);
  assert.match(localServicesSpec, /`Week-one review` primary surface/);
  assert.match(localServicesSpec, /local_services_pilot_setup_readiness/);
  assert.match(localServicesSpec, /pilot_setup_readiness/);
  assert.match(localServicesSpec, /Readiness action\s+plan/);
  assert.match(localServicesSpec, /Continue\s+setup\/test path/);
  assert.match(localServicesSpec, /Copy readiness action plan/);
  assert.match(localServicesSpec, /local_services_readiness_action_plan/);
  assert.match(localServicesSpec, /readiness_action_plan/);
  assert.match(localServicesSpec, /Open proof drawer/);
  assert.match(localServicesSpec, /Readiness proof drawer/);
  assert.match(localServicesSpec, /Copy readiness proof/);
  assert.match(localServicesSpec, /local_services_readiness_proof_drawer/);
  assert.match(localServicesSpec, /readiness_evidence_view/);
  assert.match(localServicesSpec, /Open proposal preview/);
  assert.match(localServicesSpec, /Paid pilot proposal preview/);
  assert.match(localServicesSpec, /Copy proposal preview/);
  assert.match(localServicesSpec, /local_services_paid_pilot_proposal_preview/);
  assert.match(localServicesSpec, /operator_approved_proposal_preview/);
  assert.match(localServicesSpec, /Open approval handoff/);
  assert.match(localServicesSpec, /Proposal approval handoff/);
  assert.match(localServicesSpec, /Copy approval handoff/);
  assert.match(localServicesSpec, /local_services_proposal_approval_handoff/);
  assert.match(localServicesSpec, /manual_paid_pilot_approval_handoff/);
  assert.match(localServicesSpec, /Proposal\s+approval state/);
  assert.match(localServicesSpec, /Approve proposal handoff/);
  assert.match(localServicesSpec, /proposalApprovalByService/);
  assert.match(localServicesSpec, /Open kickoff gate/);
  assert.match(localServicesSpec, /Pilot kickoff gate/);
  assert.match(localServicesSpec, /Copy kickoff gate/);
  assert.match(localServicesSpec, /local_services_pilot_kickoff_gate/);
  assert.match(localServicesSpec, /manual_day_one_kickoff_gate/);
  assert.match(localServicesSpec, /Kickoff\s+decision state/);
  assert.match(localServicesSpec, /Mark kickoff ready/);
  assert.match(localServicesSpec, /kickoffDecisionByService/);
  assert.match(localServicesSpec, /Open run sheet/);
  assert.match(localServicesSpec, /Day-one operator run sheet/);
  assert.match(localServicesSpec, /Copy run sheet/);
  assert.match(localServicesSpec, /local_services_day_one_operator_run_sheet/);
  assert.match(localServicesSpec, /manual_day_one_operator_run_sheet/);
  assert.match(localServicesSpec, /day_one_run_sheet_outcome_capture/);
  assert.match(localServicesSpec, /Day-one outcome capture gate/);
  assert.match(localServicesSpec, /Weekly scorecard sync gate/);
  assert.match(localServicesSpec, /manual_weekly_scorecard_sync_gate/);
  assert.match(localServicesSpec, /Open day-one recap/);
  assert.match(localServicesSpec, /Day-one recap/);
  assert.match(localServicesSpec, /Copy day-one recap/);
  assert.match(localServicesSpec, /local_services_day_one_recap/);
  assert.match(localServicesSpec, /manual_day_one_recap/);
  assert.match(localServicesSpec, /day_one_recap_to_week_one_review/);
  assert.match(localServicesSpec, /NEWO-style AI\s+employee\s+platform/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-pilot-runbook\.md/);
  assert.match(localServicesSpec, /\/workspace-docs\/local-services-outreach-execution-pack\.md/);
  assert.match(localServicesSpec, /Open outreach execution pack/);
  assert.match(localServicesSpec, /Actual external execution still remains outside the shell/);
  assert.match(localServicesSpec, /same approval-gated job-card payload/);
  assert.match(localServicesSpec, /operator-approved action/);

  assert.match(pilotOffer, /# Local Services Pilot Offer/);
  assert.match(pilotOffer, /AI Dispatcher for Local Services/);
  assert.match(pilotOffer, /measurement visits/);
  assert.match(pilotOffer, /construction-material stock or delivery automation/);
  assert.match(pilotOffer, /Day-One Setup/);
  assert.match(pilotOffer, /Success Criteria For 14 Days/);
  assert.match(demoScript, /# Local Services 90-Second Demo Script/);
  assert.match(demoScript, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch/);
  assert.match(demoScript, /Pilot readiness/);
  assert.match(demoScript, /Agent setup \/ training state/);
  assert.match(demoScript, /Open setup checklist/);
  assert.match(demoScript, /Business profile/);
  assert.match(demoScript, /Knowledge sources/);
  assert.match(demoScript, /Agent behavior/);
  assert.match(demoScript, /test call readiness/);
  assert.match(demoScript, /Nothing here activates phone/);
  assert.match(demoScript, /Outreach list filters/);
  assert.match(demoScript, /Column settings/);
  assert.match(demoScript, /Filtered outreach list/);
  assert.match(demoScript, /Open intake evidence/);
  assert.match(demoScript, /Transcript \+ evidence/);
  assert.match(demoScript, /Saved intake evidence/);
  assert.match(demoScript, /Pilot execution checklist/);
  assert.match(demoScript, /Pass test call\/message/);
  assert.match(demoScript, /Needs test call passed/);
  assert.match(demoScript, /Prepare first manual batch/);
  assert.match(demoScript, /Manual activity log/);
  assert.match(demoScript, /Last manual action/);
  assert.match(demoScript, /Copy activity log/);
  assert.match(demoScript, /local_services_manual_activity_log/);
  assert.match(demoScript, /No external side\s+effects/);
  assert.match(demoScript, /Open discovery prep/);
  assert.match(demoScript, /Discovery call\s+prep/);
  assert.match(demoScript, /Questions to ask/);
  assert.match(demoScript, /Pilot success criteria/);
  assert.match(demoScript, /Copy discovery call prep/);
  assert.match(demoScript, /Open day-one setup/);
  assert.match(demoScript, /Day-one setup brief/);
  assert.match(demoScript, /Business profile lock/);
  assert.match(demoScript, /Setup tasks/);
  assert.match(demoScript, /Test call plan/);
  assert.match(demoScript, /Copy day-one setup brief/);
  assert.match(demoScript, /Open daily log/);
  assert.match(demoScript, /Pilot daily log/);
  assert.match(demoScript, /Daily capture fields/);
  assert.match(demoScript, /Daily operating loop/);
  assert.match(demoScript, /Copy pilot daily log/);
  assert.match(demoScript, /Open week-one review/);
  assert.match(demoScript, /Pilot week-one review/);
  assert.match(demoScript, /Continue \/ stop decision/);
  assert.match(demoScript, /Copy week-one review/);
  assert.match(demoScript, /Week-one owner\s+decision state/);
  assert.match(demoScript, /Record continue/);
  assert.match(demoScript, /Record pause/);
  assert.match(demoScript, /Record stop/);
  assert.match(demoScript, /weekOneOwnerDecisionByProspectKey/);
  assert.match(demoScript, /Open evidence pack/);
  assert.match(demoScript, /Pilot evidence pack/);
  assert.match(demoScript, /Week-two evidence pack/);
  assert.match(demoScript, /Week-one owner decision/);
  assert.match(demoScript, /week_one_owner_decision_to_evidence_pack/);
  assert.match(demoScript, /Copy evidence pack/);
  assert.match(demoScript, /Open recording checklist/);
  assert.match(demoScript, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&recording=90s/);
  assert.match(demoScript, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&setup=7min/);
  assert.match(demoScript, /90s recording/);
  assert.match(demoScript, /7-min setup/);
  assert.match(demoScript, /Recording path/);
  assert.match(demoScript, /7-minute setup wizard/);
  assert.match(demoScript, /Setup path/);
  assert.match(demoScript, /Setup progress/);
  assert.match(demoScript, /Saved setup state/);
  assert.match(demoScript, /Mark complete/);
  assert.match(demoScript, /Mark ready for pilot test/);
  assert.match(demoScript, /Ready for pilot test/);
  assert.match(demoScript, /Test call\/message panel/);
  assert.match(demoScript, /Sample inbound/);
  assert.match(demoScript, /Expected extracted fields/);
  assert.match(demoScript, /Pass\/fail checklist/);
  assert.match(demoScript, /Mark check passed/);
  assert.match(demoScript, /Record test passed/);
  assert.match(demoScript, /Test call passed/);
  assert.match(demoScript, /Reset test call/);
  assert.match(demoScript, /Open day-one setup/);
  assert.match(demoScript, /docs\/local-services-demo-recording-checklist\.md/);

  assert.match(demoRecordingChecklist, /Local Services Demo Recording Checklist/);
  assert.match(demoRecordingChecklist, /\/app\?demo=local-services-dispatch&service=ac-repair-dispatch&recording=90s/);
  assert.match(demoRecordingChecklist, /Required On-Screen Proof/);
  assert.match(demoRecordingChecklist, /90-second recording mode/);
  assert.match(demoRecordingChecklist, /Recording path/);
  assert.match(demoRecordingChecklist, /Open recording checklist/);
  assert.match(demoRecordingChecklist, /Manual activity log/);
  assert.match(demoRecordingChecklist, /No external side\s+effects/);
  assert.match(demoRecordingChecklist, /Do-Not-Claim Rules/);
  assert.match(demoRecordingChecklist, /local-services-dispatch-90s-demo\.mp4/);
  assert.match(demoScript, /Copy evidence pack/);
  assert.match(demoScript, /Open pilot runbook/);
  assert.match(demoScript, /Open metrics tracker/);
  assert.match(demoScript, /Measurement visit booking/);
  assert.match(demoScript, /final pricing are not automated in P0/);
  assert.match(demoScript, /operator-approved dispatch/);
  assert.match(outreachList, /# Local Services Outreach List/);
  assert.match(outreachList, /Checked against public sources on 2026-04-26/);
  assert.match(outreachList, /AC MASTER/);
  assert.match(outreachList, /Santexniki\.uz/);
  assert.match(outreachList, /Service-Pro/);
  assert.match(outreachList, /IMZO/);
  assert.match(outreachList, /Modern Systems/);
  assert.match(outreachList, /BRIX\.UZ/);
  assert.match(outreachList, /Scorecard draft/);
  assert.match(outreachList, /Not contacted/);
  assert.match(outreachList, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(outreachList, /Preview \/ Test message modal/);
  assert.match(outreachList, /Agent setup \/ training state/);
  assert.match(outreachList, /Business profile/);
  assert.match(outreachList, /Knowledge sources/);
  assert.match(outreachList, /Agent behavior/);
  assert.match(outreachList, /Test call\/message/);
  assert.match(outreachList, /Ready for test call\/message/);
  assert.match(outreachList, /Test call\/message panel/);
  assert.match(outreachList, /Sample inbound/);
  assert.match(outreachList, /Expected extracted fields/);
  assert.match(outreachList, /Pass\/fail checklist/);
  assert.match(outreachList, /Mark check passed/);
  assert.match(outreachList, /Record test passed/);
  assert.match(outreachList, /Test call passed/);
  assert.match(outreachList, /Operator confirmation summary/);
  assert.match(outreachList, /Record ready for manual outreach/);
  assert.match(outreachList, /Ask AI about pilot/);
  assert.match(outreachList, /Suggested questions/);
  assert.match(outreachList, /Best candidate/);
  assert.match(outreachList, /Bottleneck/);
  assert.match(outreachList, /Next message/);
  assert.match(outreachList, /Pilot funnel summary/);
  assert.match(outreachList, /Next manual\s+batch/);
  assert.match(outreachList, /Outreach list filters/);
  assert.match(outreachList, /Column settings/);
  assert.match(outreachList, /Service filter/);
  assert.match(outreachList, /Status filter/);
  assert.match(outreachList, /Filtered candidates/);
  assert.match(outreachList, /Filtered outreach list/);
  assert.match(outreachList, /All services/);
  assert.match(outreachList, /All statuses/);
  assert.match(outreachList, /Clear filters/);
  assert.match(outreachList, /View only, no send/);
  assert.match(outreachList, /Open intake evidence/);
  assert.match(outreachList, /Transcript \+ evidence/);
  assert.match(outreachList, /Saved intake evidence/);
  assert.match(outreachList, /Copy intake\s+evidence/);
  assert.match(outreachList, /local_services_intake_evidence/);
  assert.match(outreachList, /transcript_evidence_link/);
  assert.match(outreachList, /Pilot execution checklist/);
  assert.match(outreachList, /Pass test call\/message/);
  assert.match(outreachList, /Needs test call passed/);
  assert.match(outreachList, /Prepare first manual batch/);
  assert.match(outreachList, /Ready for first manual batch/);
  assert.match(outreachList, /Record ready drafts/);
  assert.match(outreachList, /Log manual contact/);
  assert.match(outreachList, /Book discovery call/);
  assert.match(outreachList, /Start metric capture/);
  assert.match(outreachList, /Founder\/operator validation/);
  assert.match(outreachList, /No autonomous send/);
  assert.match(outreachList, /Manual activity log/);
  assert.match(outreachList, /Last manual action/);
  assert.match(outreachList, /Copy activity log/);
  assert.match(outreachList, /local_services_manual_activity_log/);
  assert.match(outreachList, /No external side\s+effects/);
  assert.match(outreachList, /Open discovery prep/);
  assert.match(outreachList, /Discovery call\s+prep/);
  assert.match(outreachList, /Questions to ask/);
  assert.match(outreachList, /Pilot success criteria/);
  assert.match(outreachList, /Copy discovery call prep/);
  assert.match(outreachList, /local_services_discovery_call_prep/);
  assert.match(outreachList, /Open day-one setup/);
  assert.match(outreachList, /Day-one setup brief/);
  assert.match(outreachList, /Business profile lock/);
  assert.match(outreachList, /Setup tasks/);
  assert.match(outreachList, /Test call plan/);
  assert.match(outreachList, /Copy day-one setup brief/);
  assert.match(outreachList, /local_services_day_one_setup_brief/);
  assert.match(outreachList, /Open daily log/);
  assert.match(outreachList, /Pilot daily log/);
  assert.match(outreachList, /Daily capture fields/);
  assert.match(outreachList, /Daily operating loop/);
  assert.match(outreachList, /Copy pilot daily log/);
  assert.match(outreachList, /local_services_pilot_daily_log/);
  assert.match(outreachList, /Open week-one review/);
  assert.match(outreachList, /Pilot week-one\s+review/);
  assert.match(outreachList, /Continue \/ stop decision/);
  assert.match(outreachList, /Copy week-one review/);
  assert.match(outreachList, /local_services_pilot_week_one_review/);
  assert.match(outreachList, /reviewed\s+decision packet/);
  assert.match(outreachList, /Week-one owner\s+decision state/);
  assert.match(outreachList, /Record continue/);
  assert.match(outreachList, /Record pause/);
  assert.match(outreachList, /Record stop/);
  assert.match(outreachList, /weekOneOwnerDecisionByProspectKey/);
  assert.match(outreachList, /Open evidence pack/);
  assert.match(outreachList, /Pilot evidence pack/);
  assert.match(outreachList, /Week-two evidence pack/);
  assert.match(outreachList, /Copy evidence pack/);
  assert.match(outreachList, /local_services_pilot_evidence_pack/);
  assert.match(outreachList, /Week-one owner decision/);
  assert.match(outreachList, /week_one_owner_decision_to_evidence_pack/);
  assert.match(outreachList, /paid-pilot readiness/);
  assert.match(outreachList, /Open pilot runbook/);
  assert.match(outreachList, /Benchmarks, Not First Calls/);
  assert.match(outreachList, /docs\/local-services-outreach-execution-pack\.md/);
  assert.match(pilotScorecard, /# Local Services Pilot Scorecard/);
  assert.match(pilotScorecard, /Account Qualification Score/);
  assert.match(pilotScorecard, /Measurement or media intake/);
  assert.match(pilotScorecard, /14-18/);
  assert.match(pilotScorecard, /Shell Draft Flow/);
  assert.match(pilotScorecard, /Pilot scorecard action/);
  assert.match(pilotScorecard, /Agent setup \/ training state/);
  assert.match(pilotScorecard, /Open setup checklist/);
  assert.match(pilotScorecard, /Business profile/);
  assert.match(pilotScorecard, /Knowledge sources/);
  assert.match(pilotScorecard, /Agent behavior/);
  assert.match(pilotScorecard, /Test call\/message/);
  assert.match(pilotScorecard, /Ready for test call\/message/);
  assert.match(pilotScorecard, /Test call\/message panel/);
  assert.match(pilotScorecard, /Sample inbound/);
  assert.match(pilotScorecard, /Expected extracted fields/);
  assert.match(pilotScorecard, /Pass\/fail checklist/);
  assert.match(pilotScorecard, /Mark check passed/);
  assert.match(pilotScorecard, /Record test passed/);
  assert.match(pilotScorecard, /Test call passed/);
  assert.match(pilotScorecard, /Open intake evidence/);
  assert.match(pilotScorecard, /Transcript \+ evidence/);
  assert.match(pilotScorecard, /Saved intake evidence/);
  assert.match(pilotScorecard, /Intake transcript \+ evidence link/);
  assert.match(pilotScorecard, /Copy intake evidence/);
  assert.match(pilotScorecard, /local_services_intake_evidence/);
  assert.match(pilotScorecard, /transcript_evidence_link/);
  assert.match(pilotScorecard, /Pilot execution checklist/);
  assert.match(pilotScorecard, /Pilot ops today/);
  assert.match(pilotScorecard, /local_services_pilot_ops_today/);
  assert.match(pilotScorecard, /Open ops confirmation/);
  assert.match(pilotScorecard, /local_services_pilot_ops_confirmation/);
  assert.match(pilotScorecard, /Pilot proof update rail/);
  assert.match(pilotScorecard, /local_services_pilot_proof_update_rail/);
  assert.match(pilotScorecard, /Pass test call\/message/);
  assert.match(pilotScorecard, /Needs test call passed/);
  assert.match(pilotScorecard, /Prepare first manual batch/);
  assert.match(pilotScorecard, /Ready for first manual batch/);
  assert.match(pilotScorecard, /Record ready drafts/);
  assert.match(pilotScorecard, /Log manual contact/);
  assert.match(pilotScorecard, /Book discovery call/);
  assert.match(pilotScorecard, /Start metric capture/);
  assert.match(pilotScorecard, /Founder\/operator validation/);
  assert.match(pilotScorecard, /No autonomous send/);
  assert.match(pilotScorecard, /Manual activity log/);
  assert.match(pilotScorecard, /Last manual action/);
  assert.match(pilotScorecard, /Copy activity log/);
  assert.match(pilotScorecard, /local_services_manual_activity_log/);
  assert.match(pilotScorecard, /No external side\s+effects/);
  assert.match(pilotScorecard, /Open discovery prep/);
  assert.match(pilotScorecard, /Discovery call\s+prep/);
  assert.match(pilotScorecard, /Questions to ask/);
  assert.match(pilotScorecard, /Pilot success criteria/);
  assert.match(pilotScorecard, /Copy discovery call prep/);
  assert.match(pilotScorecard, /local_services_discovery_call_prep/);
  assert.match(pilotScorecard, /Open day-one setup/);
  assert.match(pilotScorecard, /Day-one setup brief/);
  assert.match(pilotScorecard, /Business profile lock/);
  assert.match(pilotScorecard, /Setup tasks/);
  assert.match(pilotScorecard, /Test call plan/);
  assert.match(pilotScorecard, /Copy day-one setup brief/);
  assert.match(pilotScorecard, /local_services_day_one_setup_brief/);
  assert.match(pilotScorecard, /Open pilot runbook/);
  assert.match(pilotScorecard, /Operator confirmation summary/);
  assert.match(pilotScorecard, /Record ready for manual outreach/);
  assert.match(pilotScorecard, /Ask AI about pilot/);
  assert.match(pilotScorecard, /Suggested questions/);
  assert.match(pilotScorecard, /Best candidate/);
  assert.match(pilotScorecard, /Bottleneck/);
  assert.match(pilotScorecard, /Next message/);
  assert.match(pilotScorecard, /external LLM/);
  assert.match(pilotScorecard, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(pilotScorecard, /Draft ready/);
  assert.match(pilotScorecard, /Contacted manually/);
  assert.match(pilotScorecard, /Reply received/);
  assert.match(pilotScorecard, /Rejected for now/);
  assert.match(pilotScorecard, /First request outcome/);
  assert.match(pilotScorecard, /Manual outcome state/);
  assert.match(pilotScorecard, /firstRequestOutcomeByProspectKey/);
  assert.match(pilotScorecard, /Needs follow-up/);
  assert.match(pilotScorecard, /Booked manually/);
  assert.match(pilotScorecard, /Outcome chain summary/);
  assert.match(pilotScorecard, /Scorecard draft/);
  assert.match(pilotScorecard, /Week-one review/);
  assert.match(pilotScorecard, /Pilot funnel summary/);
  assert.match(pilotScorecard, /All candidates/);
  assert.match(pilotScorecard, /Next manual\s+batch/);
  assert.match(pilotScorecard, /Outreach list filters/);
  assert.match(pilotScorecard, /Column settings/);
  assert.match(pilotScorecard, /Service filter/);
  assert.match(pilotScorecard, /Status filter/);
  assert.match(pilotScorecard, /Filtered candidates/);
  assert.match(pilotScorecard, /Filtered outreach list/);
  assert.match(pilotScorecard, /All services/);
  assert.match(pilotScorecard, /All statuses/);
  assert.match(pilotScorecard, /Clear filters/);
  assert.match(pilotScorecard, /View only, no send/);
  assert.match(pilotScorecard, /manual execution rule/);
  assert.match(pilotScorecard, /Open pilot export/);
  assert.match(pilotScorecard, /Pilot workspace export drawer/);
  assert.match(pilotScorecard, /Copy pilot workspace export/);
  assert.match(pilotScorecard, /Pilot metrics tracker/);
  assert.match(pilotScorecard, /Copy pilot metrics tracker/);
  assert.match(pilotScorecard, /Weekly scorecard sync gate/);
  assert.match(pilotScorecard, /manual_weekly_scorecard_sync_gate/);
  assert.match(pilotScorecard, /Open weekly sync checklist/);
  assert.match(pilotScorecard, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(pilotScorecard, /weeklyScorecardSyncReviewedByService/);
  assert.match(pilotScorecard, /Evidence readiness/);
  assert.match(pilotScorecard, /Open daily log/);
  assert.match(pilotScorecard, /Pilot daily log/);
  assert.match(pilotScorecard, /Daily capture fields/);
  assert.match(pilotScorecard, /Daily operating loop/);
  assert.match(pilotScorecard, /Copy pilot daily log/);
  assert.match(pilotScorecard, /local_services_pilot_daily_log/);
  assert.match(pilotScorecard, /weekly scorecard sync/);
  assert.match(pilotScorecard, /Open week-one review/);
  assert.match(pilotScorecard, /Pilot week-one review/);
  assert.match(pilotScorecard, /Continue \/ stop decision/);
  assert.match(pilotScorecard, /Copy week-one review/);
  assert.match(pilotScorecard, /local_services_pilot_week_one_review/);
  assert.match(pilotScorecard, /owner-reviewed continue, pause/);
  assert.match(pilotScorecard, /Owner-ready summary/);
  assert.match(pilotScorecard, /Decision readiness/);
  assert.match(pilotScorecard, /Latest manual signal/);
  assert.match(pilotScorecard, /day_one_recap_to_week_one_review/);
  assert.match(pilotScorecard, /Week-one owner\s+decision state/);
  assert.match(pilotScorecard, /Record continue/);
  assert.match(pilotScorecard, /Record pause/);
  assert.match(pilotScorecard, /Record stop/);
  assert.match(pilotScorecard, /weekOneOwnerDecisionByProspectKey/);
  assert.match(pilotScorecard, /week_one_owner_decision_to_evidence_pack/);
  assert.match(pilotScorecard, /Open evidence pack/);
  assert.match(pilotScorecard, /Pilot evidence pack/);
  assert.match(pilotScorecard, /Week-two evidence pack/);
  assert.match(pilotScorecard, /Copy evidence pack/);
  assert.match(pilotScorecard, /local_services_pilot_evidence_pack/);
  assert.match(pilotScorecard, /Week-one owner decision/);
  assert.match(pilotScorecard, /paid-pilot readiness\s+proof pack/);
  assert.match(pilotScorecard, /Proposal\s+approval state/);
  assert.match(pilotScorecard, /Approve proposal handoff/);
  assert.match(pilotScorecard, /proposalApprovalByService/);
  assert.match(pilotScorecard, /Kickoff\s+decision state/);
  assert.match(pilotScorecard, /Mark kickoff ready/);
  assert.match(pilotScorecard, /kickoffDecisionByService/);
  assert.match(pilotScorecard, /Open day-one recap/);
  assert.match(pilotScorecard, /Day-one recap/);
  assert.match(pilotScorecard, /Copy day-one recap/);
  assert.match(pilotScorecard, /local_services_day_one_recap/);
  assert.match(pilotScorecard, /day_one_recap_to_week_one_review/);
  assert.match(pilotScorecard, /does not send a\s+message/);
  assert.match(pilotScorecard, /activate phone or Telegram/);
  assert.match(pilotScorecard, /14-Day Pilot Scorecard/);
  assert.match(pilotScorecard, /Pilot Exit Rules/);
  assert.match(pilotScorecard, /docs\/local-services-pilot-runbook\.md/);
  assert.match(pilotScorecard, /docs\/local-services-outreach-execution-pack\.md/);
  assert.match(pilotScorecard, /docs\/local-services-founder-execution-log\.md/);
  assert.match(pilotScorecard, /Open founder execution log/);
  assert.match(pilotScorecard, /first-10\s+contact worksheet/);
  assert.match(pilotScorecard, /Category pilot score/);
  assert.match(pilotScorecard, /Leading category/);
  assert.match(pilotScorecard, /No category expansion without proof/);
  assert.match(pilotScorecard, /Leading category\s+action layer/);
  assert.match(pilotScorecard, /Next manual\s+batch/);
  assert.match(pilotScorecard, /Discovery\s+questions/);
  assert.match(pilotScorecard, /Pilot setup\s+checklist/);
  assert.match(pilotScorecard, /Integration\s+hold/);
  assert.match(pilotScorecard, /Focus leading\s+category/);
  assert.match(pilotScorecard, /Pilot setup readiness/);
  assert.match(pilotScorecard, /Ready for first paid pilot/);
  assert.match(pilotScorecard, /Week-one owner decision/);
  assert.match(pilotScorecard, /Only `Continue` can unlock `Paid pilot proposal`/);
  assert.match(pilotRunbook, /# Local Services Pilot Runbook/);
  assert.match(pilotRunbook, /Day Minus One: Prepare/);
  assert.match(pilotRunbook, /Day Zero: Outreach/);
  assert.match(pilotRunbook, /Discovery Call/);
  assert.match(pilotRunbook, /Pilot Setup/);
  assert.match(pilotRunbook, /Daily Operating Loop/);
  assert.match(pilotRunbook, /Week One Review/);
  assert.match(pilotRunbook, /Week Two Review/);
  assert.match(pilotRunbook, /Evidence Pack/);
  assert.match(pilotRunbook, /The shell must not send messages/);
  assert.match(pilotRunbook, /Preview \/ Test message modal/);
  assert.match(pilotRunbook, /Agent setup \/ training state/);
  assert.match(pilotRunbook, /Open setup checklist/);
  assert.match(pilotRunbook, /Business profile/);
  assert.match(pilotRunbook, /Knowledge sources/);
  assert.match(pilotRunbook, /Agent behavior/);
  assert.match(pilotRunbook, /Test call\/message/);
  assert.match(pilotRunbook, /Ready for test call\/message/);
  assert.match(pilotRunbook, /Copy setup brief/);
  assert.match(pilotRunbook, /Test call\/message panel/);
  assert.match(pilotRunbook, /Sample inbound/);
  assert.match(pilotRunbook, /Expected extracted fields/);
  assert.match(pilotRunbook, /Pass\/fail checklist/);
  assert.match(pilotRunbook, /Mark check passed/);
  assert.match(pilotRunbook, /Record test passed/);
  assert.match(pilotRunbook, /Open intake evidence/);
  assert.match(pilotRunbook, /Transcript \+ evidence/);
  assert.match(pilotRunbook, /Saved intake evidence/);
  assert.match(pilotRunbook, /Intake transcript \+ evidence link/);
  assert.match(pilotRunbook, /Copy intake evidence/);
  assert.match(pilotRunbook, /local_services_intake_evidence/);
  assert.match(pilotRunbook, /transcript_evidence_link/);
  assert.match(pilotRunbook, /Category pilot score/);
  assert.match(pilotRunbook, /Leading category/);
  assert.match(pilotRunbook, /No category expansion without proof/);
  assert.match(pilotRunbook, /Leading category\s+action layer/);
  assert.match(pilotRunbook, /Next manual\s+batch/);
  assert.match(pilotRunbook, /Discovery\s+questions/);
  assert.match(pilotRunbook, /Pilot setup\s+checklist/);
  assert.match(pilotRunbook, /Integration\s+hold/);
  assert.match(pilotRunbook, /Focus leading\s+category/);
  assert.match(pilotRunbook, /Pilot setup readiness/);
  assert.match(pilotRunbook, /Paid pilot gate/);
  assert.match(pilotRunbook, /Not ready for paid pilot/);
  assert.match(pilotRunbook, /Week-one owner decision/);
  assert.match(pilotRunbook, /Only `Continue`\s+can unlock `Paid pilot proposal`/);
  assert.match(pilotRunbook, /Proposal\s+approval state/);
  assert.match(pilotRunbook, /Approve proposal handoff/);
  assert.match(pilotRunbook, /proposalApprovalByService/);
  assert.match(pilotRunbook, /Kickoff\s+decision state/);
  assert.match(pilotRunbook, /Mark kickoff ready/);
  assert.match(pilotRunbook, /kickoffDecisionByService/);
  assert.match(pilotRunbook, /Pilot execution checklist/);
  assert.match(pilotRunbook, /Pilot ops today/);
  assert.match(pilotRunbook, /local_services_pilot_ops_today/);
  assert.match(pilotRunbook, /Open ops confirmation/);
  assert.match(pilotRunbook, /local_services_pilot_ops_confirmation/);
  assert.match(pilotRunbook, /Pilot proof update rail/);
  assert.match(pilotRunbook, /local_services_pilot_proof_update_rail/);
  assert.match(pilotRunbook, /Pass test call\/message/);
  assert.match(pilotRunbook, /Needs test call passed/);
  assert.match(pilotRunbook, /Prepare first manual batch/);
  assert.match(pilotRunbook, /Ready for first manual batch/);
  assert.match(pilotRunbook, /Record ready drafts/);
  assert.match(pilotRunbook, /Log manual contact/);
  assert.match(pilotRunbook, /Book discovery call/);
  assert.match(pilotRunbook, /Start metric capture/);
  assert.match(pilotRunbook, /Founder\/operator validation/);
  assert.match(pilotRunbook, /No autonomous send/);
  assert.match(pilotRunbook, /Manual activity log/);
  assert.match(pilotRunbook, /Last manual action/);
  assert.match(pilotRunbook, /Copy activity log/);
  assert.match(pilotRunbook, /local_services_manual_activity_log/);
  assert.match(pilotRunbook, /No external side\s+effects/);
  assert.match(pilotRunbook, /Open discovery prep/);
  assert.match(pilotRunbook, /Discovery call\s+prep/);
  assert.match(pilotRunbook, /Questions to ask/);
  assert.match(pilotRunbook, /Pilot success criteria/);
  assert.match(pilotRunbook, /Copy discovery call prep/);
  assert.match(pilotRunbook, /local_services_discovery_call_prep/);
  assert.match(pilotRunbook, /Open day-one setup/);
  assert.match(pilotRunbook, /Day-one setup brief/);
  assert.match(pilotRunbook, /Business profile lock/);
  assert.match(pilotRunbook, /Setup tasks/);
  assert.match(pilotRunbook, /Test call plan/);
  assert.match(pilotRunbook, /Copy day-one setup brief/);
  assert.match(pilotRunbook, /local_services_day_one_setup_brief/);
  assert.match(pilotRunbook, /Open daily log/);
  assert.match(pilotRunbook, /Pilot daily log/);
  assert.match(pilotRunbook, /Daily capture fields/);
  assert.match(pilotRunbook, /Daily operating loop/);
  assert.match(pilotRunbook, /Copy pilot daily log/);
  assert.match(pilotRunbook, /local_services_pilot_daily_log/);
  assert.match(pilotRunbook, /Open weekly sync checklist/);
  assert.match(pilotRunbook, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(pilotRunbook, /weeklyScorecardSyncReviewedByService/);
  assert.match(pilotRunbook, /Evidence readiness/);
  assert.match(pilotRunbook, /Weekly scorecard sync gate/);
  assert.match(pilotRunbook, /manual_weekly_scorecard_sync_gate/);
  assert.match(pilotRunbook, /Open week-one review/);
  assert.match(pilotRunbook, /Pilot week-one review/);
  assert.match(pilotRunbook, /Continue \/ stop decision/);
  assert.match(pilotRunbook, /Copy week-one review/);
  assert.match(pilotRunbook, /local_services_pilot_week_one_review/);
  assert.match(pilotRunbook, /firstRequestOutcomeByProspectKey/);
  assert.match(pilotRunbook, /Owner-ready summary/);
  assert.match(pilotRunbook, /Decision readiness/);
  assert.match(pilotRunbook, /Latest manual signal/);
  assert.match(pilotRunbook, /day_one_recap_to_week_one_review/);
  assert.match(pilotRunbook, /Week-one owner\s+decision state/);
  assert.match(pilotRunbook, /Record continue/);
  assert.match(pilotRunbook, /Record pause/);
  assert.match(pilotRunbook, /Record stop/);
  assert.match(pilotRunbook, /weekOneOwnerDecisionByProspectKey/);
  assert.match(pilotRunbook, /week_one_owner_decision_to_evidence_pack/);
  assert.match(pilotRunbook, /Outcome chain summary/);
  assert.match(pilotRunbook, /Scorecard draft/);
  assert.match(pilotRunbook, /Week-one review/);
  assert.match(pilotRunbook, /Open day-one recap/);
  assert.match(pilotRunbook, /Day-one recap/);
  assert.match(pilotRunbook, /Copy day-one recap/);
  assert.match(pilotRunbook, /local_services_day_one_recap/);
  assert.match(pilotRunbook, /day_one_recap_to_week_one_review/);
  assert.match(pilotRunbook, /Open evidence pack/);
  assert.match(pilotRunbook, /Pilot evidence pack/);
  assert.match(pilotRunbook, /Week-two evidence pack/);
  assert.match(pilotRunbook, /docs\/local-services-demo-recording-checklist\.md/);
  assert.match(pilotRunbook, /Copy evidence pack/);
  assert.match(pilotRunbook, /local_services_pilot_evidence_pack/);
  assert.match(pilotRunbook, /firstRequestOutcomeByProspectKey/);
  assert.match(pilotRunbook, /Week-one owner decision/);
  assert.match(pilotRunbook, /Open pilot runbook/);
  assert.match(pilotRunbook, /Operator confirmation summary/);
  assert.match(pilotRunbook, /Ready for manual outreach/);
  assert.match(pilotRunbook, /Record ready for manual outreach/);
  assert.match(pilotRunbook, /Ask AI about pilot/);
  assert.match(pilotRunbook, /Suggested questions/);
  assert.match(pilotRunbook, /Best candidate/);
  assert.match(pilotRunbook, /Bottleneck/);
  assert.match(pilotRunbook, /Next message/);
  assert.match(pilotRunbook, /external LLM/);
  assert.match(pilotRunbook, /Outreach list filters/);
  assert.match(pilotRunbook, /Column settings/);
  assert.match(pilotRunbook, /Service filter/);
  assert.match(pilotRunbook, /Status filter/);
  assert.match(pilotRunbook, /Filtered candidates/);
  assert.match(pilotRunbook, /Filtered outreach list/);
  assert.match(pilotRunbook, /View only, no send/);
  assert.match(pilotRunbook, /docs\/local-services-outreach-execution-pack\.md/);
  assert.match(pilotRunbook, /docs\/local-services-founder-execution-log\.md/);
  assert.match(pilotRunbook, /Open founder execution\s+log/);
  assert.match(pilotRunbook, /first-10-contact worksheet/);
  assert.match(pilotRunbook, /founder\/operator\s+validation/);
  assert.match(pilotRunbook, /not a solo-only product scope/);
  assert.match(pilotRunbook, /Do not store private customer data in public docs/);
  assert.match(outreachExecutionPack, /# Local Services Outreach Execution Pack/);
  assert.match(outreachExecutionPack, /docs\/local-services-founder-execution-log\.md/);
  assert.match(outreachExecutionPack, /First Manual Batch/);
  assert.match(outreachExecutionPack, /AC MASTER/);
  assert.match(outreachExecutionPack, /Aircold/);
  assert.match(outreachExecutionPack, /Santexniki\.uz/);
  assert.match(outreachExecutionPack, /Service-Pro/);
  assert.match(outreachExecutionPack, /Measurement Visit Fallback/);
  assert.match(outreachExecutionPack, /Manual Execution Table/);
  assert.match(outreachExecutionPack, /Do-Not-Send Rules/);
  assert.match(outreachExecutionPack, /Discovery Call Template/);
  assert.match(outreachExecutionPack, /The shell must not send messages/);
  assert.match(founderExecutionLog, /# Local Services Founder Execution Log/);
  assert.match(founderExecutionLog, /First 10 Manual Contacts/);
  assert.match(founderExecutionLog, /Pilot Proof Capture/);
  assert.match(founderExecutionLog, /Manual contact only\. No external side effects\./);
  assert.match(founderExecutionLog, /Do not store private customer names/);
  assert.match(founderExecutionLog, /First 10 contacts workspace/);
  assert.match(founderExecutionLog, /Pilot ops today/);
  assert.match(founderExecutionLog, /local_services_pilot_ops_today/);
  assert.match(founderExecutionLog, /Open ops confirmation/);
  assert.match(founderExecutionLog, /local_services_pilot_ops_confirmation/);
  assert.match(founderExecutionLog, /Pilot proof update rail/);
  assert.match(founderExecutionLog, /local_services_pilot_proof_update_rail/);
  assert.match(founderExecutionLog, /Stop \/ Continue decision gate/);
  assert.match(founderExecutionLog, /Open batch review/);
  assert.match(founderExecutionLog, /First contact batch\s+review drawer/);
  assert.match(founderExecutionLog, /Copy batch review/);
  assert.match(founderExecutionLog, /liveDesk:localServicesPilotWorkspace:v1/);
  assert.match(founderExecutionLog, /NEWO-style AI\s+employee\s+platform/);
  assert.match(founderExecutionLog, /Category pilot score/);
  assert.match(founderExecutionLog, /Leading category/);
  assert.match(founderExecutionLog, /No category expansion without proof/);
  assert.match(founderExecutionLog, /Leading category\s+action layer/);
  assert.match(founderExecutionLog, /Next manual\s+batch/);
  assert.match(founderExecutionLog, /Discovery\s+questions/);
  assert.match(founderExecutionLog, /Pilot setup\s+checklist/);
  assert.match(founderExecutionLog, /Integration\s+hold/);
  assert.match(founderExecutionLog, /Pilot setup readiness/);
  assert.match(founderExecutionLog, /Ready for first paid pilot/);
  assert.match(outreachExecutionPack, /Preview \/ Test message modal/);
  assert.match(outreachExecutionPack, /Agent setup \/ training state/);
  assert.match(outreachExecutionPack, /Open setup checklist/);
  assert.match(outreachExecutionPack, /Business profile/);
  assert.match(outreachExecutionPack, /Knowledge sources/);
  assert.match(outreachExecutionPack, /Agent behavior/);
  assert.match(outreachExecutionPack, /Test call\/message/);
  assert.match(outreachExecutionPack, /Ready for test call\/message/);
  assert.match(outreachExecutionPack, /Training cards/);
  assert.match(outreachExecutionPack, /Copy setup brief/);
  assert.match(outreachExecutionPack, /No channel activation/);
  assert.match(outreachExecutionPack, /Test call\/message panel/);
  assert.match(outreachExecutionPack, /Sample inbound/);
  assert.match(outreachExecutionPack, /Expected extracted fields/);
  assert.match(outreachExecutionPack, /Pass\/fail checklist/);
  assert.match(outreachExecutionPack, /Mark check passed/);
  assert.match(outreachExecutionPack, /Record test passed/);
  assert.match(outreachExecutionPack, /Test call passed/);
  assert.match(outreachExecutionPack, /Copy test message/);
  assert.match(outreachExecutionPack, /Operator confirmation summary/);
  assert.match(outreachExecutionPack, /Ready for manual outreach/);
  assert.match(outreachExecutionPack, /Record ready for manual outreach/);
  assert.match(outreachExecutionPack, /Ask AI about pilot/);
  assert.match(outreachExecutionPack, /Suggested questions/);
  assert.match(outreachExecutionPack, /Best candidate/);
  assert.match(outreachExecutionPack, /Bottleneck/);
  assert.match(outreachExecutionPack, /Next message/);
  assert.match(outreachExecutionPack, /Copy analyst brief/);
  assert.match(outreachExecutionPack, /No external LLM call/);
  assert.match(outreachExecutionPack, /Outreach list filters/);
  assert.match(outreachExecutionPack, /Column settings/);
  assert.match(outreachExecutionPack, /Service filter/);
  assert.match(outreachExecutionPack, /Status filter/);
  assert.match(outreachExecutionPack, /Filtered candidates/);
  assert.match(outreachExecutionPack, /Filtered outreach list/);
  assert.match(outreachExecutionPack, /All services/);
  assert.match(outreachExecutionPack, /All statuses/);
  assert.match(outreachExecutionPack, /Clear filters/);
  assert.match(outreachExecutionPack, /View only, no send/);
  assert.match(outreachExecutionPack, /Open intake evidence/);
  assert.match(outreachExecutionPack, /Transcript \+ evidence/);
  assert.match(outreachExecutionPack, /Saved intake evidence/);
  assert.match(outreachExecutionPack, /Intake transcript \+ evidence link/);
  assert.match(outreachExecutionPack, /Copy intake evidence/);
  assert.match(outreachExecutionPack, /local_services_intake_evidence/);
  assert.match(outreachExecutionPack, /transcript_evidence_link/);
  assert.match(outreachExecutionPack, /Pilot execution checklist/);
  assert.match(outreachExecutionPack, /Pilot ops today/);
  assert.match(outreachExecutionPack, /local_services_pilot_ops_today/);
  assert.match(outreachExecutionPack, /Open ops confirmation/);
  assert.match(outreachExecutionPack, /local_services_pilot_ops_confirmation/);
  assert.match(outreachExecutionPack, /Pilot proof update rail/);
  assert.match(outreachExecutionPack, /local_services_pilot_proof_update_rail/);
  assert.match(outreachExecutionPack, /Pass test call\/message/);
  assert.match(outreachExecutionPack, /Needs test call passed/);
  assert.match(outreachExecutionPack, /Prepare first manual batch/);
  assert.match(outreachExecutionPack, /Ready for first manual batch/);
  assert.match(outreachExecutionPack, /Record ready drafts/);
  assert.match(outreachExecutionPack, /Log manual contact/);
  assert.match(outreachExecutionPack, /Book discovery call/);
  assert.match(outreachExecutionPack, /Start metric capture/);
  assert.match(outreachExecutionPack, /Founder\/operator validation/);
  assert.match(outreachExecutionPack, /No autonomous send/);
  assert.match(outreachExecutionPack, /Manual activity log/);
  assert.match(outreachExecutionPack, /Last manual action/);
  assert.match(outreachExecutionPack, /Copy activity log/);
  assert.match(outreachExecutionPack, /local_services_manual_activity_log/);
  assert.match(outreachExecutionPack, /No external side\s+effects/);
  assert.match(outreachExecutionPack, /Category pilot score/);
  assert.match(outreachExecutionPack, /Leading category/);
  assert.match(outreachExecutionPack, /No category expansion without proof/);
  assert.match(outreachExecutionPack, /Leading category\s+action layer/);
  assert.match(outreachExecutionPack, /Next manual\s+batch/);
  assert.match(outreachExecutionPack, /Discovery\s+questions/);
  assert.match(outreachExecutionPack, /Pilot setup\s+checklist/);
  assert.match(outreachExecutionPack, /Integration\s+hold/);
  assert.match(outreachExecutionPack, /Focus leading\s+category/);
  assert.match(outreachExecutionPack, /Pilot setup readiness/);
  assert.match(outreachExecutionPack, /Paid pilot gate/);
  assert.match(outreachExecutionPack, /Not ready for paid pilot/);
  assert.match(outreachExecutionPack, /Week-one owner\s+decision/);
  assert.match(outreachExecutionPack, /Only `Continue` can unlock `Paid pilot\s+proposal`/);
  assert.match(outreachExecutionPack, /Open discovery prep/);
  assert.match(outreachExecutionPack, /Discovery call\s+prep/);
  assert.match(outreachExecutionPack, /Questions to ask/);
  assert.match(outreachExecutionPack, /Pilot success criteria/);
  assert.match(outreachExecutionPack, /Copy discovery call prep/);
  assert.match(outreachExecutionPack, /local_services_discovery_call_prep/);
  assert.match(outreachExecutionPack, /Open day-one setup/);
  assert.match(outreachExecutionPack, /Day-one setup brief/);
  assert.match(outreachExecutionPack, /Business profile lock/);
  assert.match(outreachExecutionPack, /Setup tasks/);
  assert.match(outreachExecutionPack, /Test call plan/);
  assert.match(outreachExecutionPack, /Copy day-one setup brief/);
  assert.match(outreachExecutionPack, /local_services_day_one_setup_brief/);
  assert.match(outreachExecutionPack, /Open daily log/);
  assert.match(outreachExecutionPack, /Pilot daily log/);
  assert.match(outreachExecutionPack, /Daily capture fields/);
  assert.match(outreachExecutionPack, /Daily operating loop/);
  assert.match(outreachExecutionPack, /Copy pilot daily log/);
  assert.match(outreachExecutionPack, /local_services_pilot_daily_log/);
  assert.match(outreachExecutionPack, /Weekly scorecard sync gate/);
  assert.match(outreachExecutionPack, /Open weekly sync checklist/);
  assert.match(outreachExecutionPack, /local_services_weekly_scorecard_sync_checklist/);
  assert.match(outreachExecutionPack, /weeklyScorecardSyncReviewedByService/);
  assert.match(outreachExecutionPack, /Evidence readiness/);
  assert.match(outreachExecutionPack, /manual_weekly_scorecard_sync_gate/);
  assert.match(outreachExecutionPack, /Open week-one review/);
  assert.match(outreachExecutionPack, /Pilot week-one review/);
  assert.match(outreachExecutionPack, /Continue \/ stop decision/);
  assert.match(outreachExecutionPack, /Copy week-one review/);
  assert.match(outreachExecutionPack, /local_services_pilot_week_one_review/);
  assert.match(outreachExecutionPack, /firstRequestOutcomeByProspectKey/);
  assert.match(outreachExecutionPack, /Week-one owner\s+decision state/);
  assert.match(outreachExecutionPack, /Record continue/);
  assert.match(outreachExecutionPack, /Record pause/);
  assert.match(outreachExecutionPack, /Record stop/);
  assert.match(outreachExecutionPack, /weekOneOwnerDecisionByProspectKey/);
  assert.match(outreachExecutionPack, /Proposal\s+approval state/);
  assert.match(outreachExecutionPack, /Approve proposal handoff/);
  assert.match(outreachExecutionPack, /proposalApprovalByService/);
  assert.match(outreachExecutionPack, /Kickoff\s+decision state/);
  assert.match(outreachExecutionPack, /Mark kickoff ready/);
  assert.match(outreachExecutionPack, /kickoffDecisionByService/);
  assert.match(outreachExecutionPack, /Open evidence pack/);
  assert.match(outreachExecutionPack, /Pilot evidence pack/);
  assert.match(outreachExecutionPack, /Week-two evidence pack/);
  assert.match(outreachExecutionPack, /First request outcome/);
  assert.match(outreachExecutionPack, /Open recording checklist/);

  assert.match(demoFrontendServer, /"local-services-demo-recording-checklist\.md"/);
  assert.match(outreachExecutionPack, /Copy evidence pack/);
  assert.match(outreachExecutionPack, /local_services_pilot_evidence_pack/);
  assert.match(outreachExecutionPack, /Week-one owner decision/);
  assert.match(outreachExecutionPack, /week_one_owner_decision_to_evidence_pack/);
  assert.match(outreachExecutionPack, /Open pilot runbook/);
  assert.match(outreachExecutionPack, /Contacted manually/);
  assert.match(outreachExecutionPack, /Reply received/);
});

test("simulation lab prefers runtime governance metadata for the live policy snapshot", () => {
  const workspaceRuntime = readAppShellSource("hooks/useWorkspaceRuntime.tsx");
  const runtimePolicies = readAppShellSource("lib/runtime-simulation-policies.ts");
  const simulationLab = readAppShellSource("components/workspace/SimulationLab.tsx");
  const newReplaySheet = readAppShellSource("components/workspace/NewReplaySheet.tsx");
  const runDetailDrawer = readAppShellSource("components/workspace/RunDetailDrawer.tsx");
  const policyBlurb = readAppShellSource("components/workspace/runDetail/PolicyBlurb.tsx");
  const readme = readRepoSource("README.md");
  const operatorGuide = readRepoSource("docs/operator-guide.md");

  assert.match(workspaceRuntime, /governancePolicy: RuntimeGovernancePolicy \| null;/);
  assert.match(workspaceRuntime, /governanceTemplateCatalog: RuntimeGovernanceTemplateCatalog \| null;/);
  assert.match(workspaceRuntime, /governancePolicyUpdates: RuntimeGovernancePolicyUpdate\[\];/);
  assert.match(workspaceRuntime, /promoteGovernancePolicyTemplate: \(/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*"\/v1\/governance\/policy"/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*"\/v1\/governance\/compliance-template"/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*`\/v1\/governance\/policy\/\$\{encodeURIComponent\(tenantId\)\}\/updates\?limit=12`/);
  assert.match(workspaceRuntime, /fetchRuntimeApi\(\s*"\/v1\/governance\/policy",\s*\{\s*method: "POST"/);
  assert.match(workspaceRuntime, /"x-operator-role": "admin"/);

  assert.match(runtimePolicies, /export function buildSimulationPolicySnapshots/);
  assert.match(runtimePolicies, /const TEMPLATE_POLICY_PREFIX = "policy-template-";/);
  assert.match(runtimePolicies, /function buildRuntimeTemplatePolicyId/);
  assert.match(runtimePolicies, /Template \$\{template\} from \$\{source\}/);
  assert.match(runtimePolicies, /Promote to live to apply it to the operator desk/i);

  assert.match(simulationLab, /governanceTemplateCatalog,/);
  assert.match(simulationLab, /governancePolicyUpdates,/);
  assert.match(simulationLab, /buildSimulationPolicySnapshots\(\s*governancePolicy,\s*governanceTemplateCatalog,\s*governancePolicyUpdates,\s*\)/);
  assert.match(simulationLab, /buildRuntimeSimulationRuns\(cases, policies\)/);
  assert.match(simulationLab, /findCase,/);
  assert.match(simulationLab, /const c = findCase\(run\.caseRef\);/);
  assert.match(newReplaySheet, /policies: PolicySnapshot\[\];/);
  assert.match(newReplaySheet, /policies\.find\(\(policy\) => policy\.id === policyId\)/);
  assert.match(runDetailDrawer, /policies: PolicySnapshot\[\];/);
  assert.match(runDetailDrawer, /findSimulationPolicy\(run\.policyId, policies\)/);
  assert.match(runDetailDrawer, /promoteGovernancePolicyTemplate/);
  assert.match(runDetailDrawer, /Live policy updated to/);
  assert.match(policyBlurb, /policy: PolicySnapshot \| null \| undefined/);
  assert.match(policyBlurb, /Recent governance activity/);

  assert.match(readme, /Simulation Lab` now also overlays the live `policy-current` snapshot/i);
  assert.match(readme, /repo-owned governance runtime data \(`\/v1\/governance\/policy`\)/i);
  assert.match(readme, /`\/v1\/governance\/compliance-template`/i);
  assert.match(readme, /`POST \/v1\/governance\/policy`/i);
  assert.match(operatorGuide, /Simulation Lab policy note:/);
});
