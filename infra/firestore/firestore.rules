rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-side services (Cloud Run / Agent Engine) use IAM and bypass these rules.
    // Keep client direct access blocked by default in this baseline.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
